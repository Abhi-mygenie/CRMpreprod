# CR-001C-L — Final Scope Lock (Pre-Stage C)

**Module:** CR-001C-L (Loyalty)
**Stage:** Final Lock between B.1 Blueprint and Stage C Test Setup
**Date:** 2026-05-22
**Status:** **`cr001c_loyalty_scope_locked_awaiting_stage_c_approval`**

> Single-page summary of every locked decision. After owner replies
> "**proceed to Stage C**", I will write the Stage C Test Setup proposal
> for L1+L2.

---

## 1. Locked Owner Decisions

### From Q-LOYALTY-1..5 (Stage B inputs)
| ID | Decision |
|---|---|
| Q-LOYALTY-1 | `loyalty_enabled` is a **kill-switch** for BOTH migration and realtime |
| Q-LOYALTY-2 | **Wallet stays separate** from points (wallet debit never `$inc`s `total_points_redeemed`) |
| Q-LOYALTY-3 | **Bonuses** (first-visit, birthday, anniversary) **count toward `total_points_earned`** |
| Q-LOYALTY-4 | Off-peak — fix in CR-001C-L *(but later moved to "defer" via D-questions + Q-LB5)* |
| Q-LOYALTY-5 | Historical orders' coupon/wallet data **ignored** in migration |

### From Q-LB1..Q-LB6 (Blueprint phasing) — using recommended defaults per blueprint
| ID | Decision (recommended default) |
|---|---|
| Q-LB1 | **C** — Add **loyalty-migration-specific** config flag `loyalty_clean_slate_recalc` (per-restaurant Mongo field on `loyalty_settings.loyalty_clean_slate_recalc: bool`; env-var equivalent name `LOYALTY_CLEAN_SLATE_RECALC` if a global override is ever needed). Default `false`. Owner sets `true` per restaurant before migration runs. Name is intentionally loyalty-prefixed so it is never confused with a generic "clean slate" knob for other modules. |
| Q-LB2 | **B** — Approve L1+L2 bundled. Gate L3 separately. L4 standalone. L5 last. |
| Q-LB3 | **B** — `core/loyalty.py` owns `calculate_points()` + `calculate_tier()` only. No redemption helpers in this CR. |
| Q-LB4 | **B** — Dead-code removal happens in L5, **after** L1–L4 QA passes. |
| Q-LB5 | **C** — **Defer C9** (off-peak timezone + cross-midnight). Pre-prod is India-only. Separate CR if non-IST onboards. |
| Q-LB6 | **B** — **Defer C8** (tier-upgrade WhatsApp from realtime POS) to next WhatsApp Automation CR. |

> ⚠️ **If you disagree with any Q-LB default above**, reply with the
> override (e.g. `Q-LB1: A` instead of C) and I will revise this lock.
> Otherwise these are final.

### From D1..D8 (edge-case doubts)
| ID | Decision |
|---|---|
| D1 | **C** — Migration `points_transactions` rows carry **original order date** AND are pre-marked `points_expired=True` if older than `expiry_months`. **This `points_expired=True` pre-mark applies ONLY to `points_transactions` rows generated by clean-slate migration recalculation (i.e. produced inside `sync_orders_from_mygenie` under `loyalty_clean_slate_recalc=true`). It does NOT touch any existing/old `points_transactions` rows in the DB.** Existing rows remain governed solely by the points-expiry cron. |
| D2 | **C** — Migration **BLOCKS** for a restaurant if its `loyalty_settings` doc doesn't exist; owner must configure first |
| D3 | **DB will be wiped before go-live.** No R689 conflict. No old test data. Planning assumes empty DB at t=0. |
| D4 | **A** — `wallet_used` does NOT reduce earnable amount; points computed on full `order_amount` (matches realtime) |
| D5 | **A** — L1+L2 deploys to prod in the **same batch** as CR-001A Phase 2 + CR-001D, after this sprint |
| D6 | **A** — Keep `mygenie_synced` flag as-is |
| D7 | **A** — No action needed for missing fields on existing docs (DB wipe handles it per D3) |
| D8 | Heads-up only — I'll use grep to locate exact line numbers at implementation time |

---

## 2. Locked Scope = Phases L1, L2, L3, L4, L5

| Phase | Items shipping | Items deferred | Status |
|---|---|---|---|
| **L1** | F1 (shared helper `core/loyalty.py` with `calculate_points` + `calculate_tier`) | — | Ready |
| **L2** | C1-realtime, C4, C6, C10-POS | — | Ready (bundled with L1) |
| **L3** | C1-migration, C2, C3, C10-migration, C11, **D1 logic (pre-mark expired), D2 logic (block if no settings)**, **Q-LB1 config flag** | — | Ready (separate gate after L1+L2 QA) |
| **L4** | C5, C7 | — | Ready (standalone) |
| **L5** | Dead-code removal per §11 of blueprint | — | Last (after L1–L4 verified) |
| **DEFERRED** | — | C8 (WhatsApp), C9 (timezone) | Out of CR-001C-L |

---

## 3. Approval Bundle Path

1. **L1 + L2 first owner-approval cycle** — foundational + realtime correctness.
   **Stage C (Test Setup) covers L1 + L2 ONLY.** No migration setup. No L3 test setup.
2. **L3 is part of the approved loyalty direction**, BUT **L3 implementation requires a separate owner gate AFTER L1+L2 are merged and QA'd in preview.** Stage C/D/E/F for L3 will be written as a separate cycle once L1+L2 close.
3. **L4 standalone** — mechanical. After L3 closes.
4. **L5 last** — cleanup pass, gated on L1–L4 verified in preview.

Prod deploy: L1+L2 batched with CR-001A Phase 2 + CR-001D, after this sprint (per D5).

---

## 4. Module Boundaries (Re-Confirmed)

Untouched:
- Wallet write-path (only the clean-slate zero-init in `customer_sync` touches `wallet_balance` etc.)
- Coupon write-path (only clean-slate zero-init touches `total_coupon_used`, `last_coupon_used`)
- Dashboard / Visibility (CR-001C-V)
- WhatsApp Automation
- POS webhook schema
- Migration orchestration / batching / R689 scheduling
- CR-002 `pos_request_logs`
- Authentication / `core/auth.py`
- `core/database.py`
- `core/scheduler.py`
- Frontend `CustomerDetailPage.jsx`, `LoyaltySettingsPage.jsx`

---

## 5. Files Touched (Per Phase)

```
L1:   core/loyalty.py (NEW), routers/pos.py (wrap _calculate_points), core/helpers.py (re-export shim)
L2:   routers/pos.py (pos_order_webhook, _save_order_and_transactions, _find_or_create_customer, create_customer)
L3:   routers/migration.py (sync_orders loyalty block), routers/customers.py (sync_customers_from_mygenie new + existing branches, config-flag guard, missing-settings block)
L4:   routers/points.py (create_points_transaction), core/loyalty_jobs.py (birthday + anniversary jobs)
L5:   all of the above (remove wrappers, dead branches, drift constants)
```

---

## 6. Confirmations

- ✅ No code changed by this scope lock
- ✅ No backend / frontend / DB / env touched
- ✅ No migration or sync triggered
- ✅ No deploy / supervisor restart
- ✅ `/app/memory/final/` untouched
- ✅ Baseline docs (`CR_001_INDEX.md`, `CRM_1_0_OPEN_GAPS_REGISTER.md`) untouched
- ✅ Only this lock file created in `planning/`

---

## ⏸ Hard Gate — Owner Action Required

Reply with one of:

1. **"Proceed to Stage C"** → I write the Test Setup proposal for L1+L2 (recommend test restaurant, settings to configure, sequence of test POS orders to push). Still no code.
2. **"Override Q-LB#: X"** → I revise this lock (e.g. if you want Q-LB1: A instead of C).
3. **"Hold — clarify [...]"** → I clarify the specific item before proceeding.

Status: `cr001c_loyalty_scope_locked_awaiting_stage_c_approval`
