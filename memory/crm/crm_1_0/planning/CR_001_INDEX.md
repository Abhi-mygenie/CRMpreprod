# CRM 1.0 — CR-001 Index

Single source of truth for CR-001 family (POS realtime + migration sync work).

> **ROI Measurement Sprint — Relocated to its own folder (updated 2026-02-26):** The ROI Measurement Sprint now lives at `/app/memory/crm/crm_roi_sprint/` (sibling to this closed sprint). Master register: `/app/memory/crm/crm_roi_sprint/00_register/ROI_MEASUREMENT_CR_REGISTER.md`. Five registered CRs (all currently in Phase 0 Discovery): `CR-005 Coupon UI / Usage / Visibility Bugs`, `CR-002B Customer CRM Benefits Data Visibility Fix`, `POS-CRM Customer Cross-Sell Upsell Suggestions API`, `CR-003 Coupon Analytics Dashboard` (pointer — legacy planning doc remains in this folder), and `CR-004 WhatsApp Utility + Marketing Message Integration`. CRM 1.0 baseline (closed 2026-05-26) is **not** modified by the new sprint. Note: the new WhatsApp CR reuses the `CR-004` code also used by `./CR_004_LOYALTY_DEFAULTS_AND_UI_BUG_FIX.md`; owner may renumber it before Phase 0 Discovery starts.

---

## CR-001A — Realtime POS Webhook Schema Mismatch (ISSUE-09)

| Phase | Title | Status | Date | Notes |
|---|---|---|---|---|
| Phase 1 | Forward-only alias mapping on `POST /api/pos/orders` | ✅ **`cr001a_phase_1_closed_live_on_prod`** | Closed 2026-05-22 09:10:46 UTC | Verified live on prod via order `868899` (7/7 alias checks pass) |
| Phase 2 | Add `room_info` + `associated_order_ids` to `POSOrderWebhook` (forward-only) | ✅ **`cr001a_phase_2_and_cr001d_qa_passed_with_runtime_limitations`** | Implemented & QA passed 2026-05-22 ~10:07 UTC | Implemented in same PR as CR-001D. Static QA 12/12, order_doc 9/9, live route accepts schema (HTTP 401). Live prod closure pending natural room order after prod deploy + `pos-backend` restart. See `/app/memory/crm/crm_1_0/implementation/CR_001A_PHASE_2_AND_CR_001D_IMPLEMENTATION_REPORT.md` |

### CR-001A Phase 1 — Artifacts
- Implementation report: `/app/memory/crm/crm_1_0/implementation/CR_001A_PHASE_1_IMPLEMENTATION_REPORT.md`
- QA report: `/app/memory/crm/crm_1_0/qa/CR_001A_PHASE_1_QA_REPORT.md`
- Code: `/app/backend/routers/pos.py` (lines 1–4, 949–1016, 1019–1102)
- Static QA harness: `/tmp/cr_001a_qa.py` (12 / 12 PASS)
- On-host verifier: `/app/memory/crm/crm_1_0/qa/cr_001a_check.sh`
- Live proof: `pos_request_logs` + `orders.id=crm-phase-loyalty` (order `868899`)

### CR-001A Phase 1 — Owner decisions captured
- Forward-only. No historical backfill, no mutation of old records, no
  `item_data_lost` marker, no cleanup script.
- Migration may overwrite older realtime-broken rows opportunistically when
  the restaurant's `Sync Orders` is run — that is owner-driven, not a CR-001A
  action.

### CR-001A Phase 1 — Key lesson (deployment)
- In a multi-app pm2 setup, the **service whose name matches the endpoint
  family** is the one whose worker must be restarted. `/api/pos/*` is served
  by `pos-backend` (pm2 id 7), not `crm-backend` (id 2). Restarting the wrong
  service silently fails the deploy.
- File on disk being updated ≠ running Python process having the new model.
  Pydantic builds model classes at import time; the worker must be killed
  and re-spawned to load new model definitions.
- Verifier script `cr_001a_check.sh` should be run on prod after every
  schema-affecting deploy to catch this case in <2 s.

---

## CR-001B — Migration Sync (R689)

| Phase | Title | Status |
|---|---|---|
| Phase 2 | R689 sync hardening / F-series fixes (F9 persistent `migration_sync_logs`, F12 dedup) | ⏳ Owner-driven, in flight |

### Snapshot at CR-001A Phase 1 closure (2026-05-22 09:10 UTC)
- R689 order_sync still **running** since 03:44 UTC
- progress: page 145 / 329
- synced 347 · updated 3278 · failed 0
- untouched by CR-001A work ✅

### Guard rails (still active)
- `migration.py` must not be modified by CR-001A Phase 2 work either.
- `migration_sync_logs` collection is the persistence layer (F9). Do not
  drop/truncate.

---

## CR-001C — TBD

Not yet defined.

### CR-001C-L (Loyalty) — Bridge Phase LX-A — POS BUG-108 API Contract Alignment

| Item | Status | Date | Notes |
|---|---|---|---|
| LX-A — Loyalty API response alignment for POS BUG-108 | ✅ **`cr001c_lx_a_loyalty_pos_contract_patched_qa_passed_in_preview`** | 2026-05-23 | 4 files touched (`backend/{models/schemas.py, core/helpers.py, core/loyalty.py, routers/pos.py}`). Static QA 63/63, live read-only smoke 5/5 on restaurant `18march`. POS handoff banner flipped to GREEN-LIGHT. See `implementation/CR_001C_LX_A_IMPLEMENTATION_REPORT.md` + `qa/CR_001C_LX_A_QA_REPORT.md`. |
| **LF-MERGE — Loyalty Flag Merge (deprecate `loyalty_clean_slate_recalc`)** | ✅ **`cr001cl_lf_merge_complete_qa_passed_in_preview`** | **2026-05-23** | **Owner-driven follow-up.** Real owner-triggered migration on `Jeh's Nest` revealed that the visible "Loyalty Program ON" toggle (`loyalty_enabled`) did NOT control migration clean-slate recompute — a hidden flag `loyalty_clean_slate_recalc` (no UI exposure) did. Per owner Option 3, both flags merged: `loyalty_enabled` now drives BOTH realtime POS earning AND migration clean-slate recompute. `loyalty_clean_slate_recalc` deprecated (still on schema for backward compat, no longer read). 3 files touched (`backend/{routers/migration.py, routers/customers.py, models/schemas.py}`). Static QA 37/37, LX-A regression 63/63. Real-data L3 validation pending owner Revert → Sync Again on `Jeh's Nest`. See `implementation/CR_001C_L_LF_MERGE_IMPLEMENTATION_REPORT.md` + `qa/CR_001C_L_LF_MERGE_QA_REPORT.md` + `qa/CR_001C_L_LOYALTY_L3_REAL_MIGRATION_VERIFICATION_REPORT.md`. |
| **BUG-L3-001 — D1 expired pre-mark fix (naive vs tz-aware compare)** | ✅ **`cr001c_l_bug_l3_001_closed`** | **2026-05-23** | R2 verification surfaced 28 PT rows NOT pre-marked expired. Fix: coerce naive `od_dt` to UTC; narrow `except` to `ValueError`. **R3 re-verification PASSED** — 28 expired rows correctly marked, balances reconcile 209/209. See `qa/CR_001C_L_LOYALTY_L3_REAL_MIGRATION_VERIFICATION_REPORT_R3.md`. |
| **L3 Real Migration Validation** | ✅ **`cr001c_loyalty_l3_real_migration_validated_in_preview`** | **2026-05-23** | All 8 L3 checks pass on Jeh's Nest after BUG-L3-001 fix + owner re-migration. 209 customers, 233 orders, 98 PT earn rows (28 expired/70 active). `total_points(379) = earned(753) − expired(374)`. L3 closed in preview. See `qa/CR_001C_L_LOYALTY_L3_REAL_MIGRATION_VERIFICATION_REPORT_R3.md`. |
| **LR — POS Loyalty Redeem API** | ✅ **`cr001c_lr_pos_loyalty_redeem_api_qa_passed`** | **2026-05-23** | New `POST /api/pos/loyalty/redeem` endpoint. Tier-aware ratio (LX-A helper), idempotency-safe (replay + `IDEMPOTENCY_CONFLICT`), `loyalty_enabled` kill-switch, min/max guardrails (auto-cap per Q-LR6), no tier downgrade on redeem (Q-LR1). One file touched (`backend/routers/pos.py`), one QA harness added (`backend/tests/qa_cr001c_lr_redeem.py`). Static QA 36/36 PASS, LX-A regression 6-key blob still strict. See `implementation/CR_001C_LR_POS_LOYALTY_REDEEM_API_IMPLEMENTATION_REPORT.md` + `qa/CR_001C_LR_POS_LOYALTY_REDEEM_API_QA_REPORT.md`. |
| **LR Correction — Redemption Trigger + `/max-redeemable` Alignment** | ✅ **`cr001c_lr_correction_qa_passed`** | **2026-05-24** | Owner correction: redemption now commits when CRM receives the final order payload, not on cashier click. New shared helpers in `core/loyalty.py` (`redeem_loyalty_points` + `compute_max_redeemable`). 5 callers (`/pos/orders` adds 3 optional fields and inserts redeem-before-earn; `/pos/webhook/payment-received` embedded block routed through helper; `/pos/loyalty/redeem` and `/pos/max-redeemable` are now thin wrappers — `/max-redeemable` is now tier-aware, kill-switch-aware, no fake fallbacks, accepts `customer_id` OR `cust_mobile`, echoes full context, structured `error.code`). Static QA 52/52 PASS (36 original + 15 correction-specific + 1 alias-addendum). LX-A 6-key blob regression still strict. Standalone `/pos/loyalty/redeem` retained for testing only; original handoff doc marked superseded. See `implementation/CR_001C_LR_CORRECTION_IMPLEMENTATION_REPORT.md` + `qa/CR_001C_LR_CORRECTION_QA_REPORT.md` + `handoff/CR_001C_LR_REDEMPTION_FINAL_PAYLOAD_HANDOFF_TO_POS.md`. |
| **LR Alias Addendum (POS-legacy `used_loyalty_point` acceptance)** | ✅ **`cr001c_lr_alias_addendum_qa_passed`** | **2026-05-24** | Live R689 testing revealed POS sends `used_loyalty_point` (legacy singular) instead of canonical `loyalty_points_used`. Added Pydantic `validation_alias=AliasChoices("loyalty_points_used","used_loyalty_point","used_loyalty_points")` on the `POSOrderWebhook` field (same precedent as CR-001A Phase 1 `order_created_at ← created_at`). One file touched (`backend/routers/pos.py`, single field declaration). 1 new QA assertion (QA-21). No frozen Q-CORR decision altered. **Static QA: 52 / 52 PASS** (alias path commits identically to canonical). Aliases retire in L5 cleanup once POS migrates. See plan §12 + handoff §3.3/§6/§7 + implementation report §10. |
| **LR R689 Realtime Investigations (read-only)** | ✅ **`cr001c_lr_r689_realtime_payload_received_and_processed`** (closed 2026-05-26) | **2026-05-24 → 2026-05-26** | _Original 2026-05-24 finding:_ 7 R689 test orders (`868917`, `868924`, `868925`, `868928`, `868929`, `868931`, `008976`) all missing from `pos_request_logs` and `orders`. POS not calling `/api/pos/orders` at bill-finalize. _Closure 2026-05-26:_ POS now sends qualifying payloads — 15 / 15 most-recent `/api/pos/orders` entries are contract-compliant; 5 of 15 carry positive `loyalty_points_used` (500 – 4619). The 4 `analysis/CR_001C_LR_R689_*.md` reports are now superseded. See `handoff/CR_001C_POS_CONTRACT_COMPLIANCE_CLOSURE_2026_05_26.md`. |
| **L4 Cron-Only — Birthday + Anniversary Bonus Parity** | ✅ **`cr001c_loyalty_l4_cron_only_qa_passed`** | **2026-05-24** | 4 fixes per bonus job: (1) `$inc total_points_earned` alongside `total_points` (Q-L4-2), (2) tier recompute upgrade-only (Q-L4-3), (3) `loyalty_enabled` master kill-switch guard (Q-L4-4), (4) atomic `$inc` replacing read-then-`$set`. PT rows get `points_expired=False`. 1 file touched (`core/loyalty_jobs.py`, +`_tier_rank` helper). Admin/manual redeem parked to later sprint. Datetime safety confirmed safe (`.date()` pattern, no BUG-L3-001 risk). Static QA: **17/17 PASS**. See `implementation/CR_001C_L_LOYALTY_L4_CRON_ONLY_IMPLEMENTATION_REPORT.md` + `qa/CR_001C_L_LOYALTY_L4_CRON_ONLY_QA_REPORT.md`. |
| **BLOCKER — Final Realtime Redemption QA** | ✅ **`cr001c_lr_realtime_order_redemption_verified`** (closed 2026-05-26) | **2026-05-24 → 2026-05-26** | _Original 2026-05-24 status:_ CRM-side complete (52/52 LR + 17/17 L4); blocked on POS sending `used_loyalty_point` / `loyalty_points_used` + actual `order_amount`. _Closure 2026-05-26:_ POS shipped the contract fixes. Live-DB evidence on R689 — **76 redeem `points_transactions` rows, 8,633 points redeemed**, latest 2026-05-26 05:16:06 UTC. Sample qualifying orders: 869042 (500 pts), 869036 (630), 869033 (630), 869026 (4619), 869030 (4619). All payloads use top-level loyalty fields, no nested wrappers. See `qa/CR_001C_LR_REALTIME_ORDER_REDEMPTION_CLOSURE_2026_05_26.md`. |
| **CONTRACT FIX — `payment_status` gate removed** | ✅ **`cr001c_lr_payment_status_gate_removed`** | **2026-05-25** | Removed the `if order_data.payment_status != "success": reject` block in `_validate_order` (`backend/routers/pos.py` lines 600–605). Default on `POSOrderWebhook.payment_status` changed from `"success"` to `Optional[str] = None` (line 1196 post-edit). Docstring on `_validate_order` (line 585) updated to drop "payment status" from description. CRM now accepts orders with any `payment_status` value (e.g. `"paid"`, `"pending"`, missing) — value is stored as-sent (line 890 unchanged). **Risk:** very low — restriction removal only. Duplicate-order protection (line 600+ check on `(pos_id, pos_restaurant_id, pos_order_id)`) and API-key auth intact. Backward-compatible: orders with `"success"` continue to work. Unblocks POS bills that flag `payment_status="paid"` on the final order webhook. `migration.py` line 239 already passes through `payment_status` as-is. No DB migration, no schema change to stored docs, no env change. Live: backend reloaded clean, `/api/health` → 200. |
| **V3-A Happy Hour Admin UI wired to production `/coupons`** | ✅ **`cr001c_coupon_v3a_admin_ui_implementation_qa_passed`** | **2026-05-25** | "Happy Hour" tile at `/coupons` flipped from "Soon" to live. Owner can author / edit / list / toggle / delete time-window coupons (weekday selector × 7 buttons, start/end time, IANA timezone). **Single file touched:** `frontend/src/pages/CouponsPage.jsx` (+74 / -7 LOC, 8 self-contained edits per plan). **Critical correction over prior guide:** `offer_type` MUST remain `"simple"` (backend validator `_v3a_validate_offer_type` rejects `"time_window"`); edit-mode detection keys off `valid_days`/`start_time`/`end_time` presence. **QA:** 12/12 PASS (admin round-trip + edit + invalid HH:MM rejection + overnight window + V1 regression + visual confirmation via Playwright screenshots). Backend untouched (still 31/31 V3-A QA + 211/211 combined V1→V3-C). No DB / env / dependency / supervisor change. Reports: `planning/CR_001C_C_COUPON_V3A_UI_WIRING_PLAN.md`, `implementation/CR_001C_C_COUPON_V3A_ADMIN_UI_IMPLEMENTATION_REPORT.md`, `qa/CR_001C_C_COUPON_V3A_ADMIN_UI_QA_REPORT.md`. Next: V3-B BOGO/BXGY UI wiring. |
| **V3-B BOGO / BXGY Admin UI wired to production `/coupons`** | ✅ **`cr001c_coupon_v3b_admin_ui_implementation_qa_passed`** | **2026-05-25** | "BOGO / BXGY" tile flipped from "Soon" to live. Single tile opens one form with a `bogoMode` toggle: **BOGO same-item** (`offer_type="bogo"`, 1 picker) or **BXGY different-item** (`offer_type="bxg"`, 2 pickers). Benefit selector: Free / % Off / Rs. Off. Advanced collapsible exposes `max_applications`, `allow_repeat`, `apply_to_cheapest_item` (mutex with `apply_to_highest_item`), `pos_instruction`. **Critical correction over prior guide:** `offer_type` must be `"bxg"` NOT `"bxgy"` (the latter is rejected by `_v3a_validate_offer_type`); forbidden-string check `grep -c '"bxgy"'` returns 2 hits — both in safety comments, zero in payload code. **Files:** `frontend/src/pages/CouponsPage.jsx` (+170 / -10 LOC, primary) + 1-line `label` prop on `ItemSelector` (same file). New helpers `nthSuffix()` and `v3CouponLabel()` produce humanised list-row labels ("Buy 1 Get 1 Free", "Every 3rd Free") — covers V3-C as a free bonus. **QA:** 15/15 PASS + 4/4 regression PASS (BOGO + BXGY % create round-trips · invalid `buy_quantity=0` → HTTP 422 · `SEED_V3B_BXGY_PCT` rehydrates in BXGY mode with `50` in % field · filter "BOGO / BXGY" returns 5 SEED_V3B_* with humanised labels · V1/V2/V3-A regression clean · ESLint clean). Backend untouched (still 49/49 V3-B QA + 211/211 combined). No DB / env / dependency / supervisor change. Reports: `discovery/CR_001C_C_COUPON_V3B_UI_WIRING_GAP_DISCOVERY.md`, `planning/CR_001C_C_COUPON_V3B_UI_WIRING_PLAN.md`, `implementation/CR_001C_C_COUPON_V3B_ADMIN_UI_IMPLEMENTATION_REPORT.md`, `qa/CR_001C_C_COUPON_V3B_ADMIN_UI_QA_REPORT.md`. Lesson learned: 2/10 search-replaces silently lost to a hot-reload race window — recovery took ~10 min once caught via `grep` validation. **Next:** V3-C Every Nth Item UI wiring (last remaining "Soon" tile). |
| **POS-PERF-1 — `/api/pos/coupons/available` N+1 query fix (14.3× speedup)** | ✅ **`crm_1_0_pos_perf_1_list_available_coupons_n1_fix_qa_passed`** | **2026-05-25** | POS team reported endpoint "not working" — root cause: ~16.5 s response time (cross-region MongoDB N+1 over 50 useless `count_documents` calls all returning 0 for R689). **Fix:** surgical perf refactor of `backend/core/coupon.py` (single file, +74/-12 LOC). `validate_coupon_for_customer` gains 3 additive optional kwargs (`_precomputed_coupon`, `_precomputed_usage_count`, `_precomputed_user_tz`) with `None` defaults — used only by `list_available_coupons` to amortise DB hits. The bulk caller now does **3 pre-fetches** (coupons list, user_tz once, `coupon_usage.aggregate()` grouped by coupon_id) and a **DB-free loop**. **Result:** 16.48 s → 1.15 s mean (~14.3×). **Frozen contract guarantee (plan §0):** ZERO POS-side change — URL, headers, params, status, JSON shape, all 24 coupon fields identical. **Verified byte-identical** via raw diff (0 differing lines) + sorted-JSON diff (0 lines after `next_window_start` strip). **QA:** 211/211 combined coupon harnesses PASS (V1 45 + V2 45 + V3-A 31 + V3-B 49 + V3-C 41) + 3 cold/3 warm timing runs + `/api/health` 200. No DB schema / index / env / dependency / supervisor / contract change. 5 other callers of `validate_coupon_for_customer` (POS validate, /orders commit, /max-redeemable, /loyalty/redeem, /pos/coupons/validate) unchanged in behaviour. Reports: `planning/CR_POS_PERF_1_LIST_AVAILABLE_COUPONS_N1_FIX_PLAN.md` (🔒 frozen), `implementation/CR_POS_PERF_1_LIST_AVAILABLE_COUPONS_N1_FIX_IMPLEMENTATION_REPORT.md`, `qa/CR_POS_PERF_1_LIST_AVAILABLE_COUPONS_N1_FIX_QA_REPORT.md`. POS team can use existing curl as-is. |
| **V3-C Every-Nth Admin UI wired to production `/coupons`** | ✅ **`cr001c_coupon_v3c_admin_ui_qa_passed`** | **2026-05-26** | Tile `every_nth` `enabled: true` (`CouponsPage.jsx` L70). Production drawer renders all V3-C fields: `nth_item_number` (min 2 via Pydantic validator), `nth_discount_type` ∈ {free, percentage, flat}, conditional `nth_discount_value`, eligibility (items + categories), exclusions, `max_applications`, `allow_repeat`, mutex `apply_to_cheapest_item` / `apply_to_highest_item`, `pos_instruction`. Backend create/update endpoints use `model_dump()` → all V3-C fields persist losslessly. **QA:** 42/42 PASS (38 live API smoke + 4 engine smoke) on isolated synthetic user against live prod MongoDB. Tests cover create/list/edit/toggle/delete for V3-C FREE + PERCENT + FLAT; Pydantic validator rejection of `nth_item_number=1` and `nth_discount_type='bogus'` (both → 422); engine semantics (n=3 with 3 items → 100 off; 2 items → `NTH_REQUIREMENT_NOT_MET`; 6 items + `allow_repeat=True` → 200 off); regression V1 flat + V3-A time-window + V3-B BOGO all green. Full cleanup — zero residual artefacts in production DB. Report: `qa/CR_001C_C_COUPON_V3C_ADMIN_UI_QA_REPORT.md`. |

L1+L2+L3 status preserved verbatim in their existing reports
(`cr001c_loyalty_l3_migration_parity_qa_passed`).

### Closure 2026-05-26 — POS Contract + LR Realtime Redemption + V3-C Admin UI

Three closures recorded on 2026-05-26 (live-verified, no doc-only assumptions):

| Closure | Tracker | Evidence doc |
|---|---|---|
| POS contract compliance — all 7 violations resolved | `cr001c_pos_contract_compliance_closed_pos_shipped_2026_05_26` | `handoff/CR_001C_POS_CONTRACT_COMPLIANCE_CLOSURE_2026_05_26.md` |
| LR realtime order redemption — verified live on R689 | `cr001c_lr_realtime_order_redemption_verified` | `qa/CR_001C_LR_REALTIME_ORDER_REDEMPTION_CLOSURE_2026_05_26.md` |
| V3-C Every-Nth Admin UI — 42/42 QA PASS | `cr001c_coupon_v3c_admin_ui_qa_passed` | `qa/CR_001C_C_COUPON_V3C_ADMIN_UI_QA_REPORT.md` |

Effect: blockers B1 (POS contract), B2 (POS not calling `/api/pos/orders` with loyalty fields), B3 (Menu `product.id` vs POS `item_id`) and B5 (V3-C Admin UI QA evidence) from the 2026-05-25 consolidation are all closed. The 4 `analysis/CR_001C_LR_R689_*.md` reports are now superseded.

**L3 real migration validation is CLOSED in preview — validated on TWO restaurants.**
Status: `cr001c_loyalty_l3_real_migration_validated_in_preview`.

| Restaurant | Status | Key stats |
|---|---|---|
| Jeh's Nest (R3) | ✅ PASS | 209 customers, 98 PT recalc, 28 expired, 5% earn. See `qa/CR_001C_L_LOYALTY_L3_REAL_MIGRATION_VERIFICATION_REPORT_R3.md`. |
| Kunafa Mahal (R689) | ✅ PASS | 2034 customers, 201 PT recalc, 50% earn, tier evolution (Gold/Silver). See `qa/CR_001C_L_LOYALTY_L3_REAL_MIGRATION_VERIFICATION_REPORT_R689.md`. |

---

## CR-001C-C (Coupon) — Pre-Implementation Planning

| Item | Status | Date | Notes |
|---|---|---|---|
| **Admin UI Discovery Report** | ✅ `cr001c_coupon_admin_ui_discovery_complete_waiting_owner_ui_decisions` | 2026-05-25 | Full gap analysis: existing UI covers V1 base (12/50+ fields, ~24%). Create route drops V2+ fields. Verdict: Option B (reuse list, rebuild create/edit wizard). 8 owner questions gated. Backend fix needed: `coupons.py` create to use `model_dump()`. See `discovery/CR_001C_C_COUPON_ADMIN_UI_WHAT_EXISTS_AND_GAP_REPORT.md`. |
| **POS API Handoff Summary (V1+V2+V3-A+V3-B+V3-C)** | ✅ `cr001c_coupon_pos_api_handoff_summary_ready_after_v3c_preview_qa` | 2026-05-25 | Complete POS-facing API handoff document covering all 5 coupon phases. 3 endpoints documented (`/available`, `/validate`, `/orders` coupon commit). 27 error codes catalogued. POSCartItem contract, business rules, deprecated `/apply` endpoint, 5-phase POS implementation checklist, open items. Written from inspected code (source of truth), not memory. See `handoff/CR_001C_C_COUPON_POS_API_HANDOFF_SUMMARY.md`. |
| Existing-System Capability Audit | ✅ `cr001c_coupon_existing_system_capability_audit_complete_waiting_owner_decisions` | 2026-05-24 | 13 files + 3 collections inspected. ORDER_FLAT/PERCENTAGE partially supported. No item-level/BOGO/happy-hour. `GET /pos/coupons/available` missing. Validate uses query params not JSON body. See `planning/CR_001C_C_COUPON_EXISTING_SYSTEM_CAPABILITY_AUDIT.md`. |
| Scrap vs Keep Architecture Decision | ✅ `cr001c_coupon_scrap_vs_keep_decision_option_b_hybrid_rebuild_recommended` | 2026-05-24 | **Option B** — keep skeleton (collections + admin CRUD), rebuild POS contract + coupon engine. See `planning/CR_001C_C_COUPON_SCRAP_VS_KEEP_DECISION.md`. |
| V1 Owner Decisions | ✅ `cr001c_coupon_v1_owner_decisions_frozen_ready_for_implementation_plan` | 2026-05-24 | All 6 decisions frozen: Q1=A (ORDER_FLAT+ORDER_PERCENTAGE only); Q2=C (stacking with loyalty allowed only if coupon config permits, `stackable_with_loyalty:bool=False` default); Q3=D (wallet stacking deferred to CR-001C-W); Q4=B (usage recorded only at final `/api/pos/orders` payload); Q5=C (POS applies coupon and sends actual coupon_code + coupon_discount + order_total in final payload, CRM validates/records on POS-sent values — same final-commit pattern as loyalty); Q6=B (V1 flat/pct, V2 item/category, V3 BOGO/happy-hour). See `planning/CR_001C_C_COUPON_V1_OWNER_DECISIONS.md`. |
| V1 Implementation Plan | ✅ `cr001c_coupon_v1_implementation_plan_ready_for_owner_approval` | 2026-05-24 | New service `core/coupon.py` (5 fns: `normalize_coupon_type`, `compute_coupon_discount`, `validate_coupon_for_customer`, `list_available_coupons`, `record_coupon_usage_for_order`). New `GET /api/pos/coupons/available`. Rebuilt `POST /api/pos/coupons/validate` (JSON body + structured `error.code` × 9 codes). Final-order recording in `pos_process_order` (idempotent on `order_id` via partial unique index on `coupon_usage`). Backwards-compatible schema additions (`title`, `coupon_type`, `stackable_with_loyalty`). Analytics aligned to union `coupon_usage` + `coupon_transactions`. 28 QA cases planned. 9 admin CRUD endpoints unchanged. Loyalty/Wallet untouched. See `planning/CR_001C_C_COUPON_V1_IMPLEMENTATION_PLAN.md`. Plan approved 2026-05-24 with 7 owner clarifications (Addendum A.1–A.7). |
| V1 Implementation | ✅ `cr001c_coupon_v1_implementation_qa_passed_in_preview` | 2026-05-24 | All 32 planned QA cases pass (**45/45 assertions PASS, 0 FAIL**). Live HTTP smoke confirmed for `/api/pos/coupons/available` + `/api/pos/coupons/validate` (success + EXPIRED error case). Indexes created: `coupon_usage.(user_id, order_id)` unique partial + 2 lookup indexes; `coupons.(user_id, code)` unique. 8 files changed (3 new — `core/coupon.py`, `tests/seed_coupon_v1_fixtures.py`, `tests/qa_cr001c_c_coupon_v1.py`; 5 extended — `models/schemas.py`, `routers/pos.py`, `services/analytics_service.py`, `server.py`). 9 admin CRUD endpoints in `routers/coupons.py` UNTOUCHED. Loyalty + Wallet UNTOUCHED. See `implementation/CR_001C_C_COUPON_V1_IMPLEMENTATION_REPORT.md`. |
| V2 Plan (Item/Category) | ✅ `cr001c_coupon_v2_item_category_plan_ready_for_implementation_approval` | 2026-05-24 | Item-level + category-level coupons via `discount_scope` discriminator × existing `discount_type` (4 new effective types: ITEM_FLAT, ITEM_PERCENTAGE, CATEGORY_FLAT, CATEGORY_PERCENTAGE). Backward-compatible — V1 unchanged. New `validate` body adds optional `items[]` (required for V2 scopes). 5 new error codes (`MISSING_ITEMS_FOR_*`, `NO_ELIGIBLE_*_IN_CART`, `MIN_ITEM_QTY_NOT_MET`). Item matching: `food_id` (primary) + `item_id` (secondary). Category matching: `category_id` → `category_name` → `item_category` (live-data fallback). `GET /available` stays query-only with `requires_cart_validation` hint (OQ-1 APPROVED). One coupon per order retained (OQ-2). **Total discount only — no per-line allocation in V2 (OQ-3 APPROVED).** Idempotency, variance tolerance, stacking, admin CRUD, loyalty/wallet — all preserved. 77 QA assertions planned (45 V1 regression + 32 V2). Addendum B freezes payload examples, matching priorities, eligible_subtotal math (incl. unit_price/line_total fallback), flat cap, percentage+max_discount cap, per-line `max_applicable_qty`, non-blocking final-order revalidation, and explicit V1 regression requirement. See `planning/CR_001C_C_COUPON_V2_ITEM_CATEGORY_PLANNING.md` (incl. Addendum B). |
| V3-C Every-Nth Item Implementation + QA | ✅ `cr001c_coupon_v3c_every_nth_implementation_qa_passed_in_preview` | 2026-05-25 | **V3-C Path Alpha implemented and QA passed in preview.** Previous agent died mid-task; continuation agent verified existing code, created missing QA harness (41 assertions), ran full regression. **Result:** V1 **45/45** + V2 **45/45** + V3-A **31/31** + V3-B **49/49** + V3-C **41/41** = **211/211 PASS**. 6 files changed (1 new). No DB migration, no new indexes, no env change. **Files UNTOUCHED:** `routers/coupons.py`, `core/loyalty.py`, wallet, migration, `coupon_transactions`, `/app/memory/final/`. Reports: `implementation/CR_001C_C_COUPON_V3C_EVERY_NTH_IMPLEMENTATION_REPORT.md`, `qa/CR_001C_C_COUPON_V3C_EVERY_NTH_QA_REPORT.md`. |
| V3-C Every-Nth Item Planning + Owner Gate | ✅ `cr001c_coupon_v3c_every_nth_plan_ready_for_implementation_approval` | 2026-02 | **Owner decisions FROZEN 2026-02** — owner accepted all 12 recommended defaults (Q1=D full item+category every-Nth with free/%/flat + repeat + max_applications; Q2=A mathematical floor(eligible_total/N) — POS sequence NOT consulted; Q3=A cheapest unit; Q4=C free + percentage + flat; Q5=C `allow_repeat` field default `true`; Q6=A support `max_applications`; Q7=A include category-level; Q8=B total + `benefit_items` summary; Q9=A non-blocking final-order failure; Q10=A allow time-window + Every-Nth composition; Q11=B `pos_instruction` failure-only; Q12=A kickoff after plan). **Path Alpha selected — single-shot V3-C, ~1.5–2 working days.** **In scope:** same-item every-Nth (free / percentage / flat), category-level every-Nth, `max_applications`, `allow_repeat`, reuse V2/V3-B `apply_to_cheapest_item` / `apply_to_highest_item`. **Out:** free-item instruction-only (→ V3-D), combo (parked V4), CRM/POS auto-add, multi-coupon, wallet cashback, loyalty redemption, coupon reversal, admin UI exposure, variant matching, sequence-aware Nth. **Locked invariants preserved:** one coupon per order; benefit item from in-cart items only (no auto-add); Every-Nth is mathematically `floor(eligible_total / N)` — POS line order NOT consulted (Q2=A locks this); final-order failure non-blocking (Q9=A re-affirms V3-B Q9 / V3 OQ-V3-8); V3-A time-window pre-check composes automatically; loyalty stacking via `stackable_with_loyalty`; wallet untouched; 9 admin CRUD endpoints untouched; `coupon_transactions` legacy untouched. **Engine readiness:** `offer_type="nth_item"` already accepted by V3-A Pydantic enum (no schema-enum change). V3-B helpers `_v3b_line_matches_lists` / `_v3b_expand_units` / `_v3b_select_get_units` / `_v3b_apply_caps` / `_v3b_summarise_lines` directly reusable — Every-Nth ≡ BXGY with `buy_q=N-1, get_q=1, same_item=true`. `POSCartItem` already exposes all needed fields — no POS protocol change. **Data model (all optional, no migration):** new fields `nth_item_number` (≥2 validator), `nth_discount_type` (free/percentage/flat validator), `nth_discount_value`; reuses V2 `eligible_food_ids` / `eligible_item_ids` / `eligible_category_ids` / `eligible_category_names` (single pool, no buy/get split); reuses V3-B `max_applications`, `allow_repeat`, `apply_to_cheapest_item`, `apply_to_highest_item`, `pos_instruction`. `coupon_usage` gains `nth_item_number`, `nth_discount_type`, `nth_discount_value`, new `eligible_match_summary` field; reuses V3-B `applied_applications`, `benefit_items`, `computed_discount`, `discount_mismatch`. **Error codes (5 new):** `MISSING_ITEMS_FOR_EVERY_NTH_COUPON`, `NTH_REQUIREMENT_NOT_MET`, `NO_ELIGIBLE_NTH_ITEMS_IN_CART`, `EVERY_NTH_CONFIG_INVALID`, `UNSUPPORTED_NTH_BENEFIT_TYPE`; `MAX_APPLICATIONS_REACHED` intentionally NOT introduced (cap, not gate — consistent with V3-B). Total error-code surface after V3-C = **27**. **Analytics:** additive `nth_item_usage` block (`orders`, `total_applications`, `discount_amount`, `benefit_units_given`, `by_nth_number{N: count}` distribution); promotes `breakdown_by_offer_type.nth_item` from 0 to real values. **QA:** ~33 V3-C assertions planned in NEW `qa_cr001c_c_coupon_v3_c_every_nth.py`. V1 45/45 + V2 45/45 + V3-A 31/31 + V3-B 49/49 regression gates enforced → combined post-V3-C target **~203 PASS**. **Files:** plan + owner-decision freeze in `planning/CR_001C_C_COUPON_V3C_EVERY_NTH_PLANNING_AND_OWNER_GATE.md` (Addendum E added 2026-02). Planned implementation extends `core/coupon.py` (~120 LOC), `models/schemas.py` (~50 LOC), `routers/pos.py` (~25 LOC), `services/analytics_service.py` (~60 LOC), `tests/seed_coupon_v1_fixtures.py` (~120 LOC); NEW `tests/qa_cr001c_c_coupon_v3_c_every_nth.py` (~550 LOC). No DB migration, no new index, no new dependency, no env change. No code, DB, env, migration, or deployment changes by this owner-freeze step. Implementation NOT started; awaiting owner's explicit "begin V3-C implementation" trigger. On kickoff → flips to `cr001c_coupon_v3c_every_nth_implementation_in_progress`; on QA pass → `cr001c_coupon_v3c_every_nth_implementation_qa_passed_in_preview`. |
| V3-B BOGO / Buy-X-Get-Y Implementation + QA | ✅ `cr001c_coupon_v3b_bogo_bxgy_implementation_qa_passed_in_preview` | 2026-02 | **V3-B Path Alpha implemented and QA passed in preview.** **Result:** V1 **45/45** + V2 **45/45** + V3-A **31/31** + V3-B **49/49** = **170/170 PASS** (V3-B harness expanded planned ~32 → 49 assertions). **Engine:** new V3-B branch in `core/coupon.py` (helpers `_v3b_normalize_offer_type`, `_v3b_get_buy_lists/_get_lists`, `_v3b_line_matches_lists`, `_v3b_line_unit_price`, `_v3b_expand_units`, `_v3b_summarise_lines`, `_v3b_validate_config`, `_v3b_resolve_buy_lists/_get_lists`, `_v3b_match_lines_by_lists`, `_v3b_select_get_units`, `_v3b_apply_caps`, `_v3b_compute_discount`) inserted between V3-A Step-4 pre-check and V1/V2 scope dispatch — V3-A time-window composition is automatic (verified V3B-W1/W2). **Coverage:** BOGO same-item (qty 1/2/3/4 + mixed-price cheapest), BXG same-item buy-2-get-1, BXG different-item (pizza+garlic free/percentage/flat with per-unit cap), `max_applications` cap, `allow_repeat=False` cap, `apply_to_highest_item` override, `max_discount` ceiling, line_total fallback, invalid-price safe ignore, time-window + BOGO composition, loyalty stacking regression, idempotent final-order, non-blocking failure (`coupon_usage` not recorded, order persists), `pos_instruction` only on missing-requirement failures (Q11=B), `bxgy_usage` analytics block, admin Pydantic validators (`buy_x_get_y`→`bxg` alias, get_discount_type enum, `buy_quantity≥1`), runtime config errors (`BXGY_CONFIG_INVALID`, `UNSUPPORTED_BENEFIT_TYPE`). **Error codes added:** `MISSING_ITEMS_FOR_BXGY_COUPON`, `BUY_REQUIREMENT_NOT_MET`, `GET_REQUIREMENT_NOT_MET`, `NO_ELIGIBLE_BUY_ITEMS_IN_CART`, `NO_ELIGIBLE_GET_ITEMS_IN_CART`, `BXGY_CONFIG_INVALID`, `UNSUPPORTED_BENEFIT_TYPE` (7 new; `MAX_APPLICATIONS_REACHED` intentionally NOT introduced — cap, not gate). Total error-code surface now **22**. **Schema (all optional, no migration):** `Coupon`/`CouponCreate`/`CouponUpdate` gain `buy_quantity` / `get_quantity` / `buy_*_ids` / `get_*_ids` / `get_discount_type` (free/percentage/flat validator) / `get_discount_value` / `max_applications` / `allow_repeat` / `same_item_required` / `requires_get_item_in_cart` / `pos_instruction`. `CouponUsage` gains `applied_applications` / `benefit_items` / `buy_match_summary` / `get_match_summary` / `computed_discount` / `discount_mismatch` and the same descriptor fields. `_v3a_validate_offer_type` extended to accept `"buy_x_get_y"` (normalized to `"bxg"`). **Files changed:** `core/coupon.py` (+~530 LOC), `models/schemas.py` (+~120 LOC), `routers/pos.py` (+~45 LOC), `services/analytics_service.py` (+~65 LOC, new `_get_bxgy_usage` aggregator with `$ifNull` group + per-row `benefit_items` scan), `tests/seed_coupon_v1_fixtures.py` (+~290 LOC, 10 new `QA_C3B_*` fixtures + cleanup regex extended to `^(?:QA_C1_\|QA_C2_\|QA_C3A_\|QA_C3B_)`), NEW `tests/qa_cr001c_c_coupon_v3_b_bogo_bxgy.py` (~640 LOC, 49 assertions). **Files UNTOUCHED:** `routers/coupons.py` (9 admin CRUD endpoints), `core/loyalty.py`, wallet code, migration code, `coupon_transactions` legacy collection, `/app/memory/final/`. **Live HTTP smoke:** backend restarted clean, `GET /api/health` → 200, `POST /api/pos/coupons/validate` with V3-B cart shape parses cleanly (401 at auth only, no Pydantic 422). **No DB migration, no new index, no env change, no new dependency.** **Locked invariants intact:** one coupon per order, idempotency `(user_id, order_id)`, POS-sent total is source of truth for billing, get item required in cart (no auto-add), failure non-blocking, loyalty stacking flag respected. Reports: `implementation/CR_001C_C_COUPON_V3B_BOGO_BXGY_IMPLEMENTATION_REPORT.md`, `qa/CR_001C_C_COUPON_V3B_BOGO_BXGY_QA_REPORT.md`. **Next downstream:** joint V1+V2+V3-A+V3-B POS-side integration handoff. |
| V3-B BOGO / Buy-X-Get-Y Planning + Owner Gate | ✅ `cr001c_coupon_v3b_bogo_bxgy_plan_ready_for_implementation_approval` | 2026-02 | **Owner decisions FROZEN 2026-02** — owner accepted all 12 recommended defaults (Q1=D full BOGO+BXGY with free/%/flat; Q2=A get-item required in cart; Q3=A free cheapest; Q4=A include different-item BXGY; Q5=C free+%+flat benefit types; Q6=C `allow_repeat` field default `true`; Q7=A support `max_applications`; Q8=B total + `benefit_items` summary; Q9=A non-blocking final-order failure; Q10=A allow time-window + BOGO composition; Q11=B `pos_instruction` failure-only; Q12=A kickoff after plan). **Path Alpha selected — single-shot V3-B implementation, ~2–3 working days.** **In scope:** BOGO same-item, BXGY same-item, BXGY different-item; benefit types `free` / `percentage` / `flat`; `max_applications`, `allow_repeat`, `same_item_required`, reuse V2 `apply_to_cheapest_item` / `apply_to_highest_item`. **Locked invariants preserved:** one coupon per order; get item must already be in cart (no auto-add, per OQ-V3-3); final-order failure non-blocking (per OQ-V3-8); time-window pre-check (V3-A) composes automatically; loyalty stacking via `stackable_with_loyalty`; wallet untouched; 9 admin CRUD endpoints untouched; `coupon_transactions` legacy collection untouched. **Data model:** all new fields optional + backward-compatible (`offer_type` enum already accepts `"bogo"` / `"bxg"` via V3-A validator; `POSCartItem` already exposes `food_id` / `quantity` / `unit_price` / `line_total` — no POS protocol change). **Error codes:** 5 new (`MISSING_ITEMS_FOR_BXGY_COUPON`, `BUY_REQUIREMENT_NOT_MET`, `GET_REQUIREMENT_NOT_MET`, `NO_ELIGIBLE_BUY_ITEMS_IN_CART`, `NO_ELIGIBLE_GET_ITEMS_IN_CART`) + 2 admin/config (`BXGY_CONFIG_INVALID`, `UNSUPPORTED_BENEFIT_TYPE`); `MAX_APPLICATIONS_REACHED` intentionally NOT introduced (`max_applications` is a cap, not a gate). **Analytics:** additive `bxgy_usage` block + promotes existing `breakdown_by_offer_type.bogo` / `.bxg` buckets from 0. **QA:** ~32 V3-B assertions planned (BOGO 5 + BXGY same-item 3 + BXGY different-item 5 + benefit types 3 + caps 3 + API surface 3 + final order 3 + cross-cutting 3 + analytics 2 + admin 2). V1 45/45 + V2 45/45 + V3-A 31/31 regression gates enforced → combined post-V3-B target **~153 PASS**. **Files:** plan + owner-decision freeze in `planning/CR_001C_C_COUPON_V3B_BOGO_BXGY_PLANNING_AND_OWNER_GATE.md` (Addendum D added 2026-02). Planned implementation extends `core/coupon.py` (~250 LOC), `models/schemas.py` (~70 LOC), `routers/pos.py` (~40 LOC), `services/analytics_service.py` (~40 LOC), `tests/seed_coupon_v1_fixtures.py` (~120 LOC); NEW `tests/qa_cr001c_c_coupon_v3_b_bogo_bxgy.py` (~700 LOC). No DB migration, no new index, no new dependency, no env change. No code, DB, env, migration, or deployment changes by this planning step. Implementation NOT started; awaiting owner's explicit "begin V3-B implementation" trigger. On kickoff → status flips to `cr001c_coupon_v3b_bogo_bxgy_implementation_in_progress`; on QA pass → `cr001c_coupon_v3b_bogo_bxgy_implementation_qa_passed_in_preview`. |
| V3-A Time-Window Implementation | ✅ `cr001c_coupon_v3a_time_window_implementation_qa_passed_in_preview` | 2026-02 | V3-A (Time-window / Happy-hour) implemented end-to-end per the approved plan. **V1 regression 45/45 + V2 regression 45/45 + V3-A 31/31 = 121/121 PASS.** Live HTTP smoke confirmed (`/api/health` 200; `order_time` request field accepted). New `OUTSIDE_TIME_WINDOW` error code; window check inserted as Step 4 of `validate_coupon_for_customer` (after EXPIRED, before USAGE_LIMIT_REACHED). Server-clock decides; POS-supplied `order_time` echoed in `time_window_status.pos_supplied_order_time` and never authoritative. Timezone resolution chain implemented (`coupon.timezone` → `users.settings.timezone` → `Asia/Kolkata` → UTC with `tz_fallback="utc"` warning). Overnight wrap supported with explicit window-owning-day logic for `valid_days` interaction. `/available` returns outside-window coupons with `within_window_now=false` + `next_window_start` for greyed-out UX. `coupon_usage` rows snapshot `offer_type="simple"` + `time_window_status`. Analytics gain `breakdown_by_offer_type` + `time_window_usage` (additive — existing keys preserved; dashboards unaffected). `used_outside_window_attempts` returns 0 (deferred to V3-A2 per OQ-V3A-2). Admin CRUD Pydantic validators reject invalid HH:MM, invalid IANA tz, out-of-range `valid_days`. No DB migration, no new indexes, no new dependency (stdlib `zoneinfo`). 9 admin CRUD endpoints untouched. Loyalty / Wallet / Migration code / `coupon_transactions` / `/app/memory/final/` UNTOUCHED. Files: `core/coupon.py` (~180 LOC), `models/schemas.py` (~80 LOC), `routers/pos.py` (~25 LOC), `services/analytics_service.py` (~55 LOC), `tests/seed_coupon_v1_fixtures.py` (~80 LOC), NEW `tests/qa_cr001c_c_coupon_v3_a_time_window.py` (~575 LOC, 31 assertions). See `implementation/CR_001C_C_COUPON_V3A_TIME_WINDOW_IMPLEMENTATION_REPORT.md` + `qa/CR_001C_C_COUPON_V3A_TIME_WINDOW_QA_REPORT.md`. Ready for owner sign-off and joint V1+V2+V3-A POS-side integration handoff. |
| V3-A Time-Window Implementation Plan | ✅ `cr001c_coupon_v3a_time_window_implementation_plan_ready_for_owner_approval` | 2026-02 | V3-A implementation plan authored covering: (1) data model — optional `offer_type` (default `"simple"`), `valid_days[]` (ISO Mon=0..Sun=6), `start_time`/`end_time` (`HH:MM` 24h, both required as a pair), `timezone` (IANA) on `coupons`; optional `offer_type` + `time_window_status` on `coupon_usage`. (2) Timezone resolution chain: `coupon.timezone` → `users.settings.timezone` → `Asia/Kolkata` → UTC fallback with `tz_fallback="utc"` warning. (3) Server-clock evaluation only — POS-supplied `order_time` is informational/echoed. (4) Overnight wrap (`end <= start`) with explicit window-owning-day logic for `valid_days` interaction; exclusive end-time semantics. (5) `OUTSIDE_TIME_WINDOW` error code; window check inserted as Step 4 in `validate_coupon_for_customer` (after EXPIRED, before usage_limit). (6) `/available` returns coupons outside their window with `within_window_now=false` + `next_window_start` for greyed-out UI. (7) `/validate` + `/orders` emit `time_window_status` block; final-order non-blocking failure preserved (order persists, usage NOT recorded). (8) Analytics adds additive `breakdown_by_offer_type` + V3-A-specific `time_window_usage` (`used_outside_window_attempts` deferred to V3-A2 per OQ-V3A-2 default). (9) 7 files extended (~370 non-test LOC + ~500 LOC new harness); admin CRUD `routers/coupons.py` UNTOUCHED; no DB migration, no new index, no new dependency (stdlib `zoneinfo`). (10) QA: 25 V3-A assertions across window evaluation / overnight wrap / timezone resolution / server-clock vs POS time / `/available` shape / V1+V2 happy-hour cross-cutting / final-order non-blocking / analytics / admin CRUD / Loyalty+Wallet regression. V1 (45) + V2 (45) regression must remain green → combined post-V3-A baseline ~115 assertions. 2 micro-OQs (V3A-1 status-block uniformity, V3A-2 outside-window analytics counter) — both with recommended defaults, non-blocking. See `planning/CR_001C_C_COUPON_V3A_TIME_WINDOW_IMPLEMENTATION_PLAN.md`. Estimated 2–3 working days. Implementation NOT started; awaiting owner sign-off. |
| V3 Complex Offers Planning | ✅ `cr001c_coupon_v3a_time_window_plan_ready_for_implementation_approval` | 2026-02 | **Owner decisions applied (Addendum C).** V3-A (Time-window / Happy-hour) approved as the first V3 phase. **OQ-V3-1=V3-A first; OQ-V3-3=BXG get-item must be in cart (no auto-add); OQ-V3-4=Free-item instruction-only (no auto-add); OQ-V3-5=Restaurant-local timezone (`coupon.timezone` → `users.settings.timezone` → `Asia/Kolkata` → UTC fallback with warning, server clock decides); OQ-V3-9=Combo V3-E PARKED to V4.** Non-blocking OQs (2, 6, 7, 8, 10, 11, 12) accept proposed defaults. V3-A v1 in-scope: new optional `offer_type` (default `"simple"`), time-window fields (`valid_days`, `start_time`, `end_time`, `timezone`), `_is_within_time_window` early pre-check applies to ALL V1/V2/V3 coupons, new `OUTSIDE_TIME_WINDOW` error code (non-blocking at `/orders`), `time_window_status` block in validate/orders responses, `time_window` block per coupon in `/available` (outside-window coupons still returned for greyed-out UX), additive `breakdown_by_offer_type` + `time_window_usage` analytics, ~25 V3-A assertions in new `qa_cr001c_c_coupon_v3_a_time_window.py`. V1 (45) + V2 (45) regression must remain green (combined baseline ~115). V3-B/V3-C/V3-D scope locked in Addendum C §C.2 (require separate approval). V3-E parked. Admin UI deferred (`CouponsPage.jsx` unchanged). See `planning/CR_001C_C_COUPON_V3_COMPLEX_OFFERS_PLANNING.md` + Addendum C. Implementation NOT started; ready for owner sign-off on V3-A implementation kickoff. |
| V2 Implementation | ✅ `cr001c_coupon_v2_item_category_implementation_qa_passed_in_preview` | 2026-05-24 | V2 item/category coupons implemented end-to-end. `discount_scope` discriminator × `discount_type` → ITEM_FLAT, ITEM_PERCENTAGE, CATEGORY_FLAT, CATEGORY_PERCENTAGE. New `core/coupon.py` helpers (`resolve_discount_scope`, `_line_matches_*`, `_compute_v2_discount`, `build_eligible_match_hint`). `validate_coupon_for_customer` cart-aware via optional `items[]`. `list_available_coupons` returns `requires_cart_validation` + `eligible_match_hint` for V2 scopes. Final-order recording stores `discount_scope`, `eligible_subtotal`, matched id/name lists. Analytics gains `breakdown_by_scope` (additive). 5 new structured error codes (`MISSING_ITEMS_FOR_*`, `NO_ELIGIBLE_*_IN_CART`, `MIN_ITEM_QTY_NOT_MET`). Item matching: `food_id` → `item_id`. Category matching: `category_id` → `category_name` → `item_category` fallback. Per-line `max_applicable_qty`. 6 files changed (1 new — `tests/qa_cr001c_c_coupon_v2_item_category.py`; 5 extended — `core/coupon.py`, `models/schemas.py`, `routers/pos.py`, `services/analytics_service.py`, `tests/seed_coupon_v1_fixtures.py`). No DB migration, no env change, no new indexes (V1 indexes cover V2). **V1 regression 45/45 PASS + V2 45/45 PASS = 90/90 PASS.** Live HTTP smoke confirmed. 9 admin CRUD endpoints in `routers/coupons.py` UNTOUCHED. Loyalty + Wallet UNTOUCHED. See `implementation/CR_001C_C_COUPON_V2_ITEM_CATEGORY_IMPLEMENTATION_REPORT.md` + `qa/CR_001C_C_COUPON_V2_ITEM_CATEGORY_QA_REPORT.md`. |

---


## CR-001D — `orders.restaurant_id` silently null (NEW, surfaced 2026-05-22)

Discovered during CR-001A Phase 1 live verification. Every order persisted via
`POST /api/pos/orders` shows `orders.restaurant_id = None` even though the
payload sends `"restaurant_id": "478"`. This is a downstream-mapping miss in
`pos.py` (the `order_doc` dict does not include `order_data.restaurant_id`).

- Severity: medium (affects restaurant-level analytics & filtering)
- Scope: one-line addition to `order_doc` build in `pos.py`
- Status: ✅ **Implemented + QA passed 2026-05-22 ~10:07 UTC**
  (`cr001a_phase_2_and_cr001d_qa_passed_with_runtime_limitations`). Live prod
  closure pending natural production order after prod deploy + `pos-backend`
  restart. `pos_restaurant_id` preserved for backwards compatibility.
  See `/app/memory/crm/crm_1_0/implementation/CR_001A_PHASE_2_AND_CR_001D_IMPLEMENTATION_REPORT.md`

---

## CR-002 — POS Request Logging Middleware

| Status | Notes |
|---|---|
| ✅ Implemented (imported in `22-may` branch) | `/app/backend/core/pos_request_logger.py`; writes to `pos_request_logs`. Verified working for CR-001A — every realtime POS order during the work was captured. **Must not be modified by CR-001A.** |

---

## Cross-CR Dependencies

| From | To | Type |
|---|---|---|
| CR-001A Phase 1 | CR-002 | Read-only: CR-001A used `pos_request_logs` for raw-payload diffing against persisted state |
| CR-001A Phase 1 | CR-001B | Isolated by file boundary (`pos.py` vs `migration.py`) — verified no impact on R689 |
| CR-001A Phase 2 | CR-001A Phase 1 | Builds on the same `POSOrderWebhook` model · ✅ implemented 2026-05-22 |
| CR-001D | CR-001A Phase 1 | Adjacent in `pos.py` (order_doc build), no model change needed · ✅ implemented 2026-05-22 (same PR as CR-001A Phase 2) |

---

## Glossary of Captured Realtime Field Aliases (CR-001A Phase 1)

| POS field (incoming) | CRM canonical (stored) |
|---|---|
| `created_at` (top-level) | `order_created_at` |
| `items[].item_id` | `items[].pos_food_id` (str) |
| `items[].qty` | `items[].item_qty` (int, default 1) |
| `items[].price` | `items[].item_price` (float, default 0.0) |

Both directions accepted (`populate_by_name=True`).
