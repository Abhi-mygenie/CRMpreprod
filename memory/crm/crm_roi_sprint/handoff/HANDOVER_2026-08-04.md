# Session Handover — 2026-08-04 (Full Session)

**Date**: 2026-08-04  
**Branch**: main (Abhi-mygenie/CRMpreprod)  
**Pod URL**: https://crm-stack-preview.preview.emergentagent.com  
**DB**: Remote MongoDB 52.66.232.149:27017/mygenie (live data)

---

## What happened this session

### Environment
- Pulled `CRMpreprod` repo (main branch) fresh into `/app`
- All env vars configured (MongoDB, JWT, MyGenie, AuthKey, Meta, AWS S3, POS logging)
- Added `CRM_EXTERNAL_URL=https://crm-stack-preview.preview.emergentagent.com` to backend .env
- Both services running via supervisor

### Implemented (code changes — all QA passed 26/26)

| ID | What | Files |
|---|---|---|
| BUG-020 | "Unknown" name → "Guest" in WhatsApp | `core/whatsapp_variables.py`, `core/whatsapp.py` |
| BUG-021 | POS order updates customer name/email | `routers/pos.py` |
| BUG-022 | Migration re-sync name guard (`_is_placeholder_name`) | `routers/customers.py` |
| BUG-023 | `weasyprint` added + DB insert before PDF | `requirements.txt`, `services/invoice_generator.py` |
| CR-069 fix | `button_param_value` — correct AuthKey field for dynamic URL button | `core/whatsapp.py`, `routers/whatsapp.py` |
| CR-073 | AuthKey-created templates imported into CRM on sync | `routers/whatsapp.py` |

### Registered (no code — intake + decisions only)

| ID | What | Next |
|---|---|---|
| BUG-024 | Template button URL missing `/api/invoices/` path | Owner Q1: resubmit vs nginx redirect vs React route |
| CR-071 | B2B customer pipeline (6 gaps) | ALL decisions locked → ready for planning |
| CR-072 | Hotel document upload (Aadhaar → S3 + POS recall) | Q2-Q5 locked, Q1 pending POS payload |
| INV-013B ext | Migration sync-orders path doesn't update customer name | No code, reverted per owner. Needs intake + plan. |

---

## Test credentials

| Account | Password | Tenant |
|---|---|---|
| owner@kunafamahal.com | Qplazm@10 | Kunafa Mahal (restaurant_689) |
| owner@hungry.com | Qplazm@10 | Hungry Keya (restaurant_634) |
| owner@palmhouse.com | Qplazm@10 | Palm House (hotel, restaurant_558) |

---

## QA Status
- QA run: iteration_1.json — 26/26 PASS (BUG-020, 021, 022, 023, CR-069, CR-073)
- Test file: `/app/backend/tests/test_bugs_020_023_cr069_073.py`

---

## Open items for next session

### Needs owner action (no code needed)
1. **Switch `send_bill` event → `kmfinalbill`** for Kunafa Mahal + map variables
2. **Answer Q1 for BUG-024**: resubmit templates / nginx redirect / React route?
3. **Share POS payload** for CR-072 document types (Q1)

### Ready for planning (owner can say "plan")
4. **Plan CR-071** — B2B customer pipeline (all decisions locked)
5. **Plan CR-072** — Hotel document upload (Q1 pending POS payload, rest locked)

### Still needs attention
6. **Production deploy** — push all fixes to crm-mygenie.mygenie.online + `pip install weasyprint`
7. **CR-069 button_param_value confirmed?** — test send to 9696759718 fired at 09:13 — user to confirm button URL shows real token
8. **testbill2 has preview pod URL** — template submitted with wrong domain (crm-stack-preview instead of crm-mygenie.mygenie.online)

---

## DO NOT

- Do NOT send live WhatsApp without owner approval (real customer phones)
- Do NOT change coupon/loyalty/POS order math without owner approval
- Do NOT run destructive DB operations on live preprod data
- Do NOT re-introduce demo login (CR-015c)
