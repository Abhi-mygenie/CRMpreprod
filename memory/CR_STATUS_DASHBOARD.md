# CR Status Dashboard — `crm_roi_sprint`

> **Live flat status board.** Update on every phase transition.
> One row per CR. No narrative. For narrative, read the linked discovery / planning / impl / QA doc.
> Last updated: **2026-08-06 (SESSION — Planning Agent · CR-067 + CR-068 PLANNING APPROVED — all decisions locked · implementation plan gate open)**

---

## 📌 Latest Session Snapshot

**Session date**: 2026-07-29 (SESSION — Investigation Agent → Intake Agent)
**Pod URL**: (current pod — see frontend `.env` `REACT_APP_BACKEND_URL`)
**Branch**: `main` (pulled from `Abhi-mygenie/CRMpreprod`)
**DB**: Remote MongoDB `52.66.232.149:27017/mygenie` (no local DB)

### What happened this session
1. **Repo pulled** from `main` branch (`Abhi-mygenie/CRMpreprod`). Backend `.env` configured with all env vars (MongoDB, JWT, MyGenie API, AuthKey, Meta, AWS S3, POS logging). All deps installed. Services UP.
2. **Investigation Agent — INV-012**: Owner reported `final_bill` template (wid=41354, Hungry Keya tenant) has 2 buttons but mapping UI doesn't show them. One button has dynamic URL (`https://crm.mygenie.online/{{1}}`).
   - **10 investigation steps**: traced AuthKey raw API (no button fields returned), traced `custom_templates` (button data EXISTS), traced frontend mapping modal (body-only extraction), traced send payload (no `buttonValues`), traced all 10 affected surfaces.
   - **Root cause**: PLAN_GAP × 3 layers — backend enrichment skips `buttons`, frontend only maps body vars, send path has no button param field.
   - **Confidence**: HIGH. Report: `discovery/INV_012_TEMPLATE_BUTTON_MAPPING_INVESTIGATION.md`.
3. **Intake Agent — CR-069 REGISTERED**: "Template Button Variable Mapping & Send-Path Support". P1, MEDIUM-HIGH risk, ~6-7 hrs, 10 surfaces across 6 files. `einvoice_token` variable already in registry — just needs wiring. Q1 open (AuthKey `buttonValues` undocumented — test empirically).
   - Intake doc: `discovery/CR_069_TEMPLATE_BUTTON_MAPPING_INTAKE.md`.
4. **Planning Agent — CR-069 Impact Analysis**: Before/after screen comparison for all 6 affected screens using `final_bill` as reference. Screenshots captured: template card chips (7 body-only), preview bubble (no buttons), Map dialog (ends at `{{7}}`), campaign wizard dropdown (`7 variables, fully mapped` — false green), campaign wizard Step 2. 12 verification items defined. Risk matrix: Q1 (AuthKey `buttonValues`) mitigated by test-first approach.
   - Impact Analysis: `planning/CR_069_IMPACT_ANALYSIS_BEFORE_AFTER.md`.
5. **Planning Agent — CR-069 Implementation Plan**: 14 edit-by-edit steps across 6 files. 5 backend edits (enrichment, dataclass, send payload, event trigger resolution, campaign send resolution) + 9 frontend edits (variable extraction, Map dialog slots, preview buttons, variable chips, campaign wizard isFullyMapped + grid + preview, test template modal inputs + preview). 14 verification items mapped to ACs. 5 regression checks.
   - Implementation Plan: `planning/CR_069_IMPLEMENTATION_PLAN.md`.

### Handover doc
`/app/memory/crm/crm_roi_sprint/discovery/CR_069_TEMPLATE_BUTTON_MAPPING_INTAKE.md`

### Next-agent message (copy-paste)
READ: `CR_STATUS_DASHBOARD.md` → `CR_069_TEMPLATE_BUTTON_MAPPING_INTAKE.md` → `INV_012_TEMPLATE_BUTTON_MAPPING_INVESTIGATION.md`
**Pod URL**: (see frontend `.env` `REACT_APP_BACKEND_URL`)
**Branch**: `main`
**DB**: Remote MongoDB `52.66.232.149:27017/mygenie` (no local DB)
**Test credentials**: `owner@hungry.com / Qplazm@10` (Hungry Keya, restaurant_634)

### What happened this session (full chronology)

1. **Repo re-bootstrapped from `main` branch** (`Abhi-mygenie/CRMpreprod`). Wiped `/app`, cloned fresh. Backend `.env` configured with remote MongoDB. Frontend `.env` with preview URL. All deps installed (`pip install` + `yarn install`). Services UP.
2. **Alpha Agent prompt adopted** — Investigation Agent role per `/app/memory/control/MYGENIE_CRM_AGENT_SYSTEM_PROMPT_ALPHA_v0_1.md`.
3. **CRM token push-back investigation** — Owner asked how CRM token is pushed to POS on login. Full code trace of `_register_crm_token_with_pos()` in `routers/auth.py`. Key finding: push happens on EVERY login (not just first time as CR-001 spec said). Fire-and-forget, never blocks login.
4. **Hardcoded config audit** — Owner asked if POS URLs are in `.env`. Investigation found 22 env variables with hardcoded defaults or pure hardcoded strings across 11 files.
5. **CR-027 registered + implemented** — Hardcoded Config → Environment Variables. Discovery → planning → implementation. All 22 variables added to `.env` (total 25). Hardcoded defaults removed from 11 files. Grep audit: 0 remaining. Crash behavior locked: 18 fail-fast, 7 keep fallbacks.
6. **CR-024 investigation** — Owner asked if Phase 2-3 is really not built. Code audit confirmed ALL phases (1-4) fully implemented. CR board was stale — updated to 🟢 CLOSED.
7. **Implementation reports written** — Retroactive impl reports for CR-014 (12 ACs), CR-023 (14 ACs), CR-024 (25 ACs).
8. **Testing agent re-enabled** — Owner reversed opt-out decision.
9. **QA Run 1**: CR-027 + CR-024 → **28/28 pass**.
10. **QA Run 2**: CR-014 + CR-023 → **18/18 pass**.
11. **BUG-008 registered** — CRM token pushed on every login (should be conditional). `crm_token_registered_with_pos` written but never read. Investigation found: all 24 users already `true`, every push returns 409. Bonus: `regenerate_api_key` doesn't push new key to POS.
12. **CR-028 registered + bundled with BUG-008** — POS Integration Settings UI. Impact analysis + implementation plan with regression plan (Section 8 high-risk files).
13. **CR-028 + BUG-008 implemented** — Moved `register_crm_token_with_pos()` to `core/auth.py` (shared). Gated login push. Added regenerate push + flag reset. Built POS Integration card in Settings (masked key, copy, regenerate with AlertDialog). Manual regression R1-R10 passed.
14. **QA Run 3**: CR-028 + BUG-008 → **9/9 pass** + all frontend elements verified. Old key rejected after regeneration (401).
15. **Total QA this session: 73/73 tests, 0 issues across 3 runs.**

### 🎯 Next-agent handoff message

```
You are picking up the MyGenie CRM ROI sprint.

READ FIRST: README.md → CR_STATUS_DASHBOARD.md (this snapshot)
             → DECISIONS_LOG.md → PRD.md

CURRENT STATE (2026-06-18 session 6 close):

IMPLEMENTED + QA'd THIS SESSION:
- CR-027: 🟢 CLOSED. 22 env vars → .env, zero hardcoding. QA: iteration_1 (28/28).
- CR-028 + BUG-008: 🟢 CLOSED. POS Integration Settings card (show key, copy, regenerate).
  Login push gated by crm_token_registered_with_pos. Regenerate pushes new key.
  register_crm_token_with_pos() moved to core/auth.py (shared).
  QA: iteration_3 (9/9).

PREVIOUSLY IMPLEMENTED, QA'd THIS SESSION:
- CR-024: 🟢 CLOSED. All phases (1-4). QA: iteration_1 (28/28).
- CR-014: 🟡 QA PASSED (iteration_2, 18/18). Awaiting POS team for hotel folio room_info.
- CR-023: 🟡 QA PASSED (iteration_2, 18/18). Awaiting owner E2E Meta test.

REMAINING:
- CR-026: 📋 Registered (P3). Campaign "View Messages" deep-link. ~½ day. Unblocked.
- CR-025: ⏸ Parked. Virtual Wallet — awaiting owner Q1-Q10.
- CR-016: ⏸ Deferred to next sprint.

ENV FILES:
- Backend: /app/backend/.env (25 vars — all config, zero hardcoding)
- Frontend: /app/frontend/.env (3 vars)

TEST CREDENTIALS: owner@kunafamahal.com / Qplazm@10
                  owner@palmhouse.com / Qplazm@10

TEST REPORTS: iteration_1.json (CR-027+CR-024), iteration_2.json (CR-014+CR-023),
              iteration_3.json (CR-028+BUG-008)
TEST FILES: test_campaigns_api.py, test_cr014_cr023_qa.py,
            test_cr028_bug008_pos_key_settings.py

DECISIONS THIS SESSION:
- All 22 env vars must be in .env, zero hardcoding (CR-027)
- CAMPAIGN_SCHEDULER_ENABLED keeps false safety fallback
- testing_agent_v3 re-enabled (reversed opt-out)
- JWT_SECRET keeps current value (rotate in separate security CR)

DO NOT:
- Re-introduce demo login (CR-015c)
- Send live WhatsApp without owner approval
- Run destructive DB operations (real data)
```

### Active queue (this sprint)

| Order | CR | Status | Next action |
|---|---|---|---|
| 1 | **CR-069** | ✅ QA PASS | Owner smoke pending — button enrichment, Map dialog slots + bubbles, wizard isFullyMapped, send payload. iter_5+6 PASS. |
| — | ~~**CR-033**~~ | 🟢 CLOSED | QA iteration_5 2026-07-06 — 20 audience filters + AudiencesPage accordion. |
| — | ~~**CR-034**~~ | 🟢 CLOSED | QA iteration_5 2026-07-06 — tag schema, 5 endpoints, tag chips on 3 pages. |
| — | ~~**CR-014**~~ | 🟢 CLOSED | CRM code complete (3 invoice modes, graceful fallbacks). POS team to pass room_info. |
| — | ~~**CR-023**~~ | 🟢 CLOSED | Phase 1+2+3 + CR-066 V11-V23 done. Owner E2E = acceptance, not code gap. |
| — | ~~**CR-076**~~ | ✅ QA PASS | Lifecycle Re-engage — modal, bulk CTA, AudiencesPage filter, CampaignWizard pre-fill. Owner smoke pending. |
| — | ~~**CR-077**~~ | ✅ QA PASS | Configurable lifecycle thresholds — 11 new loyalty_settings fields, Block E wired. Owner smoke pending. |
| — | ~~**CR-071 + CR-072**~~ | ✅ QA PASS | B2B customer GST pipeline + hotel document capture. 13/13 PASS. Owner smoke pending. |
| — | ~~**BUG-011**~~ | ✅ QA PASS (iteration_9) | `_augment_run_stats()` 3/3 pytest PASS. Delivered/read aggregation + legacy $or + empty-runs all verified. |
| — | ~~**BUG-012**~~ | ✅ QA PASS (iteration_9) | Playwright 3/3 PASS. Deep-link filter, no-params regression, campaign+run dual-banner all confirmed. |
| — | ~~**CR-061**~~ | ✅ QA PASS (iteration_9) | 13/13 pytest PASS. Gate removed, jehsnest non-403, crm_templates_enabled=False, GET 200 regression clean. |
| — | ~~CR-024~~ | 🟢 CLOSED | All phases implemented + QA passed |
| — | ~~CR-027~~ | 🟢 CLOSED | Env variables + QA passed |
| — | ~~CR-028 + BUG-008~~ | 🟢 CLOSED | POS Integration Settings + login push fix + QA passed |
| — | ~~CR-022~~ | 🟢 CLOSED | 142/142 QA pass |
| — | ~~CR-021~~ | 🟢 CLOSED | 142/142 QA pass |
| — | ~~CR-020~~ | 🟢 CLOSED | QA 18/18 pass |
| — | ~~CR-015 / CR-015a/b/c / CR-017 / CR-018~~ | 🟢 CLOSED | — |
| — | ~~CR-016~~ | ⏸ Deferred next sprint | — |

---

## Status legend

| Light | Meaning |
|---|---|
| 🟢 | **Closed** — live-test passed, CR done |
| 🟡 | **In flight** — agent actively working in current session |
| 🔵 | **Planning approved** — implementation can start any time |
| ⏸ | **Parked** — discovery (or later phase) complete but waiting on owner answer |
| 🔴 | **Blocked** — waiting on external dependency or another CR |
| 📋 | **Registered only** — placeholder, no discovery yet |
| ❌ | **Cancelled / dropped** |

---

## CR Board

| # | CR | Phase | Status | Effort | Blockers / Owner asks | Last touched |
|---|---|---|---|---|---|---|
| 002 | Loyalty engine | Closed | 🟢 | — | — | 2026-05 |
| 002B | Birthday/Anniversary | Closed | 🟢 | — | — | 2026-05 |
| 003 | Coupon analytics dashboard | Closed Phase 1 | 🟢 | — | Phase 2 backlog | 2026-05 |
| **004** | **WhatsApp Utility + Marketing** | **Closed (P3.5)** | **🟢** | — | Optional Commit 8 hardening (IP allowlist) available, not blocking | **2026-05-28** |
| 005 | (per register) | — | (see register) | — | — | — |
| 006 | (per register) | — | (see register) | — | — | — |
| 007 | (per register) | — | (see register) | — | — | — |
| 008 | (per register) | — | (see register) | — | — | — |
| 009 | (per register) | — | (see register) | — | — | — |
| 010 | (per register) | — | (see register) | — | — | — |
| 011 | Coupon Optimizer | Discovery | ⏸ | — | (see register) | — |
| 012 | WhatsApp Template Builder | Planning | 🔵 | — | (see register) | — |
| 013 | Template Gallery | Discovery | 🔴 blocked by CR-012 P1 | — | (see register) | — |
| **014** | **E-Invoice PDF + Mobile HTML Link** | **🟢 CLOSED (CRM side) — All 3 modes implemented: food (GST), hotel_room (Pattern A), hotel_folio (Pattern B). Auto-detection in `invoice_generator.py`. Graceful fallbacks for missing `room_info` fields. Public routes `/api/invoices/{token}` + `/api/invoices/{token}/pdf` live. S3 + WeasyPrint wired. Live-tested on real data (sunildev #000130, Palm House #006644). Awaiting POS team to send `room_info` fields in order webhooks for rich hotel display — that is POS-side work, not a CRM code gap.** | **🟢** | done | All 4 buckets from Phase 1+2 implemented + live-tested. **Phase 3 (Hotel Folio Mode C)**: 2 patterns — Pattern A "HOTEL FOLIO" (`room_info` struct, room charges + F&B) + Pattern B "GUEST FOLIO" ("Check In" item, day-grouped F&B folio). Auto-detection in `invoice_generator.py`. Templates: `invoice_hotel_room.html` + `invoice_hotel_folio.html`. Verified with real data: sunildev #000130 + Palm House #006644. POS contract shared (`handoff/CR_014_POS_HOTEL_FOLIO_DATA_CONTRACT.md`) for 7 new `room_info` fields. **Next**: POS team adds P0 fields (`room_number`, `check_in`, `check_out`). | **2026-06-06** |
| **015** | **WhatsApp Template Variable Mapping Fidelity** | **CLOSED — live test passed** | **🟢** | done | T1-T7 done. T2 skipped. {{6}} mismatch fixed. Full audit passed. Live test: orders 869331+869333, 7/7 slots correct, status=read. | **2026-05-29** |
| **017** | **/pos/max-redeemable Projected Points Earned** | **CLOSED — implemented + verified** | **🟢** | done | Hot fix. 3 additive fields: `projected_points_earned`, `projected_earn_percent`, `earn_ratio_display`. Curl-verified. POS handoff updated. | **2026-05-29** |
| **018** | **/pos/max-redeemable Projected Tier Upgrade** | **CLOSED — implemented + verified** | **🟢** | done | 3 additive fields: `projected_tier_after`, `tier_upgrade`, `tier_upgrade_message`. Curl-verified. POS handoff updated. | **2026-05-29** |
| **015a** | **Preview Sample Data Gap for T5 Variables** | **Implemented & verified** | **🟢** | done | Preview "NA" fixed: 14 T5 sample values in `customers.py` sample-data + frontend registry-`example` fallback. Closeout: `implementation/CR_015A_PREVIEW_SAMPLE_DATA_CLOSEOUT.md`. | **2026-05-29** |
| **015b** | **Dead Variable-Mapping Code Removal** | **Implemented & verified** | **🟢** | done | Removed orphaned/unreachable mapping modal cluster on WhatsApp Automation page + unused `availableFields`/`getPreviewMessage` on Segments. Mapping is **Templates-page-only**. Closeout: `implementation/CR_015B_DEAD_VARIABLE_MAPPING_CODE_CLOSEOUT.md`. | **2026-05-29** |
| **015c** | **Remove Demo Login** | **Implemented & verified** | **🟢** | done | Demo login fully removed (was 404). Backend endpoint/constants/`is_demo` + frontend button/banner/context. Tests → real login (11 pass). Closeout: `implementation/CR_015C_REMOVE_DEMO_LOGIN_CLOSEOUT.md`. | **2026-05-29** |
| **016** | **Dynamic Event Registry + Trigger Configuration UI** | **Discovery Phase 0 done — DEFERRED to next sprint** | **⏸ next-sprint** | ~9-10 days | **Deferred 2026-05-29 by owner**: existing event mapping/firing fidelity (CR-015) takes priority. §7 Q1–Q8 still open. | **2026-05-29** |
| **019** | **`send_bill` Event-Key Mismatch (UI vs Trigger Code)** | **CANCELLED — owner says not needed** | **❌** | — | Owner closed 2026-06-05: "not needed". | **2026-06-05** |
| **020** | **Template Variable Picker — Grouped UX + Menu Variable Family** | **CLOSED — QA 18/18 pass, all gates passed** | **🟢** | ~1.5 days | HTML mock approved → planning approved → implemented → QA report written. 40 variables with block field, 7 grouped blocks, Menu Pick mode, reusable VariablePicker component. All V1–V10 backend validations pass. Frontend compiled + screenshots verified. QA: `qa/CR_020_TEMPLATE_VARIABLE_PICKER_QA_REPORT.md`. | **2026-06-05** |
| **022** | **Coupon POS-side bug fixes: alias, display_title, same_item_required** | **CLOSED — 142/142 QA pass** | **🟢** | ~½ day | 4 owner-reported bugs fixed: (B1) POSCartItem.food_id alias didn't accept `item_id` → NTH/BOGO items not matched in validate; (B2) `category_id: None` hardcoded in order cart_dicts; (B3) `display_title` missing from POS coupon APIs — added `build_display_title()` helper; (B4) `same_item_required` form default/hydration forced true on all BOGO coupons. Files: `models/schemas.py`, `routers/pos.py`, `core/coupon.py`, `pages/CouponsPage.jsx`. | **2026-06-06** |
| **023** | **WhatsApp Template Builder — Production Readiness** | **🟢 CLOSED (CRM side) — Phase 1+2+3 all implemented: Meta API v21, en_US, duplicate check, status check, `validateMetaCompliance()` V1-V10 (Phase 2) + CR-066 V11-V23 (added), dynamic URL button, Add Variable, `create_meta_template()` backend, backend safety-net V11-V20. "Awaiting owner E2E test" was owner-side acceptance (submit real template to Meta), not a CRM code gap.** | **🟢** | done | **Phase 1**: Meta API v21, en_US locale, image header examples, status check, duplicate check, Meta error detail, full builder UI with WhatsApp preview. **Phase 2**: V1-V10 Meta compliance validations — `validateMetaCompliance()` frontend gate + real-time inline warnings + backend V1-V4 safety net. **Phase 3**: "Add Variable" button (body auto-increment at cursor + header {{1}} with disable), Dynamic URL button (Static/Dynamic toggle, base URL + {{1}} chip, sample URL, backend `example` array). `einvoice_token` variable added to registry (41 total). E2E submission tested: payload correctly formatted with dynamic URL button, reached Meta. Planning: `CR_023_PHASE2_*.md` + `CR_023_PHASE3_*.md`. **Next**: owner E2E with longer body + View Bill button → Meta approval. Then AuthKey button param wiring. | **2026-06-06** |
| **024** | **Segments & Marketing Campaigns — Production Readiness** | **All Phases (1-4) IMPLEMENTED — gated by `CAMPAIGN_SCHEDULER_ENABLED`** | **🟢** | ~4-5 days (4 phases) | **Phase 1**: CRUD + execution engine + 4 frontend pages + sidebar restructure. Live test: 1 WhatsApp sent. **Phases 2-3 (Scheduled + Recurring)**: BUILT — `core/campaign_jobs.py` (291 LOC): `compute_next_run_at()` for one-time scheduled (date+time) + recurring (daily/weekly/monthly) with end conditions (never/after N occurrences/after date). APScheduler job `process_due_campaigns` every 1 min with atomic claim, stale detection (24h), recurring recomputation. **Phase 4 (Polish)**: Pause/Resume endpoints, edit-while-scheduled guard, `backfill_next_run_at()` startup migration. Frontend wizard has full schedule UI (3 types, frequency selectors, end conditions). Tests: `test_campaign_jobs.py` (168 LOC). **Gated**: `CAMPAIGN_SCHEDULER_ENABLED=false` — flip to `true` to enable auto-fire. | **2026-06-18** |
| **025** | **Virtual Wallet Management** | **Discovery Phase 0 complete — PARKED awaiting Q1-Q10** | **⏸** | ~11-15 days (4 phases) | Canteen/student/mess/subscription wallet. Current state: placeholder UI, basic credit/debit backend, 0 tenants enabled. 12 gaps identified (G1-G12). 4 phases planned: P0 (dashboard + bulk recharge + ledger + rules), P1 (refund + reports), P2 (meal plans + expiry), P3 (self-recharge via payment gateway). Discovery: `CR_025_VIRTUAL_WALLET_MANAGEMENT_DISCOVERY.md`. **Next**: Owner answers Q1-Q10 → planning doc. | **2026-06-06** |
| **026** | **Campaign "View Messages" Deep-Link** | **Registered — P3 priority** | **📋** | ~½ day | Add "View Messages" button on each campaign card (CampaignsPage) that navigates to Message Status pre-filtered by `campaign_id`. Requires: (1) route param support on MessageStatusPage (e.g., `/messages?campaign_id=xxx`), (2) button on CampaignsPage campaign rows, (3) auto-apply filter on mount. Depends on BUG-005+BUG-006 being fixed (✅ done 2026-06-17). | **2026-06-17** |
| **027** | **Hardcoded Config → Environment Variables** | **Part 1 IMPLEMENTED — zero hardcoding verified** | **🟢** | ~½ day | 22 env variables (was 3 in `.env`, now all 25). Full audit: MyGenie POS (4), AuthKey (4), Meta Graph API (1), JWT secret (1), External URLs (3), Campaign scheduler (2), POS request logging (7). All hardcoded defaults removed from production code. Grep audit: 0 remaining hardcoded values. Discovery: `discovery/CR_027_*.md`. Planning: `planning/CR_027_*.md`. Implementation: `implementation/CR_027_*.md`. | **2026-06-18** |
| **028** | **POS Integration Settings UI + BUG-008 Fix** | **IMPLEMENTED + regression passed** | **🟢** | ~½ day | **BUG-008 FIXED**: Gate login push (skip if `crm_token_registered_with_pos=true`), push new key on regeneration with flag reset. Function moved to `core/auth.py` (shared). **CR-028 BUILT**: Settings page "POS Integration" card — masked API key + show/hide + copy + Regenerate with AlertDialog confirmation. Regression: R1-R10 all pass (login, /me, POS auth, regenerate push, campaign tests 10/10). Planning: `planning/BUG_008_CR_028_POS_KEY_SETTINGS_PLAN.md`. | **2026-06-18** |
| **029** | **Disable Forgot Password (OTP security)** | **IMPLEMENTED — link hidden** | **🟢** | 5 min | "Forgot password?" link commented out on LoginPage.jsx. Reason: OTP returned in API response (`"otp": otp` on lines 608, 617 of auth.py) — security risk for production. Backend endpoints preserved (not deleted). **Re-enable when**: (1) OTP removed from API response, (2) WhatsApp/SMS delivery confirmed working for all tenants. | **2026-06-18** |
| **030** | **Freshmarketer Webhook Endpoint (`POST /api/pos/webhook`)** | **🟢 IMPLEMENTED + QA 15/15 passed** | **🟢** | ~2.5 hrs | Freshmarketer sends nested envelope: `{Body: {event, id, data: {contact, event_details, custom_data}}}`. Extract `custom_data` for our params, int→str coercion (event_time as Union[int,str], coerce_numbers_to_str on contact/event_details), contact fallback, idempotency on `Body.id`, new `webhook_logs` collection, reuse DirectSend send logic. 3 Pydantic type bugs found+fixed by QA agent. QA: iteration_6.json (15/15). Discovery: `discovery/CR_030_*.md`. Plan: `planning/CR_030_*.md`. | **2026-06-26** |
| **031** | **TemplatesPage — Tab restructure (CRM tab + AuthKey tab)** | **Discovery + plan drafted — DEFERRED** | **⏸** | ~70 min | Proposed to split TemplatesPage's stacked CRM + AuthKey sections into two tabs so per-tab filters/counts operate on a homogeneous list, collapsing INV-002's 5 defects. Owner elected to ship CR-032 (feature flag) first — hiding CRM section for 79% of tenants reduces urgency. Revisit when adoption of `features.crm_templates_enabled` grows or INV-002 defects escalate for the 6 enabled tenants. Doc: `discovery/CR_031_TEMPLATES_PAGE_TAB_RESTRUCTURE_DISCOVERY_AND_PLAN.md`. | **2026-07-01** |
| **032** | **CRM Template Builder — per-tenant feature flag (self-service)** | **🔵 Intake complete — awaits planning approval** | **🔵** | ~2 hrs | Add `features.crm_templates_enabled` bool on `users` (default false). Self-service `<Switch>` on Settings page. When off, hide TemplatesPage CRM section + Add Template button + Set Labels + `/template-builder` route. Backend `PATCH /api/settings/features` endpoint. Backfill script auto-enables the 6 existing tenants with custom_templates. Backend send path (Freshmarketer webhook, DirectSend, campaigns) NOT gated — flag is UI-only. Zero hotspot files. Zero schema migration (Mongo). Intake: `discovery/CR_032_CRM_TEMPLATES_FEATURE_FLAG_INTAKE.md`. | **2026-07-01** |
| **033** | **Additional Audience Filters** | **✅ QA PASS · Owner UAT ready (2026-07-06)** | **✅** | done | BE: `core/helpers.py` (async `build_customer_query`, 20 filter blocks P0+P1+P2), `routers/campaigns.py` + `routers/customers.py` (await call sites). FE: `AudiencesPage.jsx` rebuilt — max-w-2xl dialog, 5 accordion sections (Loyalty/Dates/WA/Flags/Tags), dismissible chips, 20 filter controls. BUG-A fixed. **QA (iteration_5, 2026-07-06)**: 6 preprod DB pytest tests + UI smoke PASS — dialog renders 5 sections with correct default open/collapsed state; BUG-A regression assertion (T02/T03/T04) confirmed subset counts, not total-customer-count leaks; ANY/ALL and combined AND logic verified; save+delete of `QA-Smoke-*` audience OK. | **2026-07-06** |
| **034** | **Customer Tag System (free-form tags + tag-based audience filter)** | **✅ QA PASS · Owner UAT ready (2026-07-06)** | **✅** | done | `models/schemas.py` (tags field on CustomerBase/CustomerUpdate/Customer), `core/helpers.py` (tags filter block), `routers/customers.py` (5 tag endpoints: GET /tags, POST /{id}/tags, DELETE /{id}/tags/{tag}, POST /bulk-tag, POST /bulk-untag), migration script (2 VIP customers backfilled). FE: `TagChip.jsx` (new), `AudiencesPage.jsx` (Section 5 tags + ANY/ALL toggle), `CustomersPage.jsx` (tag chips per row + popover), `CustomerDetailPage.jsx` (tag chips + add-tag popover). **QA (iteration_5, 2026-07-06)**: 5 preprod DB pytest tests + UI smoke PASS — add/remove tag, catalog $addToSet, tags audience filter preview, ANY/ALL. CAFE 103 tenant has 0 vip_flag customers so VIP-backfill count=0 for that tenant (correct); kunafamahal tenant has 2 (correct). Frontend: '+ tag' popover on every row + tag chips render with × dismiss. | **2026-07-06** |


| **037** | **Template Status Sync Fix — AuthKey sync overwrites rejected status** | **✅ QA PASS · Owner UAT ready (2026-07-06)** | **✅** | done | `sync_authkey_templates()` line 721 fixed — projection now includes `status:1`; overwrite to `"approved"` gated behind `current_status != "rejected"`. `authkey_wid` still back-filled on rejected templates so owner can resubmit. **QA (iteration_5, 2026-07-06)**: end-to-end preprod DB test — forced 1 real `custom_templates` doc (kunafamahal tenant) to `status='rejected'` → POST /api/whatsapp/authkey/sync-templates → re-read: status still `'rejected'`, `authkey_wid` back-filled. Reverted doc to original in `finally`. Frontend `TemplateBuilderPage.jsx` submit button correctly enabled for rejected (only disabled when status==="approved"). Plan: `planning/CR_037_TEMPLATE_STATUS_SYNC_FIX_PLAN.md`. | **2026-07-06** |
| **035** | **Customer List Export & Import** | **✅ QA PASS · Owner UAT ready (2026-07-04)** | **✅** | ~8-10 hrs (delivered + QA'd) | Backend endpoints all live in `routers/customers.py`: `GET /customers/export` (CSV/XLSX, 22 fields, orange #F26B33 header, filter-aware, 5000-row cap, tags column included) · `GET /customers/sample-import-template` · `POST /customers/import-preview` (dry-run diff) · `POST /customers/import` (update-existing on phone, additive tag merge, `$addToSet` on `users.available_tags`) · `GET /customers/import-history` (last 10). All Q1–Q10 decisions honoured. **QA** (2026-07-04, iteration_4): 19/19 backend pytest PASS (`/app/backend/tests/test_cr035_customer_export_import.py`) + all frontend data-testids validated + 3-step import modal flow verified. Verification matrix V1-V25 all PASS. Non-blocking minor items: file-size check happens post-`await file.read()` (10MB cap still holds), Import Dialog missing `aria-describedby` (a11y warning), `customers.py` file >1500 LOC (refactor recommendation, not urgent). Intake: `discovery/CR_035_*.md`. Plan: `planning/CR_035_*.md`. Test report: `/app/test_reports/iteration_4.json`. | **2026-07-04** |
| **036** | **Media Header Template Fix — All Batches** | **🟢 CLOSED (production code) — Batch A + A.1 + B.0 + B.0.1 + B.1 + B.2 + B.3 ALL SHIPPED + QA PASS. B.1: Meta /uploads dual-upload (meta_media.py 109 LOC + S3), Q16/Q17/Q19 wired. B.2: media_missing fail-loud, status_note, enrichment. B.3: chunked upload (init/chunk/complete), resend eligibility. QA: iteration_8 9/9 PASS + iteration_9 3/3 PASS. B.4 is test-automation only (no production code) — Playwright Q22 + live-send Q23 pending owner approval.** | **🟢** | done | **Batches A + A.1 + B.0 + B.0.1 shipped** (see earlier rows). **2026-07-11 unblock sequence**: (1) Q14 REVERTED — owner reasoning "single Meta ID of AuthKey" — shared env `META_APP_ID=874516431301713` set in `backend/.env`; per-tenant `users.meta_app_id` retained as optional override. (2) Live-probe validation: `POST /v21.0/874516431301713/uploads` returned 200 for 3/3 probed tenants (jehsnest+kunafamahal AuthkeyK, mayur AuthkeyP outlier) → env-route confirmed for all 6 tenants; 2-System-User finding correctly reinterpreted as "same Meta App, two System Users under it". (3) Impact analysis authored `planning/CR_036_BATCH_B1_IMPACT_ANALYSIS_2026_07_11.md` — enumerates 4 template-origin × 4 send-path scenarios, 13 gaps, 26 verifications. (4) Owner locked **Q15-(a)+(c)** (import-from-AuthKey path fails-loud + inline Re-upload button) · **Q16-(a)** (block media edit on approved with 400) · **Q17-(a)** (test-send auto-injects stored `send_media_url`) · **Q18-(a)** (dynamic `{{1}}` header OUT of scope) · **Q19 APPROVED** (`core/whatsapp.py::send_event_message` +5 LOC additive fallback hotspot). Test tenant `owner@jehsnest.com / Qplazm@10` (Jeh's Nest) added to `test_credentials.md`. All previously-blocking questions resolved: per-tenant APP_ID → eliminated; AWS S3 creds → already populated; scope gaps → locked. Docs: `discovery/CR_036_*`, `discovery/INV_005_*`, `discovery/INV_006_*`, `planning/CR_036_*` (impl plan + amendment + planning RCA + **NEW B.1 impact analysis**). | **2026-07-11 (B.1 gate-open)** |
| **038** | **Campaign Scheduler Scale-Out (sequential outer loop)** | **📋 Registered — Discovery complete, awaits Q1-Q4 + owner priority** | **📋** | ~30 min – ½ day (option-dependent) | CR-024 minute-cron processes due campaigns **sequentially** inside a `for camp in due:` loop. Not a defect (max_instances=1 + coalesce + 15s httpx timeouts + atomic claim = server-hang-safe). But under scale, N large campaigns due at the same clock minute produce user-visible delivery delays (e.g., 3×5000-recipient campaigns → ~7.5 min wall-clock; campaign #3 fires 7 min after scheduled time). Options: (A) parallelise outer loop w/ asyncio.Semaphore, (B) per-tick time budget (safest), (C) fire-and-forget dispatch, (F) motor `serverSelectionTimeoutMS=5000` for Atlas primary-loss resilience. **Open Q1-Q4**: SLA on scheduled fire-time, expected peak concurrency, F approval, sprint priority. Discovery: `discovery/CR_038_CAMPAIGN_SCHEDULER_SCALE_OUT_DISCOVERY.md`. Source: INV-2026-07-03-02. | **2026-07-03** |
| **039** | **🚨 Webhook Row Disambiguation (Composite-Key Lookup)** — P1 CRITICAL | **✅ IMPLEMENTED + LIVE-TESTED (Tests A, C, D PASSED)** | **✅** | ~1 hour (~20 LOC, 1 file) | **Silent data corruption in every multi-recipient campaign FIXED.** AuthKey returns duplicate `LogID` values for concurrent single-mobile sends, so N rows in `whatsapp_message_logs` share the same `message_id`. Old webhook handler at `routers/whatsapp.py:1399` used `find_one({"message_id": logid})` → always returned first-inserted row → rows 2..N stayed `pending` forever even though actually delivered. **Fix applied (2026-07-03)**: composite `(message_id, customer_phone)` lookup with legacy fallback + `verdict="ambiguous_row"` skip on persistent mismatch. **Live-verified**: fresh Jeh's Nest 3-recipient campaign at 09:29 UTC — all 3 rows updated correctly (delivered/rejected/rejected) despite AuthKey STILL returning identical LogID `e7c8c949d03048b05dbc981aecf4bb26`. Zero `ambiguous_row` verdicts on real traffic. **QA regression**: 10/11 pytest tests PASS (11th test surfaces CR-041 pre-existing bug, unrelated). Discovery: `discovery/CR_039_WEBHOOK_ROW_DISAMBIGUATION_DISCOVERY.md`. Plan: `planning/CR_039_WEBHOOK_ROW_DISAMBIGUATION_PLAN.md`. Source: INV-2026-07-03-04. Follow-ups: CR-039-F1/F2/F3 (deferred), CR-040 (AuthKey escalation, pending). | **2026-07-03** |
| **040** | **AuthKey Duplicate-LogID Upstream Escalation** | **📋 Registered — owner-side escalation, zero CRM code** | **📋** | 0 dev hours (external) | Vendor-side root cause of the issue CR-039 defensively worked around. AuthKey's `sendBulkSMS.php` returns identical `LogID` for concurrent single-mobile sends inside the same campaign (verified: Jeh's Nest 3-recipient campaign, 3 rows sharing LogID `e7c8c949d03048b05dbc981aecf4bb26`). CRM shipped composite-key tie-breaker (CR-039) — no customer-visible defect today, but CR-039 is a WORKAROUND. Escalation exists so vendor fix isn't quietly lost + new AuthKey flows don't hit new edge cases. **Owner actions**: (1) open ticket with AuthKey quoting affected LogIDs (Mongo query in intake doc §5), (2) log vendor response in `DECISIONS_LOG.md`, (3) close CR-040 when vendor confirms fix. Zero CRM code. **Owner Q1** open: nightly duplicate-LogID audit cron (~15 LOC future micro-CR) — Y/N? Intake: `discovery/CR_040_AUTHKEY_DUPLICATE_LOGID_ESCALATION_INTAKE.md`. | **2026-07-04** |
| **042** | **Download Message Report per Campaign / Event** | **✅ QA PASS · Owner UAT ready** | **✅** | ~3 hrs actual | Testing agent iteration_3: 38/38 backend pytest PASS + all Playwright verifications PASS. **Backend live**: `GET /api/whatsapp/message-logs/export?format=csv|xlsx` with shared `_build_message_log_query()` helper + row cap 5000 + X-Row-Count/X-Row-Cap headers. **Frontend live**: Export dropdown on MessageStatusPage (filter-aware) + per-row Export dropdown on CampaignHistoryPage (run-scoped, unique wrapper attr per row after M1 fix). Files touched: `backend/routers/whatsapp.py` (+~230 LOC), `frontend/src/pages/MessageStatusPage.jsx` (+~90 LOC), `frontend/src/pages/CampaignHistoryPage.jsx` (+~90 LOC). Test report: `/app/test_reports/iteration_3.json`. QA handover: `crm_roi_sprint/qa/BUG_009_CR_042_QA_HANDOVER.md`. | **2026-07-03** |
| **BUG-009** | **"Details" button on Marketing > History does nothing** | **✅ QA PASS · Owner UAT ready** | **✅** | ~30 min actual | Testing agent iteration_3 confirmed BUG-009 end-to-end restored after C1 hotfix. Details button navigates to `/message-status?campaign_id=X&run_id=Y` (was `/messages` — invalid route, sent user to Dashboard). MessageStatusPage reads both URL params and displays green emerald "🎯 Filtered to run" banner + Clear-run-filter button that preserves campaign_id. Files touched: same 3 files as CR-042. Test report: `/app/test_reports/iteration_3.json`. | **2026-07-03** |
| **043** | **Filter by Customer Tags on Customers page + tag-popover UX rework (Option C hybrid + Option C multi-select popover)** | **✅ QA PASS · Owner UAT ready** | **✅** | ~2.5 hrs actual | Testing agent iteration_3 verified all Part A + Part B flows. **Backend live**: `list_customers` gains `tags` + `tags_mode` params (`$in`/`$all` on `customers.tags`); `/customers/tags?with_counts=true` returns `[{tag, count}]` via aggregation; composite index `idx_customers_user_tags {user_id:1, tags:1}` on startup. **Frontend live**: chip strip above filter block with top-6 by count, `More/Less` toggle, ANY/ALL radios, Clear button, active-tag dismissible chips, chip label spacing fixed (`+ Lunch (6)` after L2). Popover reworked to multi-select autosave — checkbox per tag with count badge, inline "Create new tag", "Done" dismiss. Bulk-apply is CR-045 (parked). Files touched: `backend/routers/customers.py` (+~50 LOC), `backend/server.py` (+~7 LOC), `frontend/src/pages/CustomersPage.jsx` (+~215 LOC net). Test report: `/app/test_reports/iteration_3.json`. QA handover: `crm_roi_sprint/qa/CR_043_QA_HANDOVER.md`. **Note**: pytest suite leaks a `TESTTAG_*` catalog entry per run — cosmetic; teardown fixture pending as future micro-CR. | **2026-07-03** |
| **045** | **Bulk actions on customers (bulk-tag, bulk-delete, bulk-export, bulk-message)** | **⏸ PARKED — awaits owner promotion** | **⏸** | TBD | Split off from CR-043 (bulk-apply-from-popover was out of scope). Requires row-selection column on CustomersPage + backend bulk endpoints + confirmation dialogs. Registered 2026-07-03 to preserve scope-split traceability. No discovery / planning until owner promotes. Decision: `DECISIONS_LOG.md § 2026-07-03 [CR-045]`. | **2026-07-03** |
| **041** | **Webhook Timestamp Overwrite on transition_ignored (P1 HIGH, pre-existing)** | **✅ IMPLEMENTED + REGRESSION VERIFIED (11/11 pytest PASS)** | **✅** | ~15 min (~10 LOC, 1 file re-ordering) | **Silent corruption of `delivered_at` / `read_at` / `rejected_at` on every duplicate or out-of-order webhook FIXED.** Bug at `routers/whatsapp.py:1454-1469` assigned timestamps BEFORE state-machine gate at line 1472 → even when transition correctly rejected as `transition_ignored`, timestamps still got overwritten. **Blast-radius (live prod scan)**: 1 row detected with obvious corruption (`phone=7505242126`, `status=rejected` but `delivered_at=15:12` set 6h AFTER `rejected_at=08:50`) plus 1 more with `delivered_at > rejected_at`; invisible corruption (delivered_at → later delivered_at retry) larger. **Impact**: MessageStatusPage relative-time display wrong on retries; future analytics inherit bad data. **Discovered by**: QA agent Test 11 during CR-039 regression (2026-07-03). **Fix applied (2026-07-03)**: Option A — moved timestamp block AFTER state-machine gate, gated by `applied`. Metadata fields (`meta_message_id`, `keypress`, `button_param_value`, `channel`) left ungated to preserve late-arriving button-press annotations. Pure statement re-ordering, zero logic change. **Regression**: `pytest tests/test_cr039_webhook.py -n 0 -v` → **11/11 PASS** (was 10/11). Discovery: `discovery/CR_041_TIMESTAMP_OVERWRITE_DISCOVERY.md`. Impact: `planning/CR_041_TIMESTAMP_OVERWRITE_IMPACT_ANALYSIS.md`. Plan: `planning/CR_041_TIMESTAMP_OVERWRITE_PLAN.md`. Follow-ups: CR-041-B (retroactive audit script), F1 (split file), F2 (compound unique index), F3 (enable AuthKey HMAC). | **2026-07-03** |
| **046** | **🚨 MongoDB Lockdown + Backup/Restore Verification** (AUDIT SEC-01 + REL-02) | **📋 Registered — P0 CRITICAL, owner-infra, zero CRM code** | **📋** | 0 dev hrs (external infra) | DB on public IP `52.66.232.149:27017`, no TLS, ransomware indicator DB observed; backup snapshot failures (2026-07-02) never re-verified → no proven restore path. **Owner actions**: private subnet/VPC + security-group allowlist; rotate `mygenie_admin` creds; enable TLS; scoped non-admin app user; forensic snapshot then remove artifact; automated snapshots + oplog PITR + quarterly restore drill. Pattern follows CR-040 (owner-side escalation row). Intake: `discovery/CR_046_059_AUDIT_REMEDIATION_BATCH_INTAKE.md`. Source: `ARCHITECTURE_AUDIT.md` SEC-01, REL-02. | **2026-07-06** |
| **047** | **Webhook HMAC Activation + CORS Origin Pinning** (AUDIT SEC-03 + SEC-02, absorbs CR-041-F3) | **📋 Registered — P0, partially blocked** | **📋** | ~2 hrs | (a) Set `AUTHKEY_WEBHOOK_SECRET` and activate the dormant HMAC verification at `routers/whatsapp.py:1527` + per-IP rate limit + reject unknown `logid` — **blocked on owner obtaining the secret from AuthKey**. (b) CORS: replace `*` with exact origin whitelist per env; fail startup if `*` when `ENV=production` — code-only, unblocked. Source: AUDIT SEC-03, SEC-02. | **2026-07-06** |
| **048** | **Auth Quick Hardening — remove stored password + rate limiting** (AUDIT SEC-04 + SEC-06) | **📋 Registered — P0** | **📋** | ~3-4 hrs | (a) Delete `remembered_password` plaintext persistence from `LoginPage.jsx` (keep remembered email only). (b) Add `slowapi` rate limiting: 5/min/IP on `/api/auth/login`, 3-attempt lockout on OTP verification, defaults elsewhere. ⚠ HIGH-risk area (auth) — integration playbook + owner approval before implementation per addendum §14. Source: AUDIT SEC-04, SEC-06. | **2026-07-06** |
| **049** | **Worker/API Process Split + Redis + Distributed Job Locks** (AUDIT SCA-01 + SCA-05) | **📋 Registered — P1, prerequisite for CR-050** | **📋** | ~2-3 days | Extract APScheduler into dedicated worker deployment (`ROLE=worker` env flag; API pods never start scheduler). Add Redis (also serves CR-048 limits, CR-050 queue, caching). Distributed locks (Redis SET NX / Mongo lease) on ALL cron jobs — Daily Loyalty currently unguarded against multi-replica double-fire (financial risk). Tenant-scoped dashboard/config caching as part 2. Source: AUDIT SCA-01, SCA-05. | **2026-07-06** |
| **050** | **Queue-Based Campaign Sending + Retry/DLQ** (AUDIT SCA-02 + REL-05, supersedes CR-038 options A-C) | **📋 Registered — P1, depends on CR-049** | **📋** | ~3-4 days | Scheduler tick only enqueues `(campaign_id, batch)` jobs; scalable worker pool consumes with per-tenant concurrency limits, exponential-backoff retry (timeout/5xx only), dead-letter queue + daily digest; resumable progress in `campaign_runs`. Fixes skipped-tick campaign slippage under load. CR-038 remains open only for its Q1-Q4 SLA answers, which feed this design. Source: AUDIT SCA-02, REL-05. | **2026-07-06** |
| **051** | **Observability Foundation — Sentry + structured logs + metrics + real health checks** (AUDIT MON-01 + MON-03 + REL-04 + REL-06) | **📋 Registered — P1** | **📋** | ~2 days | (1) Sentry FE+BE. (2) Structured JSON logs with `tenant_id`/`request_id` contextvars. (3) `/metrics` via prometheus-fastapi-instrumentator (latency, error rate, scheduler tick duration, send success ratio). (4) `/health/live` vs `/health/ready` (Mongo ping + scheduler heartbeat age) wired to probes. (5) Scheduler heartbeat doc + gap alert. (6) Unique-index creation failures abort production boot instead of silent pass. Source: AUDIT MON-01, MON-03, REL-04, REL-06. | **2026-07-06** |
| **052** | **CI Pipeline + Branch Model + Staging Environment** (AUDIT MAI-02 + DEP-01 + DEP-02) | **📋 Registered — P1, partly owner-infra** | **📋** | ~2 days + infra | GitHub Actions on PR: pytest, flake8, eslint, `yarn build`, pip-audit/yarn audit. Protected `main` + short-lived feature branches + tagged releases (replaces session-branch model). Staging env with anonymized DB copy (preview stops defaulting to live preprod data). Production runbook: build → promote → smoke → rollback. Source: AUDIT MAI-02, DEP-01, DEP-02. | **2026-07-06** |
| **053** | **Session/Auth Overhaul — refresh tokens + httpOnly cookies + revocation** (AUDIT SEC-05) | **📋 Registered — P1, HIGH-risk gate** | **📋** | ~2 days | 15-min access tokens + rotating refresh tokens in `httpOnly; Secure; SameSite=Strict` cookies; per-user `token_version` bumped on password change / logout-all, checked in `get_current_user`. ⚠ CRITICAL-risk area (auth/login/SSO) — full gate flow + owner approval + integration playbook mandatory. Source: AUDIT SEC-05. | **2026-07-06** |
| **054** | **Tenant Isolation Enforcement Layer + Isolation Test Suite** (AUDIT SCA-03) | **📋 Registered — P2** | **📋** | ~3-5 days phased | Thin tenant-scoped data-access helper that force-injects `user_id` into every query; migrate endpoints incrementally, hotspot files first. Two-tenant isolation pytest suite asserting zero leakage on every list endpoint. Shard-key plan `{user_id, _id}` documented for `customers`/`orders`/`whatsapp_message_logs`. Source: AUDIT SCA-03. | **2026-07-06** |
| **055** | **Data Hygiene Bundle — unique webhook index + campaign_id migration + TTL retention + schema validators** (AUDIT PER-02 + PER-03 + DM-01 + DM-02, absorbs CR-041-F2) | **📋 Registered — P2** | **📋** | ~2 days | (a) Dedupe then unique compound index `(message_id, customer_phone)` on `whatsapp_message_logs` (ships deferred CR-041-F2). (b) One-time batched migration normalizing legacy `campaign_id=run_id` rows → delete `$or` query branches. (c) TTL indexes on raw audit logs (30-90d) + 12-month cold archival policy for message logs. (d) Moderate `$jsonSchema` validators on top-6 collections. ⚠ Live-preprod-data migrations — owner approval + backup precondition (CR-046) required first. Source: AUDIT PER-02, PER-03, DM-01, DM-02. | **2026-07-06** |
| **056** | **MyGenie SSO Resilience — circuit breaker + degraded login** (AUDIT REL-01) | **📋 Registered — P2, HIGH-risk gate** | **📋** | ~1-2 days | Strict timeouts + circuit breaker on MyGenie login/profile calls. Degraded-mode login: if MyGenie unreachable and user has prior login, verify local `password_hash` → session flagged `sso_degraded=true` (POS-fresh features disabled). Async profile refresh post-login. ⚠ CRITICAL-risk area (auth/SSO) — full gate flow + owner approval. Source: AUDIT REL-01. | **2026-07-06** |
| **057** | **Config Validation + S3-Mandatory-in-Prod + POS API v1 Versioning** (AUDIT DEP-03 + REL-03 + MAI-03) | **📋 Registered — P2** | **📋** | ~1-2 days | (a) `core/config.py` (pydantic-settings) declaring every env var with per-`ENV` requirements; production boot fails loudly on missing/insecure values (CORS `*`, unset webhook secret). (b) S3 mandatory in production (disk fallback dev-only). (c) Freeze POS contract as `/api/pos/v1/*` (alias existing paths), breaking changes → v2 with deprecation window, publish OpenAPI subset for POS team. Source: AUDIT DEP-03, REL-03, MAI-03. | **2026-07-06** |
| **058** | **Secrets Management + Tenant Credential Encryption + Hashed POS Keys** (AUDIT SEC-07 + SEC-08 + SEC-09) | **📋 Registered — P2** | **📋** | ~2-3 days | (a) Secrets manager (AWS Secrets Manager/SSM) at deploy; rotation runbooks. (b) Envelope-encrypt per-tenant AuthKey/Meta credentials in `users` (KMS master key). (c) POS API keys stored as hashes + identifying prefix + dual-key rotation window. (d) Security headers middleware + dependency scanning in CI (with CR-052). Source: AUDIT SEC-07, SEC-08, SEC-09. | **2026-07-06** |
| **060** | **Customer Import modal — bigger + clickable error rows in preview + modal consistency (60-B)** | **📋 Registered — P2, awaits Q1-Q3** | **📋** | ~½ day (60-A) | Preview step shows only first-5 rows; red "Errors" card not clickable; backend `import-preview` already returns `all_errors` (unused by frontend). 60-A: enlarge Dialog (`CustomersPage.jsx:2808` max-w-lg) + show error rows pre-import. 60-B: app-wide modal consistency sweep (design agent) — split, park until owner promotes. Intake: `discovery/SESSION_2026_07_12_BATCH_INTAKE.md` §1. | **2026-07-12** |
| **061** | **CRM-built templates — gate REMOVED, all tenants can author** | **✅ QA PASS 13/13 (2026-08-06, iteration_8) — Owner smoke pending** | **✅** | done | Gate removal per owner: `_require_crm_templates_enabled()` commented out at 6 authoring endpoints in `routers/whatsapp.py`. `crm_templates_enabled` flag is frontend-only visibility (reads `CRM_TEMPLATES_ALLOWED_RESTAURANT_IDS=510` env). Test file: `tests/test_cr061_gate_removal.py` (testing agent not yet run). | **2026-08-06** | ~1 day | TemplatesPage renders "CRM Templates" section + Template Builder for ALL tenants today. Add env allowlist (e.g. `CRM_TEMPLATES_ENABLED_TENANTS`) gating custom-templates list, Builder entry points, and (Q4) wizard dropdown. Recommend UI-hide + backend 403. Intake: `discovery/SESSION_2026_07_12_BATCH_INTAKE.md` §2. | **2026-07-12** |
| **062** | **Template Builder — formatting toolbar (bold/italic/strike/mono)** | **🔵 IMPL GATE OPEN — plan §D, mockup approved** | **🔵** | ~½ day | Owner asked "where is restriction — Meta side?" → ANSWERED: no restriction anywhere; Meta supports `*bold*` `_italic_` `~strike~` ` ```mono``` ` markers in BODY (works today if typed manually); builder just lacks a toolbar + preview rendering. Frontend-only. Intake: `discovery/SESSION_2026_07_12_BATCH_INTAKE.md` §4. | **2026-07-12** |
| **063** | **WhatsApp Opt-in toggle on Customer DETAIL page edit modal** | **📋 Registered — P2** | **📋** | ~2 hrs | LIST edit modal has working toggle (verified live: PUT true/false round-trips persist); `CustomerDetailPage.jsx::openEditModal` (L201-219) omits `whatsapp_opt_in` — detail edit has no toggle, page never shows opt-in status. FE-only, LOW risk. Intake: `discovery/SESSION_2026_07_14_BATCH_INTAKE.md` §3 · INV-007 §1. | **2026-07-14** |
| **063** | **WhatsApp Opt-in toggle on Customer DETAIL page edit modal** | **✅ IMPLEMENTED + QA PASS (iteration_19, 100% BE+FE) · Owner smoke pending** | **🟢** | done | Shipped 2026-07-14 (code marker CR-063): `Switch` import + `whatsapp_opt_in` in `openEditModal` + toggle block below Phone (`detail-edit-whatsapp-opt-in`) + Q-E=(b) header status badge (`detail-whatsapp-opt-in-badge`, Opted in/out). Verified: badge flips on save+refetch, switch reflects persisted state, no field clobbering, list-modal regression clean. | **2026-07-14** |
| **064** | **Customer Delete option (missing feature)** | **⏸ PARKED — owner D5=(c) 2026-07-14; hard-vs-soft policy deferred until revived** | **⏸** | ~1 day | No delete UI anywhere (never built, git-verified) — only Edit exists. Backend `DELETE /api/customers/{id}` exists but cascades only `points_transactions`; leaves orders/message logs/coupon_usage/feedback/wallet. Irreversible customer data → full gate + owner approval per addendum §14 when revived. Intake: `discovery/SESSION_2026_07_14_BATCH_INTAKE.md` §4 · INV-007 §5. | **2026-07-14** |
| **065** | **Message Status — show resend time on row ("Resent ×N")** | **✅ IMPLEMENTED + QA PASS (iteration_19, 100% BE+FE) · Owner smoke pending** | **🟢** | done | Shipped 2026-07-14 (code marker CR-065, D6=a): desktop time cell + mobile card show "Resent <relative>" + amber ×N badge when `resend_count>0` (testids `resent-time-*`/`resent-badge-*`/`mobile-resent-time-*`); original time stays in expanded history. Q-F=(a): report export gains "Resend Count" + "Last Resend At" columns (CSV+XLSX verified). Legacy-data scan: 0 resent logs missing `last_resend_at`. BUG-012 filter regression clean. | **2026-07-14** |
| **059** | **Scale Optimization Bundle (P3 backlog)** (AUDIT PER-01 + SCA-06/07/08/09 + MON-02/04 + DM-03/04 + MAI-04 + DEP-04 + SEC-10 + PER-04) | **📋 Registered — P3 backlog, split before implementation** | **📋** | TBD (split into sub-CRs when promoted) | Umbrella row so no audit finding is lost: analytics daily rollups + replica reads (PER-01) · per-tenant plan message limits with atomic counters (SCA-06) · Motor pool tuning (SCA-07) · PDF offload to worker (SCA-08) · bounded-concurrency vendor syncs (SCA-09) · staff-action audit trail (MON-02) · cron-log-driven metrics + status page (MON-04) · tag catalog cap (DM-03) · money-as-integer-paise migration (DM-04, coupon engine only inside fully-QA'd change) · frontend smoke tests (MAI-04) · multi-AZ replica + DR runbook (DEP-04) · OTP hardening (SEC-10) · N+1 endpoint consolidation (PER-04). Each item becomes its own CR at promotion time. Source: `ARCHITECTURE_AUDIT.md` Phase 3. | **2026-07-06** |
| **066** | **Template Builder Meta Compliance Validation — V11-V23 (formatting markers, variable position, emoji, whitespace, category)** | **✅ QA PASS 20/20 · BUG-015 FIXED · BUG-016→019 FIXED · Owner smoke pending** | **✅** | ~7 hrs (P0 ~3h + P1 ~2h + P2 ~1h + preview ~1h) | CR-023 Phase 2 shipped V1-V10. CR-062 shipped formatting toolbar. **Neither validated formatting markers** — orphan `_`/`*`/`~`/` ``` ` pass all checks, get Meta-approved, then FAIL at send-time ("error in message" from AuthKey). Confirmed failure: `daily_premiumlunchmenu_2026` (orphan `_`). **20 gaps identified (INV-009)**: P0 = V11 (unmatched markers) + V12 (var at start/end) + V13 (adjacent vars) + V14 (formatting wrapping vars) + V15 (body >1024 hard block); P1 = V16 (emoji >10) + V17 (>2 newlines) + V18 (>4 spaces/tabs) + V19 (>550 chars Marketing/Utility) + V20 (formatting in header/footer); P2 = V21 (category mismatch heuristic) + V22 (ALL CAPS) + V23 (URL shorteners). Also: preview should highlight orphan markers in red (currently renders as text = false confidence). Backend safety-net must mirror P0. **Files WILL change**: `TemplateBuilderPage.jsx` (validateMetaCompliance + getBodyWarnings + preview renderer), `routers/whatsapp.py` (create_meta_template backend gate). **Files WILL NOT change**: `core/whatsapp.py`, `core/campaign_jobs.py`, `routers/campaigns.py`, send path, webhook path. Source: INV-009. Intake: `discovery/CR_066_TEMPLATE_BUILDER_META_COMPLIANCE_V11_V23_INTAKE.md`. | **2026-07-16** |
| **067** | **WhatsApp Template Deletion + Lifecycle Sync** | **🔵 PLANNING APPROVED — Q1-Q4 all locked · implementation plan gate open** | **🔵** | ~2.5 hrs | **5 gaps found (INV-011)**: (1) CRM delete is local-only — no Meta `DELETE /{WABA_ID}/message_templates` call; (2) AuthKey has no public delete API; (3) `check_template_status` misses deleted templates (catches 404 as generic error); (4) AuthKey sync never removes stale local records; (5) No delete UI for AuthKey-sourced templates. **Phase 1** (no dependency): fix status-check detection + sync cleanup (~35 min). **Phase 2**: add Meta DELETE endpoint + delete UI with cascade (~75 min). **Phase 3** (blocked): AuthKey delete API — needs owner to contact AuthKey support. **Q1-Q4 open**: cascade behavior, in-use policy, auto-cleanup, priority. Source: INV-011. Intake: `discovery/CR_067_TEMPLATE_DELETION_LIFECYCLE_INTAKE.md`. | **2026-07-16** |
| **068** | **"Validate Template" — dry-run V1-V23 compliance check (no Meta WABA required)** | **🔵 PLANNING APPROVED — Q1-Q3 all locked · frontend-only · implementation plan gate open** | **🔵** | ~45 min | **Gap (BUG-019 root cause)**: CR-066/BUG-015 V1-V23 validation is gated inside `handleSubmitToMeta()` which requires a valid Meta WABA + access token. Tenants WITHOUT WABA configured cannot run any compliance check — they get a WABA-missing 503 before validation fires. **Proposed**: standalone "Validate Template" button on TemplateBuilderPage that runs `validateMetaCompliance()` + `getBodyWarnings()` + `getFooterWarnings()` client-side (frontend-only path). Optionally a `POST /api/whatsapp/validate-template-compliance` endpoint mirrors the same checks server-side with zero Meta/AuthKey API calls — returns structured report: `errors[]` (hard blocks), `warnings[]` (soft), `hints[]` (quality). **Q1** open: frontend-only vs frontend + new backend endpoint? **Q2** open: show inline report panel vs toast list? **Q3** open: priority (before or after CR-067)? Source: BUG-019 analysis (2026-07-17). | **2026-07-17** |
| **075** | **Hotel Guest Document Migration — POS → CRM** | **✅ QA PASS 10/10 · Owner smoke pending** | **✅** | ~2-3 hrs | Sync completed on restaurant_478 (18march): 39 docs migrated, 87 stubs skipped, 9 `/storage/;/` skipped. Idempotency confirmed (2nd sync: 0 new inserts). All naming convention checks PASS (file_name, s3_key, uploaded_by, source_url). Presigned URLs work. Sync log has all 5 `docs_*` fields. QA report: `qa/CR_075_QA_REPORT.md`. | **2026-08-06** | ~2-3 hrs | Extend existing `background_customer_sync()` in `routers/migration.py`. Same endpoint, same button, no new UI. **Q1 locked**: every sync (idempotency via source_url dedup). **Q2 locked**: skip+log on download failure. **Q3 locked**: all API URLs migrated regardless of host. **Q4 locked**: CR-072 naming convention — S3 key `customers/{id}/docs/{doc_type}/{uuid}.{ext}`, file_name `{doc_type}_{side}.{ext}`, `put_private_object`, `uploaded_by="migration"`, `source_url` stored for audit. 32 real docs confirmed (License×16, Aadhar×10, Passport×5, Other×1). 9 preprod docs permanently lost (skip `/storage/;/`). Files WILL change: `routers/migration.py` only. Intake: `discovery/CR_075_HOTEL_DOCUMENT_MIGRATION_INTAKE.md`. | **2026-08-06** |
| **077** | **Configurable Lifecycle & Intelligence Thresholds (Per-Tenant)** | **📋 REGISTERED — Intake complete, Q1-Q5 pending owner answers** | **📋** | ~3 hrs (Phase 1) + ~2 hrs (Phase 2 VIP auto-promote) + ~1.5 hrs (Phase 3 spend buckets) | **7 confirmed hardcoded areas (INV-014)**: F1 Lifecycle stage day boundaries (30/60/90 days hardcoded in `analytics.py:483`) · F2 Campaign daily limit (`DAILY_LIMIT=1000` in `campaigns.py:29`) · F3 Customer value band thresholds (VIP≥80/High≥60/Medium≥35 in `customer_intelligence.py:185`) · F4 VIP flag never auto-promoted — intelligence score computes `band="vip"` but result **never written to `customers.vip_flag`** · F5 Churn risk thresholds (High>0.7/Medium≥0.4) · F6 Spend filter bucket labels (₹500/2K/5K/10K) hardcoded in `helpers.py` + 3 FE pages · F7 Visit/points/wallet buckets. **Proposed**: extend `loyalty_settings` with new Block A (lifecycle days) + Block B (daily limit) + Block C (score bands) + Block D (VIP auto-promote) + Block E (high_spender_threshold). Defaults = current hardcoded values — zero behavior change on upgrade. **Q1-Q5 open**: config UI location · per-tenant vs global daily limit · VIP auto-promote/demote policy · spend bucket scope · phase priority. Source: INV-014. Intake: `discovery/CR_077_CONFIGURABLE_THRESHOLDS_INTAKE.md`. Cross-ref: INV-003 (VIP phantom feature, 2026-07-01). | **2026-08-05** |
| **076** | **Customer Lifecycle Re-Engage: Bulk Campaign + Automation** | **📋 REGISTERED — Intake complete, Q1-Q5 pending owner answers** | **📋** | ~3 hrs (Phase 1) + ~2 hrs (Phase 2) | **5 confirmed gaps (INV-014 sub-findings)**: G1 Re-engage button navigates to `?action=reengage` on CustomerDetailPage which ignores the param — zero action triggered · G2 No bulk selection UI on CustomerLifecyclePage · G3 No `lifecycle_stage` filter in `build_customer_query` · G4 No Lifecycle Stage accordion in AudiencesPage · G5 No campaign wizard shortcut from Lifecycle page. **Design prototype**: `cr076_lifecycle_design.html` (lifecycle page + Re-engage modal + Campaign modal) · `cr076_audience_chips.html` (full audience chip panel). Source: owner verbal report. Intake: `discovery/CR_076_LIFECYCLE_REENGAGE_INTAKE.md`. Q1-Q5 open. | **2026-08-05** |
| **069** | **Template Button Variable Mapping & Send-Path Support** | **🟡 IMPLEMENTED — 14/14 edits complete, self-test PASS on all screens, testing agent pending** | **🟡** | ~6-7 hrs | **10 surfaces blind to button components.** `final_bill` (wid=41354, Hungry Keya) has 2 buttons — "Feedback" (static URL) + "Bill" (dynamic URL `https://crm.mygenie.online/{{1}}`). Button data EXISTS in `custom_templates` but is never surfaced. (1) Backend enrichment skips `buttons`; (2) Map dialog only extracts body `{{N}}`; (3) WhatsApp previews don't render buttons; (4) Test Template modal lacks button inputs; (5) Campaign wizard `isFullyMapped` ignores buttons; (6) Send payload has no `buttonValues`. `einvoice_token` variable exists in registry but can't be wired to button URL. **Systemic** — affects all tenants with dynamic URL button templates. **Impact Analysis complete** (2026-07-29): 6 screens with before/after documented, 12 verification items. **Files WILL change**: `routers/whatsapp.py`, `core/whatsapp.py`, `routers/campaigns.py`, `TemplatesPage.jsx`, `CampaignWizardPage.jsx`, `WhatsAppAutomationContent.jsx`. **Files WILL NOT change**: `core/whatsapp_variables.py`, `core/coupon.py`, `core/loyalty.py`, `routers/pos.py`, `core/campaign_jobs.py`, `models/schemas.py`. **Q1 open**: does AuthKey accept `buttonValues` in send payload? (undocumented — test empirically). Source: INV-012. Intake: `discovery/CR_069_TEMPLATE_BUTTON_MAPPING_INTAKE.md`. Impact Analysis: `planning/CR_069_IMPACT_ANALYSIS_BEFORE_AFTER.md`. | **2026-07-29** |

> When a row's first column shows a number ≤ 010 with no detail above, look up the full row in `crm/crm_roi_sprint/00_register/ROI_MEASUREMENT_CR_REGISTER.md`.

---

## Active queue (in priority order — recommended sequence)

Owner can re-order; this is a recommendation. **CR-016 deferred to next sprint as of 2026-05-29.**

| Order | CR | Why first |
|---|---|---|
| — | ~~**CR-024**~~ | **🟢 CLOSED** — All phases implemented. Gated by `CAMPAIGN_SCHEDULER_ENABLED=false`. Flip to `true` when ready. |
| 1 | **CR-014** | E-Invoice — Phase 1+2+3 done. Awaiting POS team for hotel folio `room_info` fields (P0: room_number, check_in, check_out). |
| 2 | **CR-023** | WhatsApp Template Builder — Phase 1+2+3 done. Awaiting owner E2E test + AuthKey button param verification. |
| — | ~~CR-022~~ | **🟢 CLOSED** (2026-06-06) — POS coupon bug fixes. 142/142 QA pass. |
| — | ~~CR-021~~ | **🟢 CLOSED** (2026-06-06) — coupon engine distribute-first + POS-zero. 142/142 QA pass. |
| — | ~~CR-015~~ | **🟢 CLOSED** — live test passed (2026-05-29). |
| — | ~~CR-016~~ | **Deferred to next sprint** (2026-05-29). |

### Audit remediation queue (CR-046 → CR-059, registered 2026-07-06 from `ARCHITECTURE_AUDIT.md`)

| Phase | CRs | Gate note |
|---|---|---|
| **Phase 0 — stop the bleeding (P0)** | CR-046 (owner-infra) · CR-047 · CR-048 | CR-046 has no code; CR-047(a) blocked on AuthKey secret; CR-048 needs auth-gate approval |
| **Phase 1 — scale foundations (P1)** | CR-049 → CR-050 (ordered) · CR-051 · CR-052 · CR-053 | CR-050 depends on CR-049; CR-053 is CRITICAL-risk auth |
| **Phase 2 — hardening (P2)** | CR-054 · CR-055 · CR-056 · CR-057 · CR-058 | CR-055 migrations require CR-046 backups first; CR-056 is CRITICAL-risk auth |
| **Phase 3 — optimization (P3)** | CR-059 (umbrella — split at promotion) | Zero findings lost; sub-CRs minted when owner promotes |

---

## Out-of-sprint backlog (not yet registered)

| Idea | Source | Notes |
|---|---|---|
| Force re-send admin action for stuck-Pending rows | Suggested in CR-004 P3.5 finish summary | Owner declined backfill but per-row manual nudges may be useful |
| OR / nested condition logic | CR-016 out-of-scope | Would become CR-016b |
| Custom webhook signals (tenant-defined) | CR-016 out-of-scope | Would become CR-016c |
| Per-customer event mute / unsubscribe | CR-016 out-of-scope | Privacy / DPDP compliance CR |
| Event analytics dashboard | CR-016 out-of-scope | Read-side analytics CR |
| Multi-channel events (SMS, email, push) | CR-016 out-of-scope | Separate channel CR per provider |
| Credit notes (mutability of invoices) | CR-014 out-of-scope | GST compliance for amendments |
| Email invoice channel | CR-014 out-of-scope | When email channel exists |
| AuthKey button URL param wiring | CR-023 Phase 2 of einvoice_token | Pending owner AuthKey curl/docs for button params. Likely `bodyValues` sequential. |
| Rich text formatting toolbar (Bold/Italic/Strike) | CR-023 reference screenshot | Non-blocking cosmetic enhancement |
| Template gallery / pre-built restaurant templates | CR-023 future | Reduce template creation friction |

---

## Recent transitions (newest first)

| Date | CR | From → To |
|---|---|---|
| 2026-08-06 (QA PASS) | **CR-075** | 🟡 Implemented → **✅ QA PASS 10/10**. Restaurant_478 (18march): 39 docs migrated, 87 stubs skipped, 9 broken-URL skipped, 0 failures. Idempotency: 2nd sync 0 new inserts. All Q1-Q5 decisions verified live. QA report: `qa/CR_075_QA_REPORT.md`. |
| 2026-08-06 (IMPLEMENTED) | **CR-075** | 🔵 Planning Complete → **🟡 IMPLEMENTED**. 6 edits applied to `routers/customers.py`: E1 s3 import · E2 `_CR075_ID_TYPE_MAP`+`_CR075_EXT_CONTENT_TYPE` constants · E3 `doc_summary` init · E4 `_cr075_migrate_docs()` call at line ~535 · E5 5 doc-count fields in final sync log · E6 `_cr075_migrate_docs()` helper 80 LOC. Self-test 3/3 PASS: RUN1 counts (migrated=3/stubs=2/404=1) ✅ · Q1 idempotency (re-run: 0 inserts) ✅ · Q2 skip+log on failure ✅. Pre-existing issue noted: POS customer-migration endpoint times out at 60s on large payloads (existing code, not CR-075). Test file: `tests/test_cr075_doc_migration.py`. QA handover: `qa/CR_075_QA_HANDOVER.md`. |
| 2026-08-06 (PLANNING COMPLETE) | **CR-075** | 🔵 Planning Approved → **🔵 PLANNING COMPLETE**. 6 edits, 1 file. |
| 2026-08-06 (PLANNING APPROVED) | **CR-075** | 🟡 Impact Analysis → **🔵 PLANNING APPROVED — Q5 locked: (b) skip doc cap for migration (owner: "b"). All Q1-Q5 locked. Implementation plan gate open.** DECISIONS_LOG: 2026-08-06 §CR-075 Q5. |
| 2026-08-06 (IMPACT ANALYSIS) | **CR-075** | 🔵 Intake closed → **🟡 IMPACT ANALYSIS COMPLETE**. FILE CORRECTION: `background_customer_sync()` is in `routers/customers.py:182` (not `migration.py` as intake stated). Insertion point: line ~513, after `customer_id` resolved. New helper `_migrate_booking_documents()` ~55 LOC. Q5 surfaced: enforce 5-doc-per-type cap during migration? (customer "avi" has 15 License docs — cap would drop 10). Recommendation: skip cap for migration. Q1-Q4 locked. Impact Analysis: `planning/CR_075_IMPACT_ANALYSIS.md`. |
| 2026-08-06 (INTAKE CLOSED) | **CR-075** | 📋 Registered → **🔵 INTAKE CLOSED — All Q1-Q4 locked, ready for planning**. Q1: every sync (source_url idempotency). Q2: skip+log on failure. Q3: all API URLs migrated. Q4: CR-072 naming — `{doc_type}_{side}.{ext}`, `put_private_object`, `uploaded_by="migration"`, `source_url` stored. Intake: `discovery/CR_075_HOTEL_DOCUMENT_MIGRATION_INTAKE.md`. DECISIONS_LOG: 2026-08-06 §CR-075 Q1-Q4. |
| 2026-08-06 (CLOSED — owner decision) | **BUG-024** | 🔴 OPEN → **✅ CLOSED**. Template button URL missing `/api/invoices/` path — owner handled directly (production server / Meta template config). No CRM code change needed. |
| 2026-08-06 (QA PASS) | **CR-061** | ✅ IMPLEMENTED → **✅ QA PASS 13/13 (iteration_8) — Owner smoke pending**. Gate removal confirmed: `_require_crm_templates_enabled()` commented out at all 6 authoring endpoints. Non-allowlisted tenant (restaurant_635, Jeh's Nest) can call all authoring endpoints — no 403. `crm_templates_enabled` frontend flag still reads `CRM_TEMPLATES_ALLOWED_RESTAURANT_IDS=510` correctly (returns false for restaurant_635). GET endpoints regression clean. |
| 2026-08-06 (QA PASS) | **BUG-011 + BUG-012** | ⬜ IMPLEMENTED → **✅ QA PASS (iteration_7)**. BUG-011: pytest 3/3 PASS — `_augment_run_stats()` $or pipeline, delivered-subsumes-read, fallback-to-stored all verified. QA agent fixed missing `resolve_variable` stub in test file. Live API non-zero verification not possible (no completed campaign runs in test tenants) — unit coverage sufficient. BUG-012: Playwright 3/3 PASS — deep-link `?campaign_id=X` pre-filters correctly; no-params regression clean; `?campaign_id=X&run_id=Y` both banners shown and table filtered. |
| 2026-08-06 (REGISTRY SYNC — Intake Agent) | **BUG-011 / BUG-012 / CR-061** | 🔴/🔵 → **✅ IMPLEMENTED (testing agent pending)**. Code reality confirmed. BUG-011: `_augment_run_stats()` + campaign-level extension in `routers/campaigns.py` (code marker BUG-011, lines 54–182). BUG-012: lazy filter init (E-A1) + mount-effect removal (E-A2) + last-request-wins `fetchSeq` guard (E-A3) in `MessageStatusPage.jsx` (code markers BUG-012 lines 82–187). CR-061: gate REMOVED per owner decision — `_require_crm_templates_enabled()` commented out at 6 authoring endpoints; `crm_templates_enabled` flag now frontend-only visibility control via `GET /api/whatsapp/api-key`. Test files: `test_bug011_run_stats.py` + `test_cr061_gate_removal.py` (testing agent not yet run). |
| 2026-08-06 (REGISTRY SYNC — Intake Agent) | **CR-071 / CR-072 / CR-073 / BUG-020–023** | Status rows updated from self-test PASS → ✅ QA PASS (testing_agent). BUG-020/021/022/023 + CR-073: 26/26 testing_agent PASS (iteration_1, 2026-08-04, `test_bugs_020_023_cr069_073.py`). CR-071 + CR-072: 13/13 testing_agent PASS (iteration_1, 2026-08-04, `test_cr071_cr072.py`). CRM-2 added to cross-reference matrix: self-test PASS, testing agent pending. |
| 2026-08-06 (QA PASS) | **CR-069** | 🟡 Implemented → **✅ QA PASS — Owner smoke pending**. Backend E1/E2/E3 PASS + Frontend V1–V5 all PASS (iter_5 + iter_6). One bug found+fixed: `build_body_values()` in `core/whatsapp.py` was leaking `btn_url_` keys into body output — fixed by testing agent. One gap fixed: Map Variables dialog preview missing button bubbles — added `(mappingTemplate.buttons||[]).map()` block in `TemplatesPage.jsx`. Login smoke PASS on new JWT_SECRET. Test file: `tests/test_cr069_button_mapping.py`. |
| 2026-08-05 (REGISTRY CLOSED — Investigation confirmed) | **CR-036** | 🟢🟡 → **🟢 CLOSED (production code)**. Investigation Agent confirmed B.1+B.2+B.3 fully shipped + QA'd. Code markers: `core/meta_media.py` (B.1), `routers/whatsapp.py` (B.2 lines 34/201/1638/1707, B.3 lines 281/344/376/427/442). B.4 is test-automation only — no production functionality missing. "gate-open pending" status was stale. |
| 2026-08-05 (REGISTRY CLOSED — Investigation confirmed) | **CR-023** | 🟡 → **🟢 CLOSED (CRM side)**. Phase 1+2+3 all in codebase. "Awaiting owner E2E test" was owner-side acceptance action, not a code gap. CR-066 added V11-V23 on top. CRM has zero remaining code work. |
| 2026-08-05 (REGISTRY CLOSED — Investigation confirmed) | **CR-014** | 🟡 → **🟢 CLOSED (CRM side)**. All 3 invoice modes present with graceful fallbacks for missing `room_info`. "Awaiting POS team" is POS-side integration work, not a CRM code gap. |
| 2026-08-05 (QA PASS) | **CR-076** | 🟡 Implemented → **✅ QA PASS — Owner smoke pending**. Backend 11/11 + Frontend 4/4 (iteration_2 + iteration_4). One bug found+fixed during QA: CampaignWizardPage audienceIdSync useEffect React-18 batching race — prefill's audienceCount was overridden by sync effect on initial mount (audienceId still 'all-customers' in closure). Fix: `searchParams.get('audience_stage')` guard added as first statement in audienceIdSync — if URL has audience_stage param and not editing, entire sync effect returns early. V5 now confirmed: `/campaigns/new?audience_stage=churned&audience_count=150` → Select shows 'Churned Customers (150 customers)', Info shows '150 customers will receive this campaign'. Test files: `tests/test_cr076.py`. |
| 2026-08-05 (IMPLEMENTATION COMPLETE) | **CR-076** | 🔵 Planning Complete → **🟡 IMPLEMENTED — QA pending**. 6 edits + 1 backend sub-edit all applied. E-A: lifecycle_stage filter in helpers.py (7 stage branches, reads CR-077 loyalty_settings boundaries). E-B: AudiencesPage Section 0 Lifecycle Stage Collapsible + DEFAULT_FILTERS + chip label + key mapping. E-C: CustomerLifecyclePage — handleReengage fixed (was dead navigate), inline Re-engage modal (template picker + DirectSend), bulk CTA "Re-engage [Stage] (N)" when at_risk/dormant/churned selected. E-D: CustomerDetailPage — auto-open modal on ?action=reengage + Re-engage button. E-E: CampaignWizardPage — pre-fill audience from ?audience_stage= URL param. Backend sub-edit: _resolve_audience_customers handles lifecycle_stage: prefix. Self-test: churned segment=2021, at_risk=4, lifecycle_stage:churned campaign created OK. QA handover: qa/CR_076_QA_HANDOVER.md. |
| 2026-08-05 (BUG FIX + QA PASS) | **CR-077** | 🔵 Planning Complete → **✅ QA PASS 22/22 — Owner smoke pending**. 9 implementation edits (E-A→E-I) were already in `main` branch. QA found one PLAN_GAP: `helpers.py` Block E (`audience_type=high_spender`) missing from implementation plan and not coded. Bug fix applied: added `audience_type == "high_spender"` block to `build_customer_query()` reading `high_spender_threshold` from `loyalty_settings`. Self-test: threshold=5000→11 customers, threshold=50000→2, threshold=100→2118. Testing agent 22/22 PASS. Fix report: `implementation/CRM_CR077_BLOCK_E_BUGFIX_REPORT.md`. Test file: `tests/test_cr077.py`. Open (Phase 2, not in plan): AudiencesPage/CustomersPage spend bucket labels still hardcoded; churn_risk 0.7/0.4 thresholds (F5) not in plan scope. |
| 2026-08-05 (PLANNING COMPLETE) | **CR-077** | 📋 Registered → **🔵 PLANNING COMPLETE — Implementation gate OPEN (pending owner approval)**. Q1–Q5 locked. Impact Analysis complete (9 files, 1 hidden dependency found — F5 absolute_factor uses hardcoded 90.0 maps to dormant_days_end). Implementation Plan: 9 edits (E-A schemas → E-B analytics → E-C campaigns → E-D customer_intelligence → E-E suggestions → E-F loyalty_jobs → E-G scheduler → E-H cron → E-I LoyaltySettingsPage). 11 new LoyaltySettings fields, all defaults = current hardcoded values (zero behavior change on upgrade). VIP auto-promote defaults OFF. 11-point verification matrix. Plan: `planning/CR_077_IMPLEMENTATION_PLAN.md`. |
| 2026-08-05 (PLANNING COMPLETE) | **CR-076** | 📋 Registered → **🔵 PLANNING COMPLETE — Implementation gate OPEN (pending owner approval)**. Q1–Q5 locked. Impact Analysis complete. Implementation Plan: 5 edits + 1 backend sub-edit (E-A helpers lifecycle_stage filter → E-B AudiencesPage Section 0 → E-C CustomerLifecyclePage inline modal + bulk CTA → E-D CustomerDetailPage ?action=reengage → E-E CampaignWizardPage pre-fill + campaigns.py audience resolver). 10-point verification matrix. CR-077 dependency: lifecycle_stage filter auto-reads loyalty_settings (configurable boundaries). Plan: `planning/CR_076_IMPLEMENTATION_PLAN.md`. |
| 2026-08-05 (INTAKE) | **CR-077** | — → **📋 REGISTERED (P2, MEDIUM risk)**. Configurable Lifecycle & Intelligence Thresholds. Source: INV-014 (2026-08-05) + cross-ref INV-003 (VIP phantom). 7 hardcoded areas confirmed: lifecycle stage boundaries (30/60/90 days), campaign daily limit (1000), customer value band thresholds (VIP≥80/High≥60/Medium≥35), VIP never auto-promoted (intelligence score computed but never written to `customers.vip_flag`), churn risk thresholds, spend filter bucket labels, visit/points/wallet buckets. All additive to `loyalty_settings` schema — zero CRITICAL hotspot files. **Q1-Q5 open** (config UI location, per-tenant daily limit, VIP auto-promote/demote policy, spend bucket scope, phase priority). Intake: `discovery/CR_077_CONFIGURABLE_THRESHOLDS_INTAKE.md`. |
| 2026-08-05 (INTAKE) | **CR-076** | — → **📋 REGISTERED (P1, MEDIUM risk)**. Customer Lifecycle Re-Engage — Bulk Campaign + Automation. Source: owner verbal + code investigation. 5 confirmed gaps: Re-engage button dead (`?action=reengage` ignored by CustomerDetailPage), no bulk selection, no lifecycle_stage segment filter, no Lifecycle Stage accordion in AudiencesPage, no Campaign Wizard shortcut. Design prototypes complete. **Q1-Q5 open**. Intake: `discovery/CR_076_LIFECYCLE_REENGAGE_INTAKE.md`. |
| 2026-08-06 (QA PASS) | **BUG-011 + BUG-012 + CR-061 + CRM-2** | 🟡/✅ → **✅ QA PASS (iteration_9, 2026-08-06)**. BUG-011: 3/3 pytest. BUG-012: 3/3 Playwright. CR-061: 13/13 pytest. CRM-2: 400 confirmed. New test file: `tests/test_crm2_doc_upload.py`. |
| 2026-08-05 (BUG FIX) | **CRM-2** | Partial → **✅ FULLY FIXED**. Document upload endpoint `POST /api/pos/customers/{id}/documents` was returning 422 (FastAPI validation) instead of 400 when multipart `file` part was completely absent. Root cause: `file: UploadFile = File(...)` caused FastAPI to raise 422 before function body ran. Fix: changed to `Optional[UploadFile] = File(None)` + explicit null-guard `if file is None: raise HTTPException(400)`. Self-test PASS. Fix report: `implementation/CRM_2_BUG_FIX_REPORT.md`. |
| 2026-08-06 (PLANNING APPROVED) | **CR-067 + CR-068** | 📋 Registered → **🔵 PLANNING APPROVED — all decisions locked**. CR-067 Q1: both + warning modal. Q2: block in-use (already coded). Q3: auto-delete local record during AuthKey sync. Q4: now. CR-068 Q1: frontend-only. Q2: inline panel. Q3: build alongside CR-067. Plans: `planning/CR_067_IMPACT_ANALYSIS.md`, `planning/CR_068_IMPACT_ANALYSIS.md`. |
| 2026-07-17 (INTAKE) | **CR-068** | — → **📋 REGISTERED (P1, LOW-MEDIUM risk)**. "Validate Template" dry-run V1-V23 compliance check — no Meta WABA required. Source: BUG-019 root-cause analysis revealing that V11-V23 validation block is gated behind WABA check, making it unreachable for tenants without Meta WABA configured. Enhancement allows any user to test compliance of template body/header/footer against V1-V23 rules without submitting to Meta. Frontend-only path reuses existing `validateMetaCompliance()` + `getBodyWarnings()` + `getFooterWarnings()`. Optional backend endpoint `POST /api/whatsapp/validate-template-compliance` for server-side mirror. **Q1-Q3 open** (FE-only vs FE+BE, inline panel vs toast, priority vs CR-067). Zero code changed. |
| 2026-07-17 (BUG FIX — QA-discovered) | **BUG-016 / BUG-017 / BUG-018 / BUG-019** | — → **✅ ALL 4 FIXED**. Discovered during QA of CR-066+BUG-015 (20/20 PASS). **BUG-016** (BUG-QA-01): Preview orphan `_` regex — JS negative lookbehind `(?<!\w)` caused issues in certain browser engines; replaced with compatible equivalent pattern in `TemplateBuilderPage.jsx`. **BUG-017** (BUG-QA-02): V16 emoji count regex mismatch — frontend used `/\p{Emoji}/gu` (Unicode property escape, counts ~17 per test) vs backend `\u{1F300}-\u{1FFFF}` range (narrower, undercounted); fixed `routers/whatsapp.py` to match frontend logic. **BUG-018** (BUG-QA-03): Stale inline comment in `routers/whatsapp.py` describing pre-V11 behaviour; comment updated to reflect current logic. **BUG-019** (BUG-QA-04): CRITICAL — `create_meta_template()` V11-V20 safety-net ran AFTER Meta WABA connectivity check (`user.meta_waba_id` + `user.access_token`); tenants with no WABA configured would hit 503 before the validation block and receive no compliance error detail. Fixed: moved V11-V20 validation block BEFORE the WABA check in `routers/whatsapp.py`. Test suite: `tests/qa_cr066_bug015_direct.py` (14/14 pytest) + testing_agent iteration_2 (20/20 PASS). Registered in `BUG_REGISTRY_CAMPAIGNS.md` as BUG-016 → BUG-019. |
| 2026-07-17 (QA PASS) | **CR-066 + BUG-015** | Code complete → **✅ QA PASS 20/20**. Full QA cycle executed against T1-T25 matrix. 4 bugs found (BUG-QA-01→04), fixed, retested. Final result: 14/14 pytest PASS + testing_agent iteration_2 20/20. Files verified: `TemplateBuilderPage.jsx` (validateMetaCompliance V11-V23, getBodyWarnings, getFooterWarnings, preview orphan red highlight, V19/V21/V22 soft-warning path), `routers/whatsapp.py` (V11-V20 safety-net now runs before WABA check, emoji regex corrected). Owner smoke pending. |
| 2026-07-16 (INTAKE) | **CR-067** | — → **📋 REGISTERED (P2, MEDIUM risk)**. WhatsApp Template Deletion + Lifecycle Sync. Source: owner report + INV-011 investigation. **5 gaps found**: (1) CRM delete is local-only — no Meta DELETE API call; (2) AuthKey has no public delete API; (3) status check misses deleted templates (catches 404 → keeps stale "approved"); (4) AuthKey sync never cleans stale local records; (5) no delete UI for AuthKey-sourced templates. Meta DELETE API confirmed: `DELETE /{WABA_ID}/message_templates {"name":"..."}`. AuthKey delete API NOT found in public docs — owner must contact AuthKey. **3 phases**: P1 (~35 min, no deps): fix status-check + sync cleanup. P2 (~75 min): Meta delete endpoint + UI. P3 (blocked): AuthKey delete. **Q1-Q4 open**: cascade behavior, in-use policy, auto-cleanup, priority. Intake: `discovery/CR_067_TEMPLATE_DELETION_LIFECYCLE_INTAKE.md`. Zero code changed. |
| 2026-07-16 (PLANNING) | **CR-066** | 📋 Registered → **🔵 PLANNING COMPLETE — Implementation gate OPEN**. Q1-Q4 ALL LOCKED (2026-07-16): Q1=(a) all tiers together, Q2=(a) hard block, Q3=(a) no retroactive, Q4=(a) hotspot approved. Impact analysis + edit-by-edit implementation plan authored (7 edits E-A through E-G). Code reality FULL — all target sections inspected with line numbers. Files WILL change: `TemplateBuilderPage.jsx` (5 sections: validateMetaCompliance +V11-V23, getBodyWarnings +formatting, getFooterWarnings +V20, preview +orphan highlighting) + `routers/whatsapp.py` (1 section: create_meta_template +V11-V20 backend safety-net). Files WILL NOT change: core/whatsapp.py, core/campaign_jobs.py, routers/campaigns.py, send path, webhook path. 25-point verification matrix (T1-T25). ~145 LOC additions. Zero code changed. Plan: `planning/CR_066_IMPACT_ANALYSIS_AND_IMPL_PLAN.md`. **Next**: Owner says "start" → Implementation Agent executes E-A through E-G. |
| 2026-07-16 (IMPLEMENTATION) | **CR-066** | 🔵 gate open → **✅ CODE COMPLETE + self-test PASS**. All 7 edits (E-A through E-G) applied per plan. **Frontend**: `validateMetaCompliance()` extended with V11-V23 (13 new checks), `getBodyWarnings()` extended with 4 real-time formatting warnings, `getFooterWarnings()` extended with V20 formatting check, WhatsApp preview gains orphan `_` red highlighting. **Backend**: `create_meta_template()` gains V11-V20 safety-net (formatting markers, var position, adjacent vars, formatting wrapping, body limit, emoji count, header/footer formatting). **Self-test results**: T1 orphan `_` BLOCKED, T5 bullets+bold PASSED, T6 var-at-start BLOCKED, T8 adjacent vars BLOCKED, T9 formatting wrapping var BLOCKED, T16 footer formatting BLOCKED, T20 valid body PASSED through to Meta (created `test_valid` + `test_bullets` on Meta). Frontend screenshot confirmed: real-time warning renders below textarea ("Unmatched _"), preview highlights orphan `_` in red. Both services compiled successfully, zero errors. Files changed: `TemplateBuilderPage.jsx` (~100 LOC added), `routers/whatsapp.py` (~45 LOC added in create_meta_template validation block only). **Next**: owner smoke or QA. |
| 2026-07-16 (BUG-015 REGISTERED) | **BUG-015 / CR-066** | Owner smoke revealed V19/V21/V22 false positives → **🔴 BUG-015 OPEN**. 6/10 approved templates violate V19 (>550 chars) + V21 (category mismatch) + V22 (ALL CAPS) simultaneously — Meta approves all. Q2 revised: hard block applies to V11-V18/V20/V23 only; V19/V21/V22 downgraded to soft warnings (yellow toast, non-blocking). Fix plan: `planning/BUG_015_V19_V21_V22_SOFT_WARNING_FIX.md` — 5 edits, ~15 LOC, `TemplateBuilderPage.jsx` only. Backend unchanged. **Next**: Bug Fix Agent applies edits. |
| 2026-07-14 (IMPLEMENTATION Steps 2+3) | **CR-063 / CR-065** | 🔵 gate open → **✅ CODE COMPLETE + QA PASS (iteration_19: 100% backend + 100% frontend) · owner smoke pending**. CR-063: Switch import + editData init + toggle below Phone + Q-E=(b) status badge on header (`CustomerDetailPage.jsx`, marker CR-063). One implementation hiccup: Switch import initially didn't land → runtime error caught by smoke screenshot → re-applied, verified. CR-065 (D6=a): desktop+mobile "Resent <time>" + ×N badge (`MessageStatusPage.jsx`) · Q-F=(a) export gains Resend Count/Last Resend At (`whatsapp.py _EXPORT_HEADERS`). testing_agent seeded+cleaned synthetic resent log, restored toggled customer, NO resend clicks/live sends. Legacy scan: 0 resent logs missing last_resend_at. Suite: `tests/test_cr063_cr065_iteration19.py`, report `/app/test_reports/iteration_19.json`. ⏸ NEXT: owner smoke on all 4 shipped items. |
| 2026-07-14 (IMPLEMENTATION Step 1) | **BUG-013 / BUG-014** | 🔵 gate open → **✅ CODE COMPLETE + QA PASS · owner smoke pending**. Edits per plan E-A1…E-A6: pymongo `UpdateOne/InsertOne` import · opt-in parsing in `_validate_and_classify_row` (both headers, D1 token matrix) · `_find_duplicate_phones` helper · duplicate-400 at preview+commit (Q-A=c) · classify-all-first loop + single `bulk_write(ordered=False)` · new-import default True (D2) · template 8th column (Q-C=a). Measured 300-row import **2.18 s** (was 83-167 s). Tests: main-agent pytest 21/21 (`tests/test_bug013_014_import.py`) + independent testing_agent iteration_18 **12/12 PASS backend 100%** (report `/app/test_reports/iteration_18.json`). Synthetic data fully cleaned (0 residuals). QA handover: `qa/BATCH_2026_07_14_STEP1_QA_HANDOVER.md` (incl. 3 non-blocking hardening notes). ⏸ NEXT: owner smoke → Step 2 CR-063 → Step 3 CR-065. |
| 2026-07-14 (Q-C lock — planning fully closed) | **BUG-014 (Q-C)** | Q-C OPEN → **✅ (a) LOCKED** — sample import template gains `whatsapp_opt_in` 8th column (owner's "already has all columns" premise corrected by re-validation: template had 7 cols). ALL planning decisions for BATCH-2026-07-14 now closed (D1/D2/D4/D5/D6 + Q-A…Q-F). Impl plan final: E-A6 in scope, E-B4 out, E-C3 in, E-D3 in. ⛔ Implementation gate still WITHHELD — awaiting owner "start". Zero code changed. |
| 2026-07-14 (PLANNING) | **BUG-013 / BUG-014 / CR-063 / CR-065** | 📋 Registered → **PLANNING complete (gate WITHHELD)**. Impact analysis `planning/BATCH_2026_07_14_IMPACT.md` + impl plan `planning/BATCH_2026_07_14_IMPL_PLAN.md` (edit-by-edit E-A1…E-D3, V1-V12 matrix). Code reality FULL for all 4 (INV-007). Latent bug surfaced: in-file duplicate NEW phones create duplicate customers → owner locked Q-A=(c) reject file w/ 400 + duplicate row list. Other locks: Q-B=(a) both opt-in headers · Q-D=(b) silent apply, no preview column · Q-E=(b) detail-page toggle + status badge · Q-F=(a) report download gains Resend Count/Last Resend At. **Q-C OPEN** — owner premise "template already has all columns" re-validated FALSE (template = 7 columns, no opt-in; export = 22 cols). Files WILL change: routers/customers.py (import section) · CustomerDetailPage.jsx · MessageStatusPage.jsx · whatsapp.py `_EXPORT_HEADERS` (Q-F) · new pytest suite. ⛔ Owner: "no execution till I approve all decisions" — zero code changed. |
| 2026-07-14 (Owner decision locks) | **BUG-014 / CR-064 / CR-065 / (D4)** | 📋 Registered → **decisions locked** (`DECISIONS_LOG.md § 2026-07-14`): **D1** import honours "WhatsApp Opt-in" Yes/No for existing customers, BLANK = leave-unchanged for ANY field · **D2** NEW imported customers default opt-in **True** (file value wins; replaces hardcoded False L1456) · **D5 = (c)** CR-064 Customer Delete **⏸ PARKED** · **D6 = (a)** CR-065 row shows "Resent <time>" + ×N badge (owner: "user experience is priority") · **D4 skipped** (no export-sort CR). Ready for PLANNING: BUG-013, BUG-014, CR-063, CR-065. Priority order NOT yet confirmed by owner (agent suggestion BUG-013 → BUG-014 → CR-063 → CR-065 stands unapproved). Zero code changed. |
| 2026-07-14 (BATCH INTAKE from INV-007) | **BUG-013 / BUG-014 / CR-063 / CR-064 / CR-065** | — → **📋 5 items registered** (INTAKE role, zero code; all root causes pre-confirmed by INV-007 investigation same day). (1) **BUG-013** P1/MEDIUM import Cloudflare 5xx — 345 sequential `update_one` × ~242 ms remote-Mongo RTT ≈ 83-167 s > proxy timeout; import silently completes → owner retries caused duplicate runs; fix = `bulk_write`. (2) **BUG-014** P1/HIGH import discards "WhatsApp Opt-in" column (only 7 of 22 exported columns consumed) + NEW imports hardcoded `whatsapp_opt_in=False` — blocked on owner D1/D2. (3) **CR-063** P2/LOW opt-in toggle missing on Customer DETAIL edit modal (LIST modal verified working E2E). (4) **CR-064** P2/**CRITICAL-risk** Customer Delete UI missing feature (backend delete exists, cascades only points_transactions) — blocked on owner D5 hard-vs-soft-delete policy. (5) **CR-065** P2/LOW show `last_resend_at`/"Resent ×N" on Message Status rows (data already stored, never displayed). **Closed NOT-A-BUG at intake**: "tags missing in Excel" (rows 241-349, insertion order) and "opt-in missing in Excel" (4 No rows at 326-349, matches DB). **Drift note**: code marker `BUG-011` in CustomersPage (opt-in toggle) collides with registry BUG-011 (campaign counters). Docs: `discovery/SESSION_2026_07_14_BATCH_INTAKE.md`, `investigations/INV_007_OPTIN_EDIT_TAGS_EXPORT_IMPORT_502_2026_07_14.md`. Next: owner answers D1/D2/D5 + prioritizes → PLANNING. |
| 2026-07-12 (Planning CLOSED — gate open) | **CR-060 / CR-061 / BUG-011 / CR-062 / BUG-012** | PLANNING → **🔵 IMPLEMENTATION GATE OPEN (all 5)**. All owner Qs locked (`DECISIONS_LOG.md § 2026-07-12`): CR-060 errors-TAB + max-w-3xl + CSV download (mockup approved) · CR-061 restaurant_id env allowlist (`CRM_TEMPLATES_ALLOWED_RESTAURANT_IDS`, 2-3 tenants), UI-hide + SILENT backend 403, authoring-only gate, wizard untouched · BUG-011 read-time aggregation in campaigns.py runs endpoints, NO backfill, webhook untouched · CR-062 B/I/S/mono toolbar + rendered preview, body-only (mockup approved) · BUG-012 lazy filter init + last-request-wins. Impl plan (edit-by-edit E-A1…E-E5 + V1-V10 matrix): `planning/BATCH_2026_07_12_IMPL_PLAN.md`. Mockups: `planning/BATCH_2026_07_12_MOCKUPS.html`. Handover: `HANDOVER_2026-07-12_BATCH_PLANNING.md`. Zero code changed. Order: BUG-012 → BUG-011 → CR-060 → CR-062 → CR-061. |
| 2026-07-12 (Impact Analysis) | **CR-060 / CR-061 / BUG-011 / CR-062 / BUG-012** | INTAKE → **PLANNING (Impact Analysis complete — impl plan blocked on owner Q1-Q9)**. Doc: `planning/BATCH_2026_07_12_IMPACT.md`. Findings: **CR-060** LOW, frontend-only (~50 LOC CustomersPage) — backend `all_errors` already shipped/unused. **CR-061** MEDIUM, hotspot `routers/whatsapp.py` gate needed; nuance: approved CRM templates surface as AuthKey templates in wizard → recommend gating authoring only. **BUG-011** root cause CONFIRMED: `campaign_runs.total_delivered/total_read` never updated anywhere; option (b) read-time aggregation recommended (retroactive, avoids webhook hotspot). **CR-062** answered: no Meta/CRM restriction, markers work today; toolbar is ~70 LOC frontend. **BUG-012** root cause CONFIRMED by trace: mount double-fetch race in `MessageStatusPage.jsx` (:113-125 vs :188-191); fix = lazy filter init + last-request-wins (~25 LOC). Zero code changed. Next: owner answers Q1-Q9 → Implementation Plans. |
| 2026-07-12 (BATCH INTAKE) | **CR-060 / CR-061 / CR-062 / BUG-011 / BUG-012** | — → **📋 5 items registered from owner report** (INTAKE role, "intake only", zero code). (1) **CR-060** import modal bigger + clickable error rows in preview (backend `all_errors` already shipped, frontend unused) + 60-B modal-consistency sweep split/parked. (2) **CR-061** CRM templates gated per-restaurant via env allowlist (hotspot `routers/whatsapp.py` — approval needed). (3) **BUG-011** Campaign History Delivered/Read always 0 — `campaign_runs.total_delivered/total_read` initialized but never incremented anywhere (grep-verified); fix options webhook `$inc` vs read-time aggregation vs both+backfill. (4) **CR-062** formatting toolbar; owner Q answered: NO Meta restriction — `*bold*`/`_italic_`/`~strike~` markers work in body today, builder lacks toolbar. (5) **BUG-012** View Messages deep-link shows all messages despite selected filter — hypothesis: mount double-fetch race in `MessageStatusPage.jsx` (unfiltered first fetch resolves last, overwrites filtered results). Owner screenshot for item 1 NOT received on pod. Intake doc: `discovery/SESSION_2026_07_12_BATCH_INTAKE.md`. Both bugs in `BUG_REGISTRY_CAMPAIGNS.md`. Next: owner prioritizes + answers Qs → PLANNING. |
| 2026-07-11 (CR-036 Batch B.3 SHIPPED) | **CR-036** | IMPLEMENTATION → QA PASS. Owner locked **Q20=(a)** include Resend for media_missing rows, **Q21=(a)** 4 MB chunks. All 14 edits (E-B3-1…14) shipped per `planning/CR_036_BATCH_B3_IMPACT_AND_IMPL_PLAN_2026_07_11.md`: `_process_media_upload()` extracted, chunked upload endpoints (`/init`, `/chunk/{id}`, `/complete/{id}`, /tmp staging + 2h TTL sweep), progress bar in `MediaHeaderUpload` (single-shot `onUploadProgress` + chunked pct), inline re-upload Dialog on TemplatesPage (Q16 media-only PUT, no navigation), resend eligibility + media re-attach + `status_note` clear + `media_still_missing`/`not_resendable` skips, Message Status `isResendable` incl. failed(media_missing). **QA**: iteration_8 backend 9/9 PASS (incl. real Meta+S3 single-shot & chunked happy paths, resend skip paths, grace-window regression) · iteration_9 frontend 3/3 PASS after fixing missing `progress` useState. Test file: `backend/tests/test_cr036_b3_media_chunked_resend.py`. No live sends, no Meta templates created, all synthetic rows cleaned. Remaining in CR-036: B.4 test automation (Q22/Q23 open) + owner live E2E of B.1 with `sampletestlogo`. |
| 2026-07-11 (BUG-010 fixed) | **BUG-010** | INTAKE → FIXED. Owner-reported: approved media template `sampletestlogo` blocked in Campaign Wizard ("no uploaded file"). Root cause: `create_meta_template` dropped `send_media_url`/`header_handle` on local save (direct Meta-submit path only). Fix: persist media fields (marker BUG-010) + backfilled 2 affected docs from original S3 uploads. QA: iteration_7, 6/6 pytest + UI verified. Registered in `BUG_REGISTRY_CAMPAIGNS.md`. |
| 2026-07-11 (CR-036 Batch B.1 gate-open) | **CR-036** | INVESTIGATION → PLANNING (Impact Analysis) → gate-open. **Q14 REVERT** (2026-07-11): owner reasoning "since we integrate with AuthKey there'll be a single Meta ID" → env-first + optional per-tenant override. `META_APP_ID=874516431301713` set in `backend/.env`. **Live-probe validation**: `POST /v21.0/874516431301713/uploads` returned 200 for 3/3 probed tenants — `owner@jehsnest.com` (AuthkeyK), `owner@kunafamahal.com` (AuthkeyK), `owner@mayur.com` (AuthkeyP outlier). Same-Meta-App confirmed across both System Users; 2026-07-04 "2 System Users = 2 apps" interpretation corrected. **Impact Analysis** authored `planning/CR_036_BATCH_B1_IMPACT_ANALYSIS_2026_07_11.md` — 4 origin × 4 send scenarios (Path A CRM-built ✅ · Path B direct-Meta ⚠️ Q15 · Path C AuthKey-import ⚠️ Q15 · Path D AuthKey-only ⛔ out-of-scope · Path E legacy ⚠️ G5) · 13 gaps registered · 26 verifications (V15-V26 new). **Owner-locks Q15-Q19** (2026-07-11): Q15-(a)+(c) accept fail-loud + inline Re-upload button on TemplatesPage · Q16-(a) block media-edit on approved with 400 · Q17-(a) test-send auto-injects stored `send_media_url` · Q18-(a) dynamic `{{1}}` OUT of scope · Q19 APPROVED `core/whatsapp.py::send_event_message` +5 LOC additive hotspot. **Test tenant**: `owner@jehsnest.com / Qplazm@10` (Jeh's Nest) added to `test_credentials.md`. All Batch B.1 blockers eliminated. Effort: ~13 hrs single-session shippable. Awaits owner "start B.1" to open Implementation gate. |
| 2026-07-06 (Session 11 — Audit → Registry conversion) | **CR-046 → CR-059** | INTAKE role (batch). — → **📋 14 CRs registered** from `ARCHITECTURE_AUDIT.md` v1.0 (45 findings: 14 High / 21 Medium / 10 Low, all evidence-based with file/line refs). Grouping = one CR per remediation workstream, mapped to the audit's 4-phase roadmap; every finding ID traceable via the CR title. Cross-links: CR-047 absorbs CR-041-F3 (HMAC) · CR-055 absorbs CR-041-F2 (compound unique index) · CR-050 supersedes CR-038 options A-C (CR-038 stays open only for Q1-Q4 SLA answers) · MAI-01 hotspot splits already tracked as CR-041-F1 (no new row). Dependencies declared: CR-050←CR-049, CR-055←CR-046 (backups before migrations). Risk gates flagged: CR-048/053/056 touch auth (owner approval + full gate mandatory per addendum §14/Part C). Zero code changes this session. Intake doc: `discovery/CR_046_059_AUDIT_REMEDIATION_BATCH_INTAKE.md`. Audit: `/app/memory/ARCHITECTURE_AUDIT.md` + rendered `/docs/audit.html`. |
| 2026-07-06 (Sprint closure QA — Session 10) | **CR-033 / CR-034 / CR-037 / BUG-009 registry** | CLOSURE role. Preprod DB reconnected (`mongodb://52.66.232.149:27017/mygenie`, 29 collections, 5,971 customers, 24 campaigns, 28 users). MyGenie SSO end-to-end verified (owner@cafe103.com → HTTP 200 + real POS config + mygenie_token). `testing_agent_v3_fork` iteration_5 dispatched for **Option A (CR-033/034/037)** with **Option B accepted for CR-026/029** (cosmetic/navigation, no QA required). Result: **14/14 backend pytest PASS + UI smoke PASS, zero issues found**. CR-033 BUG-A regression (T02/T03/T04) verified against live tenant data — `vip_flag`, `whatsapp_opt_in`, `has_birthday_this_month` all return proper subsets (not full 5,971). CR-034 tag CRUD verified via real API + UI smoke ('+ tag' button and chips render on every customer row). CR-037 direct-write regression on real `custom_templates` doc (kunafamahal tenant) confirmed the `if current_status != "rejected"` guard preserves rejected status while still back-filling `authkey_wid`; doc reverted in `finally`. Test file: `/app/backend/tests/test_cr033_034_037_sprint_closure.py`. Report: `/app/test_reports/iteration_5.json`. **Registry drift corrected**: (a) CR-033/034/037 flipped from 🟢 IMPLEMENTED → ✅ QA PASS, (b) BUG-009 in `BUG_REGISTRY_CAMPAIGNS.md` flipped from 🔴 OPEN → ✅ FIXED, (c) duplicate BUG-009 stub row removed from dashboard, (d) `test_credentials.md` refreshed with owner@cafe103.com + owner@kunafamahal.com. Non-blocking rec (unchanged from earlier iterations): `routers/customers.py` now >2,200 LOC — future refactor into `customer_tags.py` / `customer_segments.py` / `customer_import_export.py`. |
| 2026-07-04 (CR-036 Batch B.0.1 shipped — Meta APP_ID Settings field) | **CR-036** | Q14 REVISED per owner "each client will have meta id so we need to put in settings". Design flipped from `shared env var + optional override` → **per-tenant Settings field, no env fallback**. Backend `routers/whatsapp.py` `GET/PUT /api/whatsapp/api-key` gained `meta_app_id` in read + write payloads (~5 LOC each). Frontend `SettingsPage.jsx` gained new input `data-testid=meta-app-id-input` under WhatsApp Configuration card next to WABA ID + Access Token (~15 LOC). `META_APP_ID=""` env var REMOVED from `backend/.env`. DECISIONS_LOG Q14 lock rewritten with revised design + 2-System-User finding (AuthkeyK vs AuthkeyP) rationale. **Verified end-to-end**: curl `PUT meta_app_id="999888777666555"` → 200 → `GET` shows saved value → cleanup PUT="" → 200. Playwright: `[data-testid=meta-app-id-input]` present on Settings page after login. Batch B.1 now reads `user.meta_app_id`; if empty → 503 "Meta App ID not configured — enter it in Settings" per tenant. Owner action item (per tenant): 6 tenants to enter their Meta APP_ID via Settings. |
| 2026-07-04 (CR-036 Batch B.0 prep complete) | **CR-036** | PREP DONE. 3 tasks executed in parallel: **(1) Meta `/uploads` playbook** via `integration_playbook_expert_v2` — confirmed 2-step resumable upload flow (`POST /{APP_ID}/uploads` init → `POST /{upload_session_id}` data → returns `"h": "4::..."`), auth via same long-lived Meta token in `Authorization` header, size limits per format (image 5MB, video 16MB, document 100MB), handle expiry ≈30 days with no dedicated error code (parse message text). **(2) AuthKey media routing** verified via grep in `core/whatsapp.py` — single endpoint, `type="media"` flag, `headerValues` block already built. `send_bulk_messages` already threads `media_url`+`media_filename` — NO changes needed to `core/whatsapp.py`; Batch B only touches `campaigns.py` to POPULATE the field. **(3) `status_note` field** CONFIRMED ABSENT (grep + live Mongo doc inspection — 39 keys, `status_note` not present). G6 additive schema change confirmed needed. **CRITICAL CONFLICT surfaced**: playbook proved Meta does NOT support AUDIO as header format → contradicts Q1 (2026-07-03) which locked "all 4 media types". Owner resolved: **Q13 (a)** drop audio from header support, keep image/video/document only. **Meta APP_ID discovery**: verified 8 API probes — cannot programmatically fetch app_id from tenant tokens (they lack `business_management` scope; all use AuthKey's Meta app under the hood — `GET /me` reveals `"AuthkeyK System User"`). Owner resolved: **Q14** = shared env var `META_APP_ID` (single AuthKey app across all AuthKey tenants) + optional per-tenant `meta_app_id` override for future direct-Meta tenants. `META_APP_ID=""` placeholder added to `backend/.env`. **NEW BLOCKER**: owner must obtain AuthKey's Meta APP_ID from AuthKey support/dashboard before Batch B.1 code work begins. All decisions locked in `DECISIONS_LOG.md § 2026-07-04 [CR-036] §q13 + §q14`. |
| 2026-07-04 (CR-036 Planning RCA + Batch A.1 patch) | **CR-036** | PLANNING (retro) → IMPLEMENTATION (hotfix). Owner asked "recheck how it's missed in planning" — I wrote `planning/CR_036_PLANNING_RCA_2026_07_04.md` with 6 root causes (symptom-scoped discovery, wrong Q's, missed hotspot registry check, integration surface under-budgeted, frontend UX under-multipled, no legacy-fleet Q at discovery), 12 gaps identified in Batch B plan (G1-G12), and self-audit found a **real Batch A gap**: WeasyPrint HTTPS S3 `base_url` would 404 on legacy tenants' `/api/auth/profile/logo/xxx` URLs → PDFs render without logo. **Owner chose option C** (ship A.1 only, pause Batch B). **G-locks 2026-07-04**: G2 (auto-re-upload once on Meta handle 30-day expiry) · G5 (legacy media templates fail-loud with `status_note=media_missing`, NOT text-only or old-URL) · G6 (schema-additive `status_note` field on `whatsapp_message_logs`) · G10 (duplicate media per tenant on clone via `s3.copy_object`). **Batch A.1 shipped**: `_resolve_logo_url()` helper in `services/invoice_generator.py` (10 LOC) + new env var `PUBLIC_BACKEND_URL` in `backend/.env` (populated with preview URL). Absolute URLs pass through unchanged; legacy `/api/...` paths get prefixed with `PUBLIC_BACKEND_URL`. **Verification**: 4/4 unit tests pass (empty · S3 HTTPS passthrough · legacy resolve · http passthrough) + HTML dual-write E2E confirmed + backend healthy. **Batch B awaits owner-go**: `Batch B.0` prep (Meta `/uploads` playbook call + confirm AuthKey media routing + confirm `status_note` field absence), then `B.1-B.4` execution (~12-14 hrs). |
| 2026-07-04 (CR-036 Batch A shipped) | **CR-036** | IMPLEMENTATION → verified end-to-end. Owner supplied real AWS creds; playbook fetched from `integration_playbook_expert_v2`. **Batch A** (safer, dual-mode, non-regressive) shipped: (a) `/app/backend/core/s3.py` NEW ~130 LOC — S3_CONFIGURED flag, lazy client, put_public_object / object_exists / get_object_bytes / delete_object / get_public_url (virtual-hosted URL style, playbook §1.1). (b) `routers/auth.py` Part 3 — bill logo upload switches to S3 (`bill-logos/{user_id}.{ext}`), falls back to local disk if S3 unreachable, legacy serve endpoint kept for Q9 dual-mode. (c) `services/invoice_generator.py` Part 4 — new helper `_write_invoice_html()` dual-writes S3+local at all 3 sites (food + hotel_room + hotel_folio), `generate_invoice_pdf` uploads PDF to S3, HTTPS S3 `base_url` for WeasyPrint per Q11. (d) `routers/invoices.py` Part 4 — HTML serve reads S3-first-then-local (preserves runtime PDF-URL injection), PDF serve 302-redirects to S3 URL when present. **Smoke test PASS** 2026-07-04 11:56 UTC: `POST /auth/profile/logo` returned `https://mygenie-prod.s3.ap-south-1.amazonaws.com/bill-logos/pos_0001_restaurant_635.png`, public GET returned 200 with correct bytes, Mongo persisted new URL. Invoice generator `_write_invoice_html()` verified — both S3 and local disk written, `get_object_bytes()` returned correct content, cleanup succeeded. |
| 2026-07-04 (INV-006 + CR-036 scope amendment) | **CR-036** | INVESTIGATION → PLANNING (amended). Owner asked for placeholder AWS env vars + full audit of every local-file-upload surface before starting CR-036. INVESTIGATION doc `discovery/INV_006_FILE_UPLOAD_SURFACE_AUDIT.md` enumerated 5 surfaces: (A) bill logo upload — local disk, (B) customer CSV import — in-memory only, (C+D) invoice HTML+PDF — local disk / on-the-fly, (E) WhatsApp media header — broken (CR-036). Owner chose to **ship all 3 disk-backed surfaces as ONE CR** (not split into CR-046/CR-047). CR-036 scope amended: original Parts 1+2 (media approval + delivery) + NEW **Part 3 bill logo → S3 (dual-mode, no backfill)** + NEW **Part 4 invoices → S3 (no backfill, HTTPS WeasyPrint `base_url`)**. New Q-locks: **Q9** dual-mode logos, **Q10** no invoice backfill, **Q11** HTTPS `base_url` approved, **Q12** hotspot approved for `routers/auth.py` + `services/invoice_generator.py` + `routers/invoices.py`. Effort revised ~10-12 hr → **~16-20 hr**. Safety property: `S3_CONFIGURED=False` (placeholder creds) → all 4 parts fall back to local disk → **PR is a no-op** without AWS creds. Amendment: `planning/CR_036_SCOPE_AMENDMENT_2026_07_04.md`. Placeholder creds now in `/app/backend/.env`. Blocker: AWS creds ("later" per owner). |
| 2026-07-04 (QA PASS + Incident+Recovery) | **CR-035** | ✅ **QA PASS via `testing_agent_v3_fork` iteration_4** (owner-invoked). 19/19 backend pytest PASS (new file `/app/backend/tests/test_cr035_customer_export_import.py`) + all frontend data-testids validated + 3-step import modal flow works + verification matrix V1-V25 all PASS. Non-blocking observations: file-size check post-read (10MB cap holds), Import Dialog missing `aria-describedby`, `customers.py` >1500 LOC (refactor rec). **QA CLEANUP INCIDENT**: ad-hoc final DB sweep used broad regex `^8888` which matched 2 real POS-synced tenant customers (`shaharukh` 8888726667, `Vikram Davare police` 8888212250) — both hard-deleted. **RECOVERY**: main agent triggered `POST /api/customers/sync-from-mygenie` for owner@jehsnest.com. Sync completed 2026-07-04 10:02 UTC: synced=52 new (includes the 2 lost rows), updated=233, failed=0. Both customers confirmed restored (curl-verified, tier=Bronze default from POS). Local-only CRM annotations lost, POS-side profile+loyalty+wallet intact. **RCA lock**: cleanup queries MUST AND together phone-prefix + name-prefix (`QATest_*` / `UITest_*`) + `user_id`, never OR. Full RCA in iteration_4.json §rca_of_the_qa_incident. |
| 2026-07-04 (doc hygiene) | **CR-040 / CR-035** | Registry hygiene pass — no code. (1) **CR-040** formally registered as `📋 Registered` on the CR Board (AuthKey duplicate-LogID upstream escalation, owner-side action, 0 CRM dev hours). Intake doc `discovery/CR_040_AUTHKEY_DUPLICATE_LOGID_ESCALATION_INTAKE.md` written last session; row now visible on dashboard for tracking. (2) **CR-035 dashboard drift corrected**: row previously said `🔵 Discovery complete — Impact Analysis next` but backend already ships `GET /customers/export` + `POST /customers/import-preview` + `POST /customers/import` + `GET /customers/import-history` + `GET /customers/sample-import-template` (verified via `grep @router` on `routers/customers.py`). Row flipped to `✅ IMPLEMENTED — dashboard drift corrected 2026-07-04`. Session-close handover: `crm_roi_sprint/handoff/SESSION_2026_07_04_HANDOVER.md`. |
| 2026-07-03 (QA PASS) | **CR-042 / BUG-009 / CR-043** | Testing agent iteration_3 → all fixes verified · **CR-042 · BUG-009 · CR-043 all flipped to ✅ QA PASS · Owner UAT ready**. Backend pytest 38/38 PASS (27 new + 11 CR-039 regression). Frontend Playwright verified: BUG-009 end-to-end restored after C1 hotfix (route `/messages` → `/message-status`), M1 per-row dropdown dedup (13 unique wrappers), L1 duplicate id fixed (embedded renamed), L2 chip label spacing (`ml-1` + `gap-1`), L3 leaked TESTTAG cleaned from DB. Zero regressions. One residual action item: pytest teardown fixture to prevent TESTTAG_ leak (cosmetic; deferred as future micro-CR). Report: `/app/test_reports/iteration_3.json`. |
| 2026-07-03 (Implementation) | **CR-043 A + B** | ✅ **IMPLEMENTED and self-tested**. Backend: `tags`/`tags_mode` params on `list_customers` + `with_counts=true` variant on `/customers/tags` (aggregation) + composite index `idx_customers_user_tags {user_id:1, tags:1}` on startup. Frontend: FILTER-BY-TAG chip strip with real counts (Lunch:5, Dinner:5, Breakfast:2, Churn:0 verified on Jeh's Nest) + ANY/ALL radio + Clear + active-tag chips + More/Less toggle. Popover reworked to multi-select autosave with checkbox per tag, count badges, inline Create-new, Done dismiss. Curl verified: any/all modes work; empty tags= is no-op regression; backward-compat GET /customers/tags unchanged. Screenshot confirmed chip strip renders. pytest 11/11 PASS. Bulk-apply parked as CR-045 (unchanged). Files touched: `backend/routers/customers.py` (+~50 LOC), `backend/server.py` (+~7 LOC), `frontend/src/pages/CustomersPage.jsx` (+~215 LOC net). QA handover: `crm_roi_sprint/qa/CR_043_QA_HANDOVER.md`. |
| 2026-07-03 (Implementation) | **BUG-009 + CR-042** | ✅ **IMPLEMENTED and self-tested**. Role 3 (Implementation Agent) completed edit-by-edit per plan. Backend: new shared `_build_message_log_query()` helper + `run_id` filter on `/message-logs` + new `GET /message-logs/export?format=csv|xlsx` endpoint with 5000-row cap. Frontend: Details button now wired to deep-link + emerald "🎯 Filtered to run" banner on MessageStatusPage + Export dropdown on MessageStatusPage (filter-aware) + per-row Export dropdown on CampaignHistoryPage (run-scoped). All curl verifications B1-B9 PASS; pytest regression 11/11 PASS; frontend compiled with only pre-existing eslint warnings; both services healthy. Files touched: `backend/routers/whatsapp.py` (+~230 LOC net), `frontend/src/pages/MessageStatusPage.jsx` (+~90 LOC), `frontend/src/pages/CampaignHistoryPage.jsx` (+~90 LOC). Docs: `crm_roi_sprint/planning/BUG_009_CR_042_MESSAGE_EXPORT_AND_DEEP_LINK_IMPL_PLAN.md`, `crm_roi_sprint/qa/BUG_009_CR_042_QA_HANDOVER.md`. |
| 2026-07-03 (Impl Plans) | **CR-042 / BUG-009 / CR-043 / CR-036** | PLANNING role · Implementation Plans authored for all 4 owner-locked items (3 docs, since BUG-009+CR-042 are bundled): `planning/BUG_009_CR_042_MESSAGE_EXPORT_AND_DEEP_LINK_IMPL_PLAN.md` · `planning/CR_043_CUSTOMER_TAG_FILTER_AND_POPOVER_IMPL_PLAN.md` · `planning/CR_036_MEDIA_TEMPLATE_APPROVAL_AND_DELIVERY_IMPL_PLAN.md`. Each plan is edit-by-edit precise for Role 3 (Implementation) — includes exact code blocks, file locations, verification matrices (backend curl + browser + regression), migration steps, rollout / rollback, and remaining owner asks. CR-036 formally raises hotspot flag on `routers/whatsapp.py` + `routers/campaigns.py` (both approved 2026-07-03 "q8 approved"). Owner needs to supply AWS S3 credentials before CR-036 Implementation can start. Zero code changes made this pass. All 4 items now Gate-ready. |
| 2026-07-03 (owner-lock) | **CR-042 / BUG-009 / CR-043-A / CR-043-B / CR-036 / CR-045** | All owner decisions from batch intake through INV-005 planning are locked. **CR-042** frozen ("1 freeze") · **BUG-009** approved with 🎯 banner ("2 ok") · **CR-043-A** = Option C hybrid chip strip + panel ("1 C") · **CR-043-B** = Option C polished multi-select autosave ("2 ok") · **CR-036** Q1-Q8 answered: image+video+doc+audio, Meta caps, Meta pass-through handle, block-early UX, **Amazon S3 for delivery** (Q6), silent-degrade + log (Q7), **hotspot APPROVED** for `whatsapp.py` + `campaigns.py` (Q8) · **CR-045** (bulk-actions-on-customers) registered as `⏸ PARKED — awaits owner promotion` (split off from CR-043 bulk-apply scope). All 4 active items ready for Implementation Plan (still in PLANNING role). Full trail: `DECISIONS_LOG.md § 2026-07-03`. |
| 2026-07-03 (INV-005) | **CR-036** | Owner challenged Planning's "Scenario 1 no impact" claim — INVESTIGATION role verified defect. **Approved templates with media headers DO fail at send-time** because `routers/campaigns.py` never passes `media_url` to `WhatsAppMessage()` in any of 3 send paths. AuthKey requires `headerValues.headerData` per-send. CR-036 re-scoped from ~5.5h/MEDIUM to ~10-12h/MEDIUM–HIGH. New scope splits: **Part 1** template approval (original) + **Part 2** send-time media delivery (object storage + campaign path fix + legacy banner). New Q6-Q8 surfaced (object storage choice, fallback for missing media, hotspot approval for `routers/campaigns.py`). Report: `discovery/INV_005_CAMPAIGN_MEDIA_SEND_GAP.md`. Correction note in `crm_roi_sprint/planning/BATCH_2026_07_03_IMPACT.md`. |
| 2026-07-03 (Planning) | **CR-042 / BUG-009 / CR-043 / CR-036** | PLANNING role: **Impact Analysis complete** for all 4 items. Doc: `crm_roi_sprint/planning/BATCH_2026_07_03_IMPACT.md`. Findings: **CR-042** LOW risk, ~130+40+35 LOC across whatsapp.py + 2 pages, coordinates with BUG-009 on `run_id` filter. **BUG-009** LOW risk, ~23 LOC across 3 files, defect fully confirmed (dead onClick on CampaignHistoryPage:164-166), option (a) deep-link recommended, 80% plumbing already present via CR-026. **CR-043 Part A (filter)** LOW risk, ~55 LOC across customers.py + CustomersPage.jsx, backend `build_customer_query` already supports tags. **CR-043 Part B (popover rework)** BLOCKED — owner UX spec required. **CR-036** MEDIUM risk (touches HIGH-risk `routers/whatsapp.py`), ~230 LOC across whatsapp.py + TemplateBuilderPage.jsx, defaults locked per Meta v21 spec, needs owner explicit approval per §CRM-SPECIFIC + 5 Q's surfaced. Zero code changes. Recommended implementation order: (1) coordinated CR-042 + BUG-009 backend, (2) CR-042 frontend, (3) CR-043 Part A, (4) CR-036, (5) CR-043 Part B once spec arrives. |
| 2026-07-03 (addendum) | **CR-042 / BUG-009 / CR-043 / CR-036** | Owner answers received. **CR-042**: all Q1-Q4 answered → Intake complete, ready for Planning. **BUG-009**: recommendation locked on file (option a — deep-link, ~15 LOC). **CR-043**: scope narrowed to Customers page + popover UX rework; other surfaces deferred to future CR. **CR-036**: defaults locked per Meta v21 spec (image+video+doc+audio; Meta caps; pass-through via `/uploads` handle). Owner directive: DO NOT start impact analysis until Planning agent is invoked. Addendum in `discovery/SESSION_2026_07_03_BATCH_INTAKE.md` §Addendum. |
| 2026-07-03 | **BATCH INTAKE** | — → **📋 5 items registered from owner report**. INTAKE role. (1) **CR-042** Download message report per campaign/event (feature, P2, awaits Q1-Q4). (2) **BUG-009** "Details" button on Marketing > History does nothing (bug, P2, `CampaignHistoryPage.jsx:164-166` missing onClick, awaits Q1). (3) **CR-043** Tag/label filter extension of CR-034 (feature, P2, awaits Q1 BLOCKING on which page). (4) Owner question on Excel export tag field answered inline; surfaced **CR-035 dashboard drift** (code shipped, dashboard says planning). (5) Media/header not working = duplicate of **CR-036** (already registered, awaits Q1-Q3). Zero code changes. Batch intake doc: `discovery/SESSION_2026_07_03_BATCH_INTAKE.md`. |
| 2026-07-03 | **CR-026** | 📋 Registered (2026-06-17) → **✅ IMPLEMENTED + LIVE-VERIFIED · Frontend-only, ~38 LOC across 2 files**. Re-validated plan against current code (2026-07-03) — zero drift; bonus finding that backend BUG-006 `$or` fallback auto-handles legacy campaigns. Applied 6 edits: (CampaignsPage.jsx: 1 import + inline Messages button + dropdown item · MessageStatusPage.jsx: 1 import + URL-param useEffect + blue Filtered banner with Clear). Lint clean. **Live browser test on prod DB** (owner@jehsnest.com): 10/10 checks PASS — 8 "Messages" inline buttons visible on 8 completed campaigns, hidden on 1 draft; click on "Paneer Masala + Chicken Masala" → URL updated to `?campaign_id=dfa1fed9-2ee8-4542-a160-fefe3f91853f`, blue banner shows correct campaign name, filter dropdown auto-populated, message list correctly filtered to 5 recipients (Sapna/jayshree/Sapna Maam/Abhishek Goyal/saurav); Clear button reset filter AND URL. Mockup: `frontend/public/cr026_mockup.html`. |
| 2026-07-03 | **CR-037** | 📋 Planning complete (2026-07-01) → **✅ IMPLEMENTED + REGRESSION VERIFIED · P1 BUG FIXED**. Re-validated plan against current code (2026-07-03) — zero drift on target lines 712 & 719-722. Discovered 1 additional downstream consumer (POS `/api/pos/templates` at `pos.py:2999-3018`) and 1 additional writer (edit-template resets to draft at `whatsapp.py:220-227`) — both **improved** by this fix, no additional code needed. Applied 2 edits: (1) line 712 projection now includes `status:1`, (2) lines 719-731 gate `"approved"` overwrite behind `current_status != "rejected"`. `authkey_wid` still back-filled on rejected templates so owner can resubmit. **Logic simulation**: 4/4 scenarios PASS (rejected→rejected, draft→approved, pending→approved, approved→approved). **pytest regression**: `test_cr039_webhook.py -n 0 -v` → **11/11 PASS** in 15.74s. Prod DB scan: 1 existing rejected template (`order_bill_test`, INVALID_FORMAT) now protected from future overwrites. |
| 2026-07-03 | **CR-041** | 📋 Registered → **✅ IMPLEMENTED + REGRESSION VERIFIED · P1 HIGH BUG FIXED**. Owner approved Q1 (Option A) + Q2 (gate only temporal fields, leave metadata ungated). Safe defaults on Q3 (SEPARATE backfill) + Q4 (LATER CI). Applied ~30 LOC re-ordering to `routers/whatsapp.py:1453-1495` — timestamp block moved AFTER state-machine gate, gated by `applied`. Metadata fields (`meta_message_id`, `keypress`, `button_param_value`, `channel`) left ungated. **pytest regression**: `test_cr039_webhook.py -n 0 -v` → **11/11 PASS** in 15.54s. Hot-reload only; no supervisor restart. Zero regression on other tests. |
| 2026-07-03 | **CR-041** | — → **📋 REGISTERED · DISCOVERY + IMPACT ANALYSIS + PLANNING COMPLETE · P1 HIGH**. Pre-existing bug surfaced by QA (Test 11 failure) during CR-039 regression. |
| 2026-07-03 | **CR-039 QA** | — → **✅ QA COMPLETE — 10/11 tests PASS**. `testing_agent_v3_fork` created `/app/backend/tests/test_cr039_webhook.py` with 11 pytest tests. All CR-039-specific behaviour verified working. Test 11 (duplicate delivered timestamp preservation) FAILED because of pre-existing timestamp-overwrite bug — routed to CR-041 as separate discovery. Success rate 91%. Login regression PASSED. QA report: `/app/test_reports/iteration_1.json`. |
| 2026-07-03 | **CR-039** | 📋 Registered → **✅ IMPLEMENTED + LIVE-TESTED · P1 CRITICAL BUG FIXED**. Option A applied to `routers/whatsapp.py` — composite `(message_id, customer_phone)` lookup with legacy fallback + `verdict="ambiguous_row"` skip on persistent mismatch. **Test A (live 3-recipient Jeh's Nest campaign at 09:29 UTC)**: AuthKey still returned identical LogID `e7c8c949d03048b05dbc981aecf4bb26` for all 3 sends, but composite lookup correctly routed each webhook to its own row — 9811657657→delivered ✅, 9035133228→rejected ✅, 7505242126→rejected ✅. Zero rows stuck at pending. **Test C (synthetic ambiguous_row)**: fake webhook with mismatching mobile → verdict `ambiguous_row`, no wrong-row update. **Test D (empty payload smoke)**: HTTP 200 `{"success":false,"error":"logid required"}`. Zero regressions on single-recipient path. |
| 2026-07-03 | **INV-2026-07-03-04** | — → **📋 REPORTED · P1 CRITICAL**. Investigation of live Jeh's Nest campaign that surfaced CR-039. All 3 recipient rows created 5 ms apart with identical `message_id` and byte-identical `authkey_raw_response`. Two callbacks arrived matching correct mobiles per AuthKey but hit row 1 in CRM due to `find_one` determinism. Fully documented; root cause: CRM defect (webhook lookup key not unique). Route to CR-039 for fix and CR-040 for AuthKey escalation. |
| 2026-07-03 | **INV-2026-07-03-03** | — → **📋 REPORTED · ROOT CAUSE = EXTERNAL CONFIG**. WhatsApp webhook not updating dashboard. Analysed 500 `whatsapp_callback_logs`: 84.4% verdict `no_matching_row`. Live-tested Jeh's Nest after owner updated AuthKey webhook URL → automation single-send succeeded end-to-end (2m 3s round-trip); campaign single-recipient succeeded (2m 9s). BUT campaign multi-recipient exposed a NEW code defect → INV-2026-07-03-04 + CR-039. Original external-config issue confirmed resolved for Jeh's Nest via owner action. |
| 2026-07-03 | **CR-038** | — → **📋 REGISTERED · DISCOVERY COMPLETE**. New CR registered from INV-2026-07-03-02 (owner concern: can CR-024 minute-cron hang server?). Finding: NOT a defect — max_instances=1 + coalesce=True + 15s httpx timeouts + atomic claim + async I/O make server-hang impossible. BUT sequential outer `for camp in due:` loop in `core/campaign_jobs.py:219` creates user-visible campaign delivery delays under scale (3×5000-recipient campaigns due same minute → ~7.5 min total wall-clock). MEDIUM risk when scale grows. 6 options sized (A parallelise / B time budget / C fire-and-forget / D reduce delay / E raise cap / F motor timeout). No implementation until owner priority + Q1-Q4 answered. Discovery: `discovery/CR_038_CAMPAIGN_SCHEDULER_SCALE_OUT_DISCOVERY.md`. |
| 2026-07-03 | **INV-2026-07-03-01** | — → **📋 REPORTED**. Prod error log `e1.txt` attributed to Customer App (`/var/www/customer-app5th-march/backend/server.py:987 get_app_config`), NOT CRM. Root cause: MongoDB Atlas cluster `ac-g2irjm2.xdqqdpi.mongodb.net` had NO PRIMARY (2 SECONDARY + 1 UNKNOWN) — replica-set election failure. CRM impact: none observed (e2.txt shows scheduler healthy). Shared collections at risk: `customer_app_config`, `customer_otps`. Recommendation: DevOps investigates Atlas primary election; Customer-App team considers `readPreference=secondaryPreferred` hardening. No CRM code change required. |
| 2026-07-03 | **INV-2026-07-03-02** | — → **📋 REPORTED**. Owner-triggered risk audit of CR-024 per-minute campaign scheduler. Reviewed `core/scheduler.py`, `core/campaign_jobs.py`, `routers/campaigns.py::_execute_campaign_send`, `core/whatsapp.py::send_bulk_messages`. Confirmed: cannot hang server (async event loop, bounded timeouts, single-instance lock, coalescing, atomic claim, per-tick cap of 100, per-campaign try/except, auto-stale marking at 24h). Advisory findings: (1) sequential outer loop scale ceiling → registered as CR-038, (2) `os.environ['CAMPAIGN_TIMEZONE']` at import-time has no fallback → brittle startup, (3) motor default `serverSelectionTimeoutMS=30000` could compound if Atlas loses PRIMARY. |
| 2026-07-01 | **CR-034** | 🔵 Intake complete → **🔵 PLANNING COMPLETE · IMPLEMENTATION GATE OPEN**. All Q1-Q6 locked: Q1=CustomersPage row + Customer Detail modal, Q2=OR default + AND toggle, Q3=keep catalog, Q4=max 30 chars case-preserving, Q5=tier stays separate, Q6=auto-backfill VIP. Impact analysis complete. `planning/CR_033_CR_034_IMPACT_ANALYSIS.md`. |
| 2026-07-01 | **CR-033** | 🔵 Discovery complete → **🔵 PLANNING COMPLETE · IMPLEMENTATION GATE OPEN**. All Q1-Q6 locked: Q1=all phases together, Q2=multi-select OR / cross-filter AND, Q3=wider Dialog + accordion sections, Q4=20 filters approved. Impact analysis + impl plan: `planning/CR_033_CR_034_IMPACT_ANALYSIS.md`. Mockup: `/cr033_cr034_mockup.html`. |
| 2026-07-01 | **CR-034** | — → **🔵 INTAKE COMPLETE**. Customer tag system (Approach A: embedded `Customer.tags` + per-tenant `available_tags` catalog). 5 new endpoints + tag filter in `build_customer_query`. UI on CustomersPage (chips + inline input + bulk action) and AudiencesPage (tag filter). Backfill auto-tags 46 `vip_flag=true` customers with `VIP`. Zero hotspot files. Intake: `discovery/CR_034_CUSTOMER_TAG_SYSTEM_INTAKE.md`. Awaits Q1-Q6. |
| 2026-07-01 | **CR-033** | — → **🔵 DISCOVERY COMPLETE**. 30 additional filter dimensions proposed across 4 tiers (P0=6, P1=11, P2=13, P3=8 deferred). MVP recommended = P0 (fixes BUG-A) + P1 + cheap P2 subset ≈ 1.5 days. Live DB data-density audit informed prioritisation. Discovery: `discovery/CR_033_ADDITIONAL_AUDIENCE_FILTERS_DISCOVERY.md`. Awaits owner priority ranking + Q1-Q6. |
| 2026-07-01 | **INV-003** | — → **📋 REPORTED**. Investigated audience filter mechanics + customer tag mechanics. Findings: filters are hardcoded end-to-end (7 frontend keys, 14 backend keys); no user-defined tag system exists; "VIP" is a phantom feature (`vip` field never written to any live doc despite extensive UI); 4 latent defects surfaced (BUG-A/B/C/D). Tag system feasibility confirmed at LOW risk (~1-1.5 days). Report: `investigations/INV_003_AUDIENCE_FILTERS_AND_TAGS.md`. |
| 2026-07-01 | **CR-032** | — → **🔵 INTAKE COMPLETE**. New CR registered — per-tenant feature flag (`features.crm_templates_enabled`) to hide the CRM Template Builder section for 79% of tenants who don't use it. Self-service toggle on Settings page. Backend send path (Freshmarketer, DirectSend, campaigns) unchanged. Auto-enable backfill for the 6 existing tenants with custom_templates. Intake doc: `discovery/CR_032_CRM_TEMPLATES_FEATURE_FLAG_INTAKE.md`. Awaits planning approval + optional Q1-Q4 answers. |
| 2026-07-01 | **CR-031** | 🔵 Planning drafted → **⏸ DEFERRED**. Tab restructure of TemplatesPage parked in favour of CR-032 (feature flag first). Filter defects (INV-002 BUG-A through BUG-E) will remain for the 6 enabled tenants until adoption grows or defects escalate. |
| 2026-07-01 | **INV-002** | — → **📋 REPORTED**. Investigation of CRM template filter dropdown bug — 5 defects identified in `TemplatesPage.jsx` filter block (status counters use AuthKey only; section header gated on AuthKey list; mapped/not-mapped counters exclude CRM; mapping toggle no-ops on CRM; Draft/All counts missing). Report: `investigations/INV_002_CRM_TEMPLATE_FILTER_BUG.md`. |
| 2026-07-01 | **INV-001** | — → **📋 REPORTED**. Investigation of `custom_data` payload freedom, template mapping mechanic, and "Set Labels" vs normal template variable mapping. Confirms Pydantic `extra='allow'` on FreshmarketerCustomData; label-based mapping via `variable_labels`; two independent mapping systems (external DirectSend/webhook uses `variable_labels`, internal events use registry-backed `whatsapp_template_variable_map`). Report: `investigations/INV_001_CUSTOM_DATA_AND_LABEL_MAPPING.md`. |
| 2026-06-17 | **BUG-005/006/007** | 🔴 OPEN → **✅ FIXED**. BUG-005: Campaign filter queried `db.segments` → fixed to `db.campaigns`. BUG-006: Campaign messages logged with `run_id` as `campaign_id` → fixed to use actual `campaign_id`, backward-compatible `$or` filter added. BUG-007: Template preview literal `\n` → normalized at data load time across 3 frontend files. |
| 2026-06-18 | **CR-027** | 🔵 Planning approved → **🟢 IMPLEMENTED + CLOSED**. Part 1 implemented: 22 env variables wired to `.env`, zero hardcoding. 11 files modified (`core/auth.py`, `routers/auth.py`, `routers/customers.py`, `routers/migration.py`, `routers/menu.py`, `core/whatsapp.py`, `routers/whatsapp.py`, `core/campaign_jobs.py`, `core/pos_request_logger.py`, `server.py`, `routers/pos.py`). `.env` expanded from 3 to 25 vars. Grep audit: 0 hardcoded URLs/secrets in production code. Backend startup verified, health check passed, scheduler active. Owner decision: `CAMPAIGN_SCHEDULER_ENABLED` keeps `false` safety fallback. |
| 2026-06-18 | **CR-029** | — → **🟢 IMPLEMENTED**. Forgot Password link disabled on LoginPage.jsx (commented out). Security reason: OTP returned in API response — anyone can read OTP without WhatsApp/SMS. Backend endpoints preserved. Re-enable when OTP removed from response + delivery confirmed. |
| 2026-06-18 | **CR-024** | 🟡 Phase 1 done, "Phase 2-3 NOT built" → **🟢 CLOSED — All phases (1-4) confirmed BUILT via code investigation**. CR board was stale. Code audit found: `core/campaign_jobs.py` (291 LOC) — `compute_next_run_at()` for scheduled (one-time date+time) + recurring (daily/weekly/monthly, end conditions: never/after N/after date). `process_due_campaigns` APScheduler job every 1 min with atomic claim, stale detection (24h window), recurring recomputation. Pause/Resume endpoints with `next_run_at` recalculation. Edit-while-scheduled guard (Phase 4 P4.9). `backfill_next_run_at()` startup migration. Frontend wizard fully wired (3 schedule types, frequency selectors, end conditions). Tests: `test_campaign_jobs.py` (168 LOC). Gated by `CAMPAIGN_SCHEDULER_ENABLED=false`. |
| 2026-06-18 | **CR-028** | 📋 Registered → **🟢 IMPLEMENTED**. BUG-008 fixed: login push gated by `crm_token_registered_with_pos` flag. `_register_crm_token_with_pos()` moved to `core/auth.py` (shared). `regenerate_api_key` resets flag + pushes new key. Settings UI: POS Integration card with masked key, copy, regenerate + AlertDialog. Regression: R1-R10 pass (login skip verified, regenerate push verified, 10/10 campaign tests). |
| 2026-06-18 | **BUG-008** | — → **🔴 OPEN**. CRM token pushed to POS on every login (should be conditional). `_register_crm_token_with_pos()` called unconditionally — CR-001 spec said first-time only. `crm_token_registered_with_pos` field written but never read as gate. No functional harm (POS returns 409) but wasted HTTP call + DB write. Bonus: `regenerate_api_key` doesn't push new key to POS. |
| 2026-06-18 | **CR-014** | 🟡 Implemented → **🟡 QA PASSED (18/18)**. testing_agent_v3 Run 2: Profile fields persist (GSTIN/PAN/FSSAI), GSTIN regex rejects invalid, bill settings merge correctly, logo upload+serve works, invoice HTML renders (token `67ddd6833bee4f33af2aaa941ee146c9`), invoice PDF downloads, 404 for nonexistent token. Frontend ProfilePage loads. QA: `qa/CR_014_E_INVOICE_QA_REPORT.md`. Still awaiting POS team for hotel folio `room_info` fields. |
| 2026-06-18 | **CR-023** | 🟡 Implemented → **🟡 QA PASSED (18/18)**. testing_agent_v3 Run 2: Template CRUD (create/list/update/delete), Meta compliance V1 (single-brace rejected), V2 (non-sequential rejected), V3 (footer vars rejected), V4 (header >1 var rejected), template name duplicate check, frontend TemplateBuilderPage + TemplatesPage load. QA: `qa/CR_023_WHATSAPP_TEMPLATE_BUILDER_QA_REPORT.md`. Still awaiting owner E2E Meta test. |
| 2026-06-18 | **CR-024** | 🟢 Implemented → **🟢 QA PASSED (28/28)**. testing_agent_v3 Run 1: 10/10 pytest unit tests (campaign_jobs) + 18/18 API tests (create/list/get/update/delete/daily-limit/pause/resume/clone/test-send/runs). Frontend CampaignsPage (5 stat cards, filter tabs) + CampaignWizardPage (3-step flow). QA: `qa/CR_024_MARKETING_CAMPAIGNS_QA_REPORT.md`. |
| 2026-06-18 | **CR-027** | 🟢 Implemented → **🟢 QA PASSED**. testing_agent_v3 Run 1: 25 env vars verified, 0 hardcoded URLs, health endpoint healthy. QA: `qa/CR_027_ENV_VARIABLES_QA_REPORT.md`. |
| 2026-06-18 | **CR-027** | — → **🔵 Discovery complete, ready to implement**. New CR registered: Hardcoded Config → Environment Variables. Investigation found 22 total env variables (not 11 as initially reported — full audit expanded scope to include Campaign Scheduler + POS Request Logging). Categories: MyGenie POS URLs (4), AuthKey URLs (4), Meta Graph API (1), JWT secret (1), External URLs (3), Campaign scheduler (2), POS request logging (7). All use hardcoded fallbacks or pure hardcoded strings — must be moved to `.env`. Discovery: `CR_027_HARDCODED_CONFIG_ENV_VARIABLES_DISCOVERY.md`. |
| 2026-06-17 | **CR-026** | — → **📋 Registered (P3)**. Campaign "View Messages" deep-link: add button on CampaignsPage rows to navigate to Message Status pre-filtered by `campaign_id`. ~½ day effort. |
| 2026-06-06 | **CR-025** | — → **⏸ Discovery Phase 0 complete — PARKED**. New CR registered: Virtual Wallet Management. Current state audit: placeholder frontend, basic credit/debit backend, 0 tenants enabled, 12 transactions (historical sync), 0 orders using wallet. 12 gaps identified. 4 phases planned (P0-P3, ~11-15 days). Primary use case: canteen/student/mess/subscription. Discovery: `CR_025_VIRTUAL_WALLET_MANAGEMENT_DISCOVERY.md`. 10 owner questions (Q1-Q10) block planning. |
| 2026-06-06 | **CR-014 Phase 3** | ⏸ Code complete → **🟡 Phase 3 (Hotel Folio Mode C) IMPLEMENTED**. Two patterns: (A) "HOTEL FOLIO" — auto-detected via `room_info.room_price > 0`, shows room charges + advance/balance + F&B items. Template: `invoice_hotel_room.html`. Tested with sunildev #000130 (Rs.5,945). (B) "GUEST FOLIO" — auto-detected via "Check In" item at Rs.0, shows day-grouped F&B breakdown with stay summary bar (60 Days / 200 Items / Rs.49,355). Template: `invoice_hotel_folio.html`. Tested with Palm House #006644 (Ms. Jamie Finlayson, 61-day stay). Mode detection + routing added to `invoice_generator.py`. POS data contract shared (`handoff/CR_014_POS_HOTEL_FOLIO_DATA_CONTRACT.md`) requesting 7 new `room_info` fields. Normal food invoices (Mode A/B) unchanged. |
| 2026-06-06 | **CR-024 Phase 1** | 🔵 Planning approved → **🟡 Phase 1 IMPLEMENTED + live tested**. S1 approved by owner. Backend: `routers/campaigns.py` (CRUD + execution engine + history + daily limit, 7 endpoints, 2 new collections). Frontend: 4 new pages matching approved mock — CampaignsPage (5 stat cards, columnar stats), AudiencesPage (3-col grid with counts + filter tags + "Create New" card), CampaignHistoryPage (table with delivery % bars), CampaignWizardPage (numbered circle steps, 2-col template picker, all schedule options enabled). Sidebar: WhatsApp + Marketing groups. Seed data: 4 segments + 4 campaigns + 3 runs. Live test: 1 msg sent to abhishek jain — AuthKey `Success`. React.Fragment bug fixed. Phase 2-3 (Scheduled/Recurring) not built. |
| 2026-06-06 | **CR-024** | — → **⏸ Discovery** → **🔵 Mock approved**. New CR registered: Segments & Marketing Campaigns Production Readiness. Deep audit found 13 gaps (4 CRITICAL: "Send Now" doesn't send, no execution engine, no scheduled/recurring). Q1-Q5 answered: Phase 1 first, 1000/day limit, skip opted-out, campaign name required, double confirm >500. Architecture locked: 3 pages (Campaigns/Audiences/History), multi-step wizard, full-page builder, sidebar rename to "Marketing". HTML mock built (`/cr024_mock.html`) with 6 screens — owner approved. Discovery: `CR_024_SEGMENTS_MARKETING_CAMPAIGNS_DISCOVERY.md`. Next: planning doc. |
| 2026-06-06 | **einvoice_token** | — → **IMPLEMENTED**. New variable `einvoice_token` added to `whatsapp_variables.py` (Order/Bill block, 41 total vars). Raw 32-char hex token for dynamic URL button suffix. Forwarded in `pos.py` send_bill event_data alongside `einvoice_link`. Discovery: token available in invoice_generator return but not forwarded. Phase 2 (AuthKey button param wiring) deferred — owner to verify AuthKey includes button vars in `bodyValues`. |
| 2026-06-06 | **CR-023 Phase 3** | 🟡 Phase 2 done → **🟡 Phase 2 + 3 IMPLEMENTED**. Two features: (A) "Add Variable" button — orange pill below body textarea, inserts `{{N}}` at cursor position, auto-increments (max+1). Header gets "Add {{1}}" button, disabled after first use (Meta 1-var limit). (B) Dynamic URL button — Static/Dynamic radio toggle on URL buttons. Dynamic: base URL input + `{{1}}` chip + sample URL input (labeled "BASE URL" / "SAMPLE URL (REQUIRED BY META)"). Backend sends `example` array to Meta. V5 validation updated for dynamic URLs. UX fix: clearer labels replaced confusing placeholders. E2E submission test: `invoice_bill_test_2` template with dynamic "View Bill" button — payload correctly formatted, reached Meta (rejected for body length, not our code). Planning: `CR_023_PHASE3_ADD_VARIABLE_DYNAMIC_URL_PLAN.md`. |
| 2026-06-06 | **CR-023 Phase 2** | 🟡 Phase 1 implemented → **🟡 Phase 1 + Phase 2 IMPLEMENTED**. Investigation: owner's `order_bill_test` template was REJECTED by Meta (`INVALID_FORMAT` — body `{1}` not `{{1}}`). Local status stale (pending, never synced). Root cause: zero frontend validation against Meta standards. **Phase 2 implemented**: `validateMetaCompliance()` function with 10 checks (V1 single-brace, V2 sequential vars, V3 footer no vars, V4 header max 1 var, V5 URL button, V6 phone button, V7 QR text required, V8 media URL, V9 no leading underscore, V10 examples no curly braces). Real-time inline warnings for V1/V3/V4/V9. Full error box on submit attempt. Backend safety net (V1-V4) in `create_meta_template()`. Files: `TemplateBuilderPage.jsx` (validation + inline hints), `routers/whatsapp.py` (backend gate). Verified: 4/4 backend curl pass, 5 frontend screenshots confirmed. Planning doc: `planning/CR_023_PHASE2_META_VALIDATION_V1_V10_PLAN.md`. |
| 2026-06-06 | **CR-023** | — → **🟡 Discovery** → **🔵 Planning + Mock approved** → **🟡 Phase 1 IMPLEMENTED**. Backend: Meta API v21, en_US locale, image header examples, status check endpoint, duplicate name check, detailed Meta errors. Frontend: new full-page `/template-builder` with name validation, char limits, 5 header types, body variable examples, buttons UI (3 types, max 3), WhatsApp live preview, status tracker with auto-polling, duplicate warning. Access via Templates page → "+ Add Template". All existing modules untouched. 52/52 regression pass. Next: owner E2E test with real Meta submission. |
| 2026-06-06 | **CR-022** | — → **🟢 CLOSED**. 4 POS-side coupon bugs fixed: (B1) `POSCartItem.food_id` alias now accepts `item_id` — POS validate was not matching items by `eligible_food_ids` because POS sends `item_id` not `food_id`. (B2) Order webhook cart_dicts `category_id` was hardcoded to `None` — now maps from `oi.item_category`. (B3) `display_title` added to `/pos/coupons/available` and `/pos/coupons/validate` — auto-generated descriptive titles (e.g., "Buy 1 Get 2 Free", "Every 3rd Rs.100 off"). (B4) `same_item_required` frontend edit hydration fixed — was defaulting to `true` via `!== false` check, causing BXG coupons to load as same-item BOGO. Changed to `=== true \|\| offer_type === "bogo"`. **QA: 142/142 pass** (all existing suites green). Files: `models/schemas.py`, `routers/pos.py`, `core/coupon.py`, `pages/CouponsPage.jsx`. Decisions: D1-D4 in DECISIONS_LOG.md. |
| 2026-06-06 | **CR-021** | — → ⏸ discovery → ⏸ planning → 🟡 implemented → **🟢 CLOSED**. Coupon engine: (B1) distribute-first benefit selection across distinct eligible item-lines (was: cheapest greedy single-line) — fixes BOGO/BXG/Nth landing on cheapest item only. (B2) Universal POS-zero CRM safety net — when POS sends `coupon_discount=0` AND CRM computes > 0, CRM records with `discount_mismatch=True` and increments `total_used`. Applies to V1/V2/V3-B/V3-C (no whitelist per owner D3 "if POS sends by mistake CRM shd honour and record drift in log"). Closes silent usage-limit loop. (B3) `per_user_limit` default flipped to Unlimited (was 1); Pydantic + runtime coercions fixed. (Hidden) Runtime `or 1` in `core/coupon.py:1727` + `routers/coupons.py:194` caught during planning audit. **QA: 142/142 pass** (V3-B 49/49 + V3-C 41/41 + new `qa_cr021` 52/52). Files: `core/coupon.py`, `routers/pos.py`, `routers/coupons.py`, `models/schemas.py`, `pages/CouponsPage.jsx`, new `tests/qa_cr021_distribute_and_pos_zero.py`. Docs: discovery/planning/closeout/impl_report. |
| 2026-06-05 | **CR-014** | 🟡 In flight → **⏸ CODE COMPLETE — live test PARKED**. All 4 buckets implemented: (B1) Profile page expansion — 10 fields + VAT + MyGenie auto-fetch on login; (B2) Bill Settings — 18 configurable fields, logo upload, color pickers; (B3) Invoice generator — Jinja2 HTML template + weasyprint PDF, fully dynamic from bill_settings, Mode A (food GST) + receipt fallback; (B4) Invoice public routes — `/api/invoices/{token}` (HTML) + `/pdf`, invoices collection with dedup; (B5) POS webhook hook — invoice generated inline before send_bill trigger, einvoice_link injected, failure-safe. Test invoice KM/010585 verified live (HTML + PDF). **PARKED**: POS + AuthKey webhooks not pointed at this pod. |
| 2026-06-05 | **CR-019** | ⏸ Plan drafted → **❌ CANCELLED**. Owner: "not needed". |
| 2026-06-05 | **CR-014** | ⏸ Discovery parked → **🟡 Phase 1 discovery in progress**. Owner answered C1=a (replace address with 4-field split), C2=a (allow blank GSTIN/FSSAI). Profile page fields from login API. Phase 1 = Profile page expansion. |
| 2026-06-05 | **CR-020** | 🟢 IMPLEMENTED → **🟢 CLOSED — QA report written, 18/18 ACs pass, owner confirmed all gates passed**. QA doc: `qa/CR_020_TEMPLATE_VARIABLE_PICKER_QA_REPORT.md`. Backend V6–V10 curl pass. Frontend S1–S5 screenshots verified. |
| 2026-06-05 | **CR-020** | ⏸ Planning → **🟢 IMPLEMENTED + VERIFIED**. S1–S5 sign-off approved → full implementation. Backend: `block` field on 40 vars (7 blocks), 3 menu vars, `menu_pick` mode validation + resolution, menu sample data. Frontend: `<VariablePicker />` reusable grouped popover (search, suggested, recently-used, alphabetical sort, green/amber dots), `<MenuPickModal />` (Items/Categories tabs), TemplatesPage rewired (wider modal). V1–V10 curl pass. Screenshots verified. Post-fix: alphabetical sort + wider modal per owner feedback. |
| 2026-06-05 | **CR-020** | ⏸ Discovery → ⏸ **Planning drafted**. HTML mock approved. Q1–Q9 answered (Q1=static menu from POS API, Q3=POS sync, Q6=menu last, Q8=reusable). Planning doc: 7 files, 18 ACs, mandatory live API validation protocol. S1–S5 sign-off approved. Doc: `planning/CR_020_TEMPLATE_VARIABLE_PICKER_PHASE_1_PLAN.md` |
| 2026-06-05 | **CR-020** | — → ⏸ **Discovery drafted**. UX restructure: flat 37-var dropdown → single intelligent popover w/ 7 grouped blocks + Menu variable family. Doc: `discovery/CR_020_TEMPLATE_VARIABLE_PICKER_GROUPED_UX_DISCOVERY.md` |
| 2026-06-05 | **CR-019** | — → ⏸ **Planning drafted**. Root cause: UI exposes `send_bill_manual`/`auto`, trigger code only reads `send_bill` after hard-coded collapse. 3 tenants silently broken (Mygenie Dev / Mayur's Kitchen / Jeh's Nest = 2,388 unsent bills). Plan: remove dead UI keys, move `send_bill` from CRM_EVENTS → POS_EVENTS, migration script (idempotent, dry-run mandatory), loud-log skip. Awaiting Q1/Q2/Q3. Docs: `discovery/CR_019_*.md` + `planning/CR_019_*.md` |
| 2026-06-05 | live debug | Owner repointed POS → orders 000457 (Mygenie Dev) + 010585 (Kunafa) landed @ 04:20 UTC. `send_bill` fired E2E both tenants. Kunafa delivered+read in 8s. Mygenie Dev customer received WhatsApp but DB stuck at `pending` — AuthKey delivery webhook URL on Mygenie Dev's AuthKey account points elsewhere. Owner deferred fix. |
| 2026-06-05 | Mygenie Dev | Owner added missing `send_bill` mapping via CRM tab (template 36320 `payment_bill`, enabled, 2026-06-05 04:03:40 UTC) — workaround for CR-019 bug until proper fix lands. |
| 2026-06-05 | repo | Re-bootstrapped from `main` (HEAD `18e879d`, 2026-05-29). First clone went to `dev` (repo default) — caught and re-cloned with `-b main`. memory/ folder on main preserved (8 docs). Pod URL: `63c9eabd-…`. |
| 2026-05-29 | **CR-018** | ⏸ discovery → **🟢 CLOSED**. Approved, implemented, verified in single session. 3 fields added to `/pos/max-redeemable`: tier upgrade projection. POS handoff updated. |
| 2026-05-29 | **CR-018** | — → ⏸ **REGISTERED + Discovery complete**. Feature: projected tier upgrade on `/pos/max-redeemable`. Awaiting owner approval. |
| 2026-05-29 | **CR-017** | — → ⏸ → **🟢 CLOSED**. Registered, discovered, approved, implemented, verified in single session. 3 fields added to `/pos/max-redeemable`. POS handoff updated. |
| 2026-05-29 | **CR-015** | 🟡 code complete + data clean → **🟢 CLOSED — live test passed**. Orders 869331 (009577, Rs.409, abhi123, points_balance=70) and 869333 (009579, Rs.2571, abhishek jain, points_balance=128) — both WhatsApp sent + read, all 7 slots correct. |
| 2026-05-29 | CR-015 | 🟡 Code complete, live test parked → 🟡 **CODE COMPLETE + DATA CLEAN**. POS repointed to preview. Order 869329 received (WhatsApp sent+read). {{6}} semantic mismatch found (`points_earned` mapped where template says "Loyalty Points Used") → fixed to `loyalty_points_used`. Full audit across all 4 R689 templates (18 slots): **0 remaining mismatches**. Awaiting 1 clean order for formal closure. |
| 2026-05-29 | CR-015 | 🟡 Day 3 done, T7 pending → 🟡 **CODE COMPLETE, live test parked**. T7 committed ({{7}} `points_earned`→`points_balance`; {{4}}/{{5}} were already fixed via UI). T2 skipped (owner: resolver handles int→str). Live test parked (POS at prod, order 009573 didn't land on preview). |
| 2026-05-29 | CR-015a | ⏸ frozen spec → 🟢 **IMPLEMENTED** (backend: 14 T5 sample values in `customers.py` sample-data — curl-verified 37 keys; frontend: registry-example fallback in `WhatsAppAutomationContent.jsx` + `TemplatesPage.jsx`; preview shows values, no "NA"). Doc: `planning/CR_015A_PREVIEW_SAMPLE_DATA_FROZEN_SPEC.md` |
| 2026-05-29 | CR-015c | — → 🟢 **Demo login FULLY REMOVED** (owner-approved). Backend `/demo-login` endpoint + constants + `is_demo` schema field; frontend Demo Login button + AuthContext demo code + DemoModeBanner (deleted) + CustomersPage `isDemoMode`. Tests switched to real login (11 passed). demo-login now 404. Doc: `discovery/CR_015C_REMOVE_DEMO_LOGIN_DISCOVERY.md` |
| 2026-05-29 | CR-015b | — → 🟢 **Dead variable-mapping code REMOVED** (owner-approved B2 + Segments leftovers; orphaned mapping modal cluster on WhatsApp Automation page + unused `availableFields`/`getPreviewMessage` on Segments deleted; lint/compile clean, zero residual refs). Doc: `discovery/CR_015B_DEAD_VARIABLE_MAPPING_CODE_DISCOVERY.md` |
| 2026-05-29 | CR-015a | — → ⏸ **Frozen spec written** (`planning/CR_015A_PREVIEW_SAMPLE_DATA_FROZEN_SPEC.md`, Option A + partial B); awaiting impl after CR-015b |
| 2026-05-29 | CR-015 | 🟡 Day 3 frozen → 🟡 **Day 3 DONE — T4+T6 landed, T7 dry-run complete, awaiting owner commit** (119/119 tests, 5/5 smoke probes, frontend compiles) |
| 2026-05-29 | CR-015 | 🟡 Day 2 done → 🟡 **Day 3 spec FROZEN** (T6+T7+T4 freeze doc at `planning/CR_015_DAY_3_FROZEN_SPEC.md`, 17 acceptance checks) |
| 2026-05-29 | CR-015 | 🟡 Day 2 frozen → 🟡 **Day 2 DONE — T3 landed** (`build_order_event_context` + 3 pos.py callsites refactored, 119/119 tests, lint clean) |
| 2026-05-29 | CR-015 | 🟡 Day 1 done → 🟡 **Day 2 spec FROZEN, T3 ready for implementation** (freeze doc at `planning/CR_015_DAY_2_FROZEN_SPEC.md`) |
| 2026-05-29 | CR-015 | 🟡 Phase 1 plan approved → 🟡 **Day 1 DONE** (T1 resolver hardening + T5 registry expansion landed, 109/109 tests, live smoke confirmed Bug #1 resolved) |
| 2026-05-29 | CR-015 | ⏸ discovery parked → 🟡 **Phase 1 plan drafted, awaiting sign-off** (Q1–Q8 all answered `a`) |
| 2026-05-29 | CR-016 | ⏸ discovery parked → ⏸ **deferred to next sprint** (owner: "we have almost definate event we used need to ensure they map and fire correctly") |
| 2026-05-29 | sprint queue | reaffirmed: CR-015 (P1) → CR-014 (P2); CR-016 out of this sprint |
| 2026-05-29 | pod URL | rotated to `a28cb9e3-…` (AuthKey webhook updated by owner) |
| 2026-05-28 evening | CR-016 | created → ⏸ discovery parked (cooldown removed per owner) |
| 2026-05-28 evening | CR-015 | created → ⏸ discovery parked |
| 2026-05-28 evening | CR-014 | discovery parked → discovery parked + Profile fields appendix added |
| 2026-05-28 evening | **CR-004 P3.5** | parked → **🟢 closed (Option A live test passed; 17/17 ACs)** |
| 2026-05-28 afternoon | CR-004 P3.5 | implementation complete → ⏸ parked awaiting Option A live test |
| 2026-05-28 afternoon | CR-004 P3.5 | receive-side hotfix applied (form-urlencoded parser) |
| 2026-05-28 morning | CR-014 | created → ⏸ discovery parked |
| 2026-05-26 | CRM 1.0 baseline | closed |

---

## How to update this dashboard

1. After any CR phase transition, edit the row's `Phase` + `Status` + `Last touched` cells.
2. Append a row to "Recent transitions" with date + CR + from→to.
3. If a brand-new CR is registered, add a new row above the relevant section and reference its discovery doc.
4. If a CR is closed, change light to 🟢 and clear "Blockers / Owner asks" column.

---

**End of dashboard.**

|| **BUG-020** | **"Unknown" name in WhatsApp templates — fallback to "Guest"** | **✅ FIXED + QA PASS (2026-08-04) — 26/26 testing_agent PASS (iteration_1)** | P1 | `core/whatsapp_variables.py` + `core/whatsapp.py` — 2 edits, 4 LOC | 2026-08-04 |
|| **BUG-021** | **POS order does not update existing customer name / email** | **✅ FIXED + QA PASS (2026-08-04) — 26/26 testing_agent PASS (iteration_1)** | P1 | `routers/pos.py` — 1 edit, 4 LOC | 2026-08-04 |
|| **BUG-022** | **Migration re-sync overwrites CRM-edited customer names with "Unknown"** | **✅ FIXED + QA PASS (2026-08-04) — 26/26 testing_agent PASS (iteration_1)** | P1 | `routers/customers.py` — 3 edits (helper + 2 guards), 11 LOC | 2026-08-04 |
|| **CR-071** | **B2B Customer Capture — GST fields + is_b2b across full pipeline** | **✅ QA PASS 13/13 (2026-08-04, iteration_1) · Owner smoke pending** | P1 | `routers/pos.py`, `routers/customers.py`, `models/schemas.py`, invoice generator. POS API contract v2 FINAL published. | 2026-08-04 |


|| **BUG-023** | **`weasyprint` missing from requirements.txt — `einvoice_token` empty in production (P0)** | **✅ FIXED + QA PASS (2026-08-04) — 26/26 testing_agent PASS (iteration_1)** | P0 | `requirements.txt` (+weasyprint==69.0) + `services/invoice_generator.py` (DB insert before PDF + try/except) | 2026-08-04 |
|| **BUG-024** | **Template button URL missing `/api/invoices/` — Bill button opens 404** | **🔴 OPEN — Intake complete. Q1 (fix approach) + Q2 (final_bill?) open. 3 affected templates: bill_4, testbill1, final_bill.** | P1 | Q1: resubmit vs nginx redirect vs frontend route? Q2: fix final_bill too? Intake: `discovery/BUG_024_TEMPLATE_BUTTON_URL_MISSING_API_PATH_INTAKE.md` | 2026-08-04 |

|| **CR-072** | **Hotel Customer Document Capture — Aadhaar / ID proof upload to S3 + POS auto-recall on return visit** | **✅ QA PASS 13/13 (2026-08-04, iteration_1) · Owner smoke pending** | P1 | `routers/pos.py` (upload + GET docs), `core/s3.py`, `customer_documents` collection, `voter_id` added. POS API contract v2 FINAL. | 2026-08-04 |

|| **CR-073** | **AuthKey-created templates not syncing into CRM — buttons invisible, variable mapping blocked** | **✅ QA PASS (2026-08-04) — 26/26 testing_agent PASS (iteration_1). `imported_count=9` confirmed. button1 + kmfinalbill buttons visible.** | P1 | `routers/whatsapp.py` (+~75 lines, 1 edit). | 2026-08-04 |
